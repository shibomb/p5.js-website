---
title: getRowCount
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>Tableの総行数を返します。</p>
line: 716
isConstructor: false
itemtype: method
example:
  - |2-

     <div>
     <code>
     // /assets ディレクトリにある "blobs.csv" ファイルが与えられた場合
     //
     // ID, Name, Flavor, Shape, Color
     // Blob1, Blobby, Sweet, Blob, Pink
     // Blob2, Saddy, Savory, Blob, Blue

     let table;

     function preload() {
     table = loadTable('/assets/blobs.csv');
     }

     function setup() {
     createCanvas(200, 100);
     textAlign(CENTER);
     background(255);
     }

     function draw() {
     text('テーブルには ' + table.getRowCount() + ' 行あります。', 100, 50);
     }
     </code>
     </div>
class: p5.Table
return:
  description: このテーブルの行数
  type: Integer
chainable: false
---


# getRowCount

