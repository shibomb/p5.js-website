---
title: getNum
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>テーブルの指定された行と列からFloat値を取得します。
  行はIDで指定され、列はIDまたはタイトルで
  指定することができます。</p>
line: 1131
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    // プロジェクトの "assets" フォルダにある
    // "mammals.csv" ファイルが与えられた場合：
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leopard
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      //テーブルはカンマ区切り値 "csv" で
      //列ラベルを指定するヘッダーがあります
      table = loadTable('/assets/mammals.csv', 'csv', 'header');
    }

    function setup() {
      print(table.getNum(1, 0) + 100);
      //id 1 + 100 = 101
      describe('画像は表示されません');
    }
    </code>
    </div>
class: p5.Table
params:
  - name: row
    description: |
      <p>行ID</p>
    type: Integer
  - name: column
    description: |
      <p>列名（文字列）または
                                        ID（数値）</p>
    type: String|Integer
return:
  description: ''
  type: Number
chainable: false
---

# getNum
