---
title: getArray
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>テーブルのすべてのデータを取得し、多次元配列として返します。</p>
line: 1285
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    // プロジェクトの "assets" フォルダにある
    // "mammals.csv" ファイルが与えられた場合
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leoperd
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      // テーブルはカンマ区切り値 "CSV" で、
      // 列ラベルのヘッダーを指定しています
      table = loadTable('/assets/mammals.csv', 'csv', 'header');
    }

    function setup() {
      let tableArray = table.getArray();
      for (let i = 0; i < tableArray.length; i++) {
        print(tableArray[i]);
      }
      describe('画像は表示されません');
    }
    </code>
    </div>
class: p5.Table
return:
  description: ''
  type: Array
chainable: false
---


# getArray

