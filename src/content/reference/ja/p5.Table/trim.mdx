---
title: trim
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>文字列テーブル値から、スペースやタブなどの先頭と末尾の空白を削除します。
  列が指定されていない場合、すべての列と行の値がトリムされます。
  特定の列は、IDまたはタイトルで参照できます。</p>
line: 824
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender"><code>
     function setup() {
     let table = new p5.Table();

     table.addColumn('name');
     table.addColumn('type');

     let newRow = table.addRow();
     newRow.setString('name', '   Lion  ,');
     newRow.setString('type', ' Mammal  ');

     newRow = table.addRow();
     newRow.setString('name', '  Snake  ');
     newRow.setString('type', '  Reptile  ');

     table.trim();
     print(table.getArray());
     }

     // 出力:
     //  0  "Lion"   "Mamal"
     //  1  "Snake"  "Reptile"
     </code></div>
class: p5.Table
params:
  - name: column
    description: |
      <p>列ID（数値）
                                       または名前（文字列）</p>
    type: String|Integer
    optional: true
chainable: false
---


# trim

