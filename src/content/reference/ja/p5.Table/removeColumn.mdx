---
title: removeColumn
module: IO
submodule: Table
file: src/io/p5.Table.js
description: >
  <p><a href="/reference/p5/removeColumn/">removeColumn()</a>を使用して、Table
  オブジェクトから既存の列を削除します。
  削除する列は、
  そのタイトル（文字列）またはインデックス値（整数）で
  指定できます。removeColumn(0)は最初の列を削除し、removeColumn(1)
  は2番目の列を削除します。以下同様です。</p>
line: 888
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの"assets"フォルダにある
     // "mammals.csv"ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //私のテーブルはカンマ区切り値"csv"で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     table.removeColumn('id');
     print(table.getColumnCount());
     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
params:
  - name: column
    description: |
      <p>列名（文字列）またはID（数値）</p>
    type: String|Integer
chainable: false
---


# removeColumn

