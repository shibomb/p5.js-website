---
title: columns
module: IO
submodule: Table
file: src/io/p5.Table.js
description: >
  <p>テーブルの列名を含む配列です。テーブルが "header" パラメータで

  読み込まれた場合に利用可能です。</p>
line: 43
isConstructor: false
itemtype: property
example:
  - |-

    <div class="norender">
    <code>
    // プロジェクトの "assets" フォルダにある
    // "mammals.csv" ファイルが与えられた場合：
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leopard
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      //テーブルはカンマ区切り値 "csv" で
      //列ラベルを指定するヘッダーがあります
      table = loadTable('/assets/mammals.csv', 'csv', 'header');
    }

    function setup() {
      //列名を表示します
      for (let c = 0; c < table.getColumnCount(); c++) {
        print('column ' + c + ' is named ' + table.columns[c]);
      }
    }
    </code>
    </div>
class: p5.Table
type: '{String[]}'
---


# columns

