---
title: findRows
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>テーブル内で指定された値を含む行を見つけ、それらの行への参照を返します。
  配列を返すので、上記の例のように、すべての行を反復処理するには
  forを使用する必要があります。検索する列は、IDまたはタイトルで
  指定できます。</p>
line: 351
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの "assets" フォルダにある
     // CSV ファイル "mammals.csv" が与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //私のテーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     //別のヤギを追加
     let newRow = table.addRow();
     newRow.setString('id', table.getRowCount() - 1);
     newRow.setString('species', 'Scape Goat');
     newRow.setString('name', 'Goat');

     //名前が Goat の動物を含む行を見つける
     let rows = table.findRows('Goat', 'name');
     print(rows.length + ' 匹のヤギが見つかりました');
     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
params:
  - name: value
    description: |
      <p>一致させる値</p>
    type: String
  - name: column
    description: |
      <p>検索する列のID番号または
                                     タイトル</p>
    type: Integer|String
return:
  description: TableRowオブジェクトの配列
  type: 'p5.TableRow[]'
chainable: false
---


# findRows

