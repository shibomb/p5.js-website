---
title: findRow
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>テーブル内で指定された値を含む最初の行を見つけ、その行への参照を返します。
  複数の行が一致する可能性がある場合でも、最初に一致した行のみが返されます。
  検索する列は、IDまたはタイトルで指定できます。</p>
line: 289
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの "assets" フォルダにある
     // "mammals.csv" ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //私のテーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     //Zebraという名前の動物を見つける
     let row = table.findRow('Zebra', 'name');
     //対応する種を見つける
     print(row.getString('species'));
     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
params:
  - name: value
    description: |
      <p>一致させる値</p>
    type: String
  - name: column
    description: |
      <p>検索する列のID番号またはタイトル</p>
    type: Integer|String
return:
  description: ''
  type: p5.TableRow
chainable: false
---


# findRow

