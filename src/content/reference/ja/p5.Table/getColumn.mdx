---
title: getColumn
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>指定された列のすべての値を取得し、配列として返します。
  列はIDまたはタイトルのいずれかで指定できます。</p>
line: 542
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの "assets" フォルダにある
     // "mammals.csv" ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //テーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     //getColumnは直接出力できる配列を返します
     print(table.getColumn('species'));
     //出力: ["Capra hircus", "Panthera pardus", "Equus zebra"]
     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
params:
  - name: column
    description: |
      <p>返す列の文字列または数値</p>
    type: String|Number
return:
  description: 列の値の配列
  type: Array
chainable: false
---


# getColumn

