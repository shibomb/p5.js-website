---
title: getRows
module: IO
submodule: Table
file: src/io/p5.Table.js
description: >
  <p>テーブルからすべての行を取得します。<a
  href="/reference/p5/p5.TableRow">p5.TableRow</a>の配列を返します。</p>
line: 242
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの "assets" フォルダにある
     // "mammals.csv" ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //テーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     let rows = table.getRows();

     //警告：rowsはオブジェクトの配列です
     for (let r = 0; r < rows.length; r++) {
       rows[r].set('name', 'Unicorn');
     }

     //結果を表示
     for (let r = 0; r < table.getRowCount(); r++)
       for (let c = 0; c < table.getColumnCount(); c++)
         print(table.getString(r, c));

     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
return:
  description: <a href="#/p5.TableRow">p5.TableRow</a>の配列
  type: 'p5.TableRow[]'
chainable: false
---


# getRows

