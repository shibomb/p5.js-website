---
title: removeTokens
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>指定された文字（または「トークン」）を削除します。</p>
  <p>列が指定されていない場合、すべての列と行の値が処理されます。
  特定の列は、IDまたはタイトルで参照できます。</p>
line: 752
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender"><code>
     function setup() {
     let table = new p5.Table();

     table.addColumn('name');
     table.addColumn('type');

     let newRow = table.addRow();
     newRow.setString('name', '   $Lion  ,');
     newRow.setString('type', ',,,Mammal');

     newRow = table.addRow();
     newRow.setString('name', '$Snake  ');
     newRow.setString('type', ',,,Reptile');

     table.removeTokens(',$ ');
     print(table.getArray());
     }

     // 出力:
     //  0  "Lion"   "Mamal"
     //  1  "Snake"  "Reptile"
     </code></div>
class: p5.Table
params:
  - name: chars
    description: |
      <p>削除する文字をリストした文字列</p>
    type: String
  - name: column
    description: |
      <p>列ID（数値）
                                       または名前（文字列）</p>
    type: String|Integer
    optional: true
chainable: false
---


# removeTokens

