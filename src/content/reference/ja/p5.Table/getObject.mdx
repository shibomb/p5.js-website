---
title: getObject
module: IO
submodule: Table
file: src/io/p5.Table.js
description: |
  <p>テーブルのすべてのデータを取得し、オブジェクトとして返します。列名が
  渡された場合、各行オブジェクトはその属性をタイトルとして保存されます。</p>
line: 1223
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    // プロジェクトの "assets" フォルダにある
    // "mammals.csv" ファイルが与えられた場合：
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leopard
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      //テーブルはカンマ区切り値 "csv" で
      //列ラベルを指定するヘッダーがあります
      table = loadTable('/assets/mammals.csv', 'csv', 'header');
    }

    function setup() {
      let tableObject = table.getObject();

      print(tableObject);
      //オブジェクトを出力します

      describe('画像は表示されません');
    }
    </code>
    </div>
class: p5.Table
params:
  - name: headerColumn
    description: |
      <p>各行オブジェクトのタイトルとして使用される列の名前（オプション）</p>
    type: String
    optional: true
return:
  description: ''
  type: Object
chainable: false
---


# getObject

