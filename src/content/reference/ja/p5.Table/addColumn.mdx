---
title: addColumn
module: IO
submodule: Table
file: src/io/p5.Table.js
description: >
  <p><a href="/reference/p5/addColumn/">addColumn()</a>を使用して、<a href="/reference/p5/p5.Table">Table</a>オブジェクトに新しい列を追加します。

  通常、後で名前で簡単に参照できるように、タイトルを指定することをお勧めします。

  （タイトルが指定されていない場合、新しい列のタイトルはnullになります。）</p>
line: 631
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender">
     <code>
     // プロジェクトの "assets" フォルダにある
     // "mammals.csv" ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //私のテーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     table.addColumn('carnivore');
     table.set(0, 'carnivore', 'no');
     table.set(1, 'carnivore', 'yes');
     table.set(2, 'carnivore', 'no');

     //結果を表示
     for (let r = 0; r < table.getRowCount(); r++)
       for (let c = 0; c < table.getColumnCount(); c++)
         print(table.getString(r, c));

     describe('画像は表示されません');
     }
     </code>
     </div>
class: p5.Table
params:
  - name: title
    description: |
      <p>指定された列のタイトル</p>
    type: String
    optional: true
chainable: false
---


# addColumn

