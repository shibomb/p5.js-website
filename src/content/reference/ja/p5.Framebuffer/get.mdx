---
title: get
module: Rendering
submodule: ''
file: src/webgl/p5.Framebuffer.js
description: >
  <p>フレームバッファからピクセルまたはピクセル領域を取得します。</p>

  <p><code>myBuffer.get()</code>は使いやすいですが、

  <a href="/reference/p5.Framebuffer/pixels/">myBuffer.pixels</a>ほど高速ではありません。

  多くのピクセル値を読み取るには

  <a href="/reference/p5.Framebuffer/pixels/">myBuffer.pixels</a>を使用してください。</p>

  <p>パラメータなしの<code>myBuffer.get()</code>バージョンは、

  フレームバッファ全体を<a href="/reference/p5/p5.Image">p5.Image</a>オブジェクトとして返します。</p>

  <p>2つのパラメータを持つ<code>myBuffer.get()</code>バージョンは、

  それらを座標として解釈します。指定されたポイントの<code>[R, G, B, A]</code>値を

  含む配列を返します。</p>

  <p>4つのパラメータを持つ<code>myBuffer.get()</code>バージョンは、

  それらを座標と寸法として解釈します。フレームバッファのサブセクションを

  <a href="/reference/p5/p5.Image">p5.Image</a>オブジェクトとして返します。最初の2つの

  パラメータはサブセクションの左上隅の座標です。最後の2つの

  パラメータはサブセクションの幅と高さです。</p>
line: 1455
isConstructor: false
itemtype: method
class: p5.Framebuffer
return:
  description: サブセクションを<a href="#/p5.Image">p5.Image</a>オブジェクトとして。
  type: p5.Image
overloads:
  - line: 1455
    params:
      - name: x
        description: |
          <p>ピクセルのx座標。デフォルトは0。</p>
        type: Number
      - name: 'y'
        description: |
          <p>ピクセルのy座標。デフォルトは0。</p>
        type: Number
      - name: w
        description: |
          <p>返されるサブセクションの幅。</p>
        type: Number
      - name: h
        description: |
          <p>返されるサブセクションの高さ。</p>
        type: Number
    return:
      description: サブセクションを<a href="#/p5.Image">p5.Image</a>オブジェクトとして。
      type: p5.Image
  - line: 1483
    params: []
    return:
      description: フレームバッファ全体を<a href="#/p5.Image">p5.Image</a>オブジェクトとして。
      type: p5.Image
  - line: 1487
    params:
      - name: x
        description: ''
        type: Number
      - name: 'y'
        description: ''
        type: Number
    return:
      description: >-
        `(x, y)`におけるピクセルの色を色値の配列`[R, G, B, A]`として。
      type: 'Number[]'
chainable: false
---


# get

