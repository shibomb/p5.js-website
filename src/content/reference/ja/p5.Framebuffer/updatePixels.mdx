---
title: updatePixels
module: Rendering
submodule: ''
file: src/webgl/p5.Framebuffer.js
description: >
  <p><a href="/reference/p5.Framebuffer/pixels/">pixels</a>配列のRGBA値でフレームバッファを更新します。</p>

  <p><code>myBuffer.updatePixels()</code>は、<a href="/reference/p5.Framebuffer/pixels/">myBuffer.pixels</a>配列の値を変更した後にのみ呼び出す必要があります。
  このような変更は、<a href="/reference/p5.Framebuffer/loadPixels/">myBuffer.loadPixels()</a>を呼び出した直後に行うことができます。</p>
line: 1581
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      // p5.Framebufferオブジェクトを作成します。
      let myBuffer = createFramebuffer();

      // pixels配列を読み込みます。
      myBuffer.loadPixels();

      // フレームバッファの上半分のピクセル数を取得します。
      let numPixels = myBuffer.pixels.length / 2;

      // フレームバッファの上半分をピンク色に設定します。
      for (let i = 0; i < numPixels; i += 4) {
        myBuffer.pixels[i] = 255;
        myBuffer.pixels[i + 1] = 102;
        myBuffer.pixels[i + 2] = 204;
        myBuffer.pixels[i + 3] = 255;
      }

      // pixels配列を更新します。
      myBuffer.updatePixels();

      // p5.Framebufferオブジェクトをキャンバスに描画します。
      image(myBuffer, -50, -50);

      describe('灰色の長方形の上にピンク色の長方形があります。');
    }
    </code>
    </div>
class: p5.Framebuffer
chainable: false
---


# updatePixels

