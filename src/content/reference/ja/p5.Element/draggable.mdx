---
title: draggable
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素をドラッグ可能にします。</p>

  <p>パラメータ <code>elmnt</code> はオプションです。別の

  <a href="/reference/p5/p5.Element">p5.Element</a> オブジェクトが渡された場合、

  <code>myElement.draggable(otherElement)</code> のように、その他の要素がドラッグ可能になります。</p>
line: 3927
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let stickyNote;
    let textInput;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // divエレメントを作成してスタイルを設定します。
      stickyNote = createDiv('Note');
      stickyNote.position(5, 5);
      stickyNote.size(80, 20);
      stickyNote.style('font-size', '16px');
      stickyNote.style('font-family', 'Comic Sans MS');
      stickyNote.style('background', 'orchid');
      stickyNote.style('padding', '5px');

      // ノートをドラッグ可能にします。
      stickyNote.draggable();

      // パネルのdivを作成してスタイルを設定します。
      let panel = createDiv('');
      panel.position(5, 40);
      panel.size(80, 50);
      panel.style('background', 'orchid');
      panel.style('font-size', '16px');
      panel.style('padding', '5px');
      panel.style('text-align', 'center');

      // パネルをドラッグ可能にします。
      panel.draggable();

      // テキスト入力を作成してスタイルを設定します。
      textInput = createInput('Note');
      textInput.size(70);

      // 入力をパネルに追加します。
      textInput.parent(panel);

      // テキストが入力されたときにhandleInput()を呼び出します。
      textInput.input(handleInput);

      describe(
        'ドラッグすると移動する2つの紫色の長方形がある灰色の正方形。上の長方形には下の長方形に入力されたテキストが表示されます。'
      );
    }

    // テキストが入力されたときにstickyNoteのHTMLを更新します。
    function handleInput() {
      stickyNote.html(textInput.value());
    }
    </code>
    </div>
class: p5.Element
params:
  - name: elmnt
    description: |
      <p>別の <a href="#/p5.Element">p5.Element</a>。</p>
    type: p5.Element
    optional: true
chainable: true
---


# draggable

