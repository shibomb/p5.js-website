---
title: mouseMoved
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: |
  <p>要素上でマウスが移動したときに関数を呼び出します。</p>
  <p><code>myElement.mouseMoved(false)</code>を呼び出すと、関数が無効になります。</p>
line: 623
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // マウスが移動したときに
      // randomColor()を呼び出します。
      cnv.mouseMoved(randomColor);

      describe('マウスがキャンバス上を移動すると、灰色の正方形の色が変わります。');
    }

    // 背景を赤、黄、青、緑の
    // いずれかで塗ります。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>要素上でマウスが移動したときに
                                     呼び出す関数。
                                     <code>false</code>で関数を無効にします。</p>
    type: Function|Boolean
chainable: true
---


# mouseMoved

