---
title: mouseClicked
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素上でマウスが押されて離されたときに関数を呼び出します。</p>

  <p><code>myElement.mouseReleased(false)</code>を呼び出すと、この関数が無効になります。</p>

  <p>注意：一部のモバイルブラウザでは、要素が素早いタップを受け取ったときにもこのイベントが
  トリガーされる場合があります。</p>
line: 578
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てる。
      let cnv = createCanvas(100, 100);

      background(200);

      // マウスの押下が終了したときに
      // randomColor()を呼び出す。
      cnv.mouseClicked(randomColor);

      describe('ユーザーがマウスの押下を解除すると、灰色の正方形の色が変わります。');
    }

    // 背景を赤、黄、青、緑の
    // いずれかで塗りつぶす。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>要素上でマウスが押されて離されたときに
                                     呼び出される関数。
                                     <code>false</code>を指定すると関数が無効になります。</p>
    type: Function|Boolean
chainable: true
---


# mouseClicked

