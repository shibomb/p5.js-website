---
title: touchMoved
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: |
  <p>ユーザーが要素にタッチして動かしたときに関数を呼び出します。</p>
  <p><code>myElement.touchMoved(false)</code>を呼び出すと、この関数を無効にします。</p>
  <p>注意：タッチ機能はモバイルデバイスでのみ動作します。</p>
line: 793
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // ユーザーがキャンバスにタッチして
      // 動かしたときに、randomColor()を
      // 呼び出します。
      cnv.touchMoved(randomColor);

      describe('ユーザーがキャンバスにタッチして動かすと、灰色の正方形の色が変わります。');
    }

    // 背景を赤、黄、青、緑の
    // いずれかの色で塗ります。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>タッチが要素上を移動したときに呼び出す関数。
                                     <code>false</code>を指定すると関数を無効にします。</p>
    type: Function|Boolean
chainable: true
---


# touchMoved

