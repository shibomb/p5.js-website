---
title: remove
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>要素を削除し、すべてのオーディオ/ビデオストリームを停止し、すべてのコールバック関数を削除します。</p>
line: 3713
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let p;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 段落要素を作成します。
      p = createP('p5*js');
      p.position(10, 10);

      describe('灰色の正方形の中央に書かれた "p5*js" というテキスト。');
    }

    // ユーザーがダブルクリックしたときに段落を削除します。
    function doubleClicked() {
      p.remove();
    }
    </code>
    </div>
class: p5.Element
chainable: false
---


# remove

