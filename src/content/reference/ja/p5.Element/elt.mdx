---
title: elt
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素の基礎となる<code>HTMLElement</code>オブジェクトです。</p>

  <p>

  <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/"
  target="_blank">HTMLElement</a>

  オブジェクトのプロパティとメソッドを直接使用することができます。</p>
line: 56
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // キャンバスのボーダースタイルを
      // 設定します。
      cnv.elt.style.border = '5px dashed deeppink';

      describe('ピンク色の破線のボーダーが描かれたグレーの正方形。');
    }
    </code>
    </div>
class: p5.Element
---


# elt

