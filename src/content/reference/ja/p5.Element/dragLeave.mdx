---
title: dragLeave
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: |
  <p>ファイルが要素からドラッグされた時に関数を呼び出します。</p>
  <p><code>myElement.dragLeave(false)</code>を呼び出すと、この機能を無効にします。</p>
line: 923
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // ファイルをキャンバスの上に
    // ドラッグし、その後離して
    // テストします。

    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // ファイルがキャンバスの上に
      // ドラッグされ、その後離れた時に
      // byeFile()を呼び出します。
      cnv.dragLeave(byeFile);

      describe('灰色の正方形。ファイルが正方形の上にドラッグされ、その後離れると「bye, file」というテキストが表示されます。');
    }

    function byeFile() {
      text('bye, file', 50, 50);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>ファイルが要素からドラッグされた時に
                                     呼び出す関数。
                                     <code>false</code>を指定すると機能を無効にします。</p>
    type: Function|Boolean
chainable: true
---

# dragLeave
