---
title: mouseReleased
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素上でマウスが離されたときに関数を呼び出します。</p>

  <p><code>myElement.mouseReleased(false)</code>を呼び出すと、この機能が無効になります。</p>

  <p>注意：一部のモバイルブラウザでは、要素が素早いタップを受け取ったときにもこのイベントが発生する場合があります。</p>
line: 533
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // マウスプレスが終了したときに
      // randomColor()を呼び出します。
      cnv.mouseReleased(randomColor);

      describe('ユーザーがマウスプレスを解除すると、灰色の正方形の色が変わります。');
    }

    // 背景を赤、黄、青、緑のいずれかに
    // 塗ります。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>要素上でマウスが離されたときに呼び出す関数。
                                     <code>false</code>を指定すると機能が無効になります。</p>
    type: Function|Boolean
chainable: true
---


# mouseReleased

