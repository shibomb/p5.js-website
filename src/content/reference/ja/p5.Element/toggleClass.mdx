---
title: toggleClass
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>要素にクラスを適用するかどうかを切り替えます。</p>
line: 2659
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    let div;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      div = createDiv('div');

      // divに'show'クラスを追加します。
      div.addClass('show');

      describe('灰色の正方形。');
    }

    // マウスが押されたときに'show'クラスを切り替えます。
    function mousePressed() {
      div.toggleClass('show');
    }
    </code>
    </div>
class: p5.Element
params:
  - name: c
    description: |
      <p>切り替えるクラス名。</p>
    type: String
chainable: true
---


# toggleClass

