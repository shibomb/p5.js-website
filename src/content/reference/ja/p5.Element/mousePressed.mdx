---
title: mousePressed
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素上でマウスが押されたときに関数を呼び出します。</p>

  <p><code>myElement.mousePressed(false)</code>を呼び出すと、この関数を無効にします。</p>

  <p>注意：一部のモバイルブラウザでは、要素が素早いタップを受け取ったときにもこのイベントが
  トリガーされる場合があります。</p>
line: 350
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てる
      let cnv = createCanvas(100, 100);

      background(200);

      // キャンバスが押されたときに
      // randomColor()を呼び出す
      cnv.mousePressed(randomColor);

      describe('マウスが押されると色が変わるグレーの正方形。');
    }

    // 背景を赤、黄、青、緑の
    // いずれかでペイントする
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>要素上でマウスが押されたときに呼び出す関数。
                                     <code>false</code>を指定すると関数を無効にします。</p>
    type: Function|Boolean
chainable: true
---


# mousePressed

