---
title: id
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>与えられた文字列を使用して要素のIDを設定します。</p>

  <p>引数なしで<code>myElement.id()</code>を呼び出すと、そのIDを文字列として返します。</p>
line: 254
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // キャンバスのIDを
      // "mycanvas"に設定します。
      cnv.id('mycanvas');

      // キャンバスのIDを取得します。
      let id = cnv.id();
      text(id, 24, 54);

      describe('灰色の背景に黒で書かれた"mycanvas"というテキスト。');
    }
    </code>
    </div>
class: p5.Element
overloads:
  - line: 254
    params:
      - name: id
        description: |
          <p>要素のID。</p>
        type: String
    chainable: 1
  - line: 286
    params: []
    return:
      description: 要素のID。
      type: String
chainable: true
---


# id

