---
title: child
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素を別の要素の子として追加します。</p>

  <p><code>myElement.child()</code>は、文字列ID、DOMノード、または

  <a href="/reference/p5/p5.Element">p5.Element</a>のいずれかを受け入れます。例えば、

  <code>myElement.child(otherElement)</code>のようになります。引数が提供されない場合、

  子DOMノードの配列が返されます。</p>
line: 2704
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      let div0 = createDiv('親');
      let div1 = createDiv('子');

      // p5.Elementを使用して
      // div1をdiv0の子にします。
      div0.child(div1);

      describe('グレーの正方形の下に「親」と「子」という言葉が書かれています。');
    }
    </code>
    </div>

    <div class='norender'>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      let div0 = createDiv('親');
      let div1 = createDiv('子');

      // div1にIDを付与します。
      div1.id('apples');

      // IDを使用して
      // div1をdiv0の子にします。
      div0.child('apples');

      describe('グレーの正方形の下に「親」と「子」という言葉が書かれています。');
    }
    </code>
    </div>

    <div class='norender notest'>
    <code>
    // この例では、ページ上に既に
    // id "myChildDiv"を持つdivが存在すると仮定しています。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      let div0 = createDiv('親');

      // IDで子要素を選択します。
      let elt = document.getElementById('myChildDiv');

      // HTMLElement オブジェクトを使用して
      // div1をdiv0の子にします。
      div0.child(elt);

      describe('グレーの正方形の下に「親」と「子」という言葉が書かれています。');
    }
    </code>
    </div>
class: p5.Element
return:
  description: 子ノードの配列。
  type: 'Node[]'
overloads:
  - line: 2704
    params: []
    return:
      description: 子ノードの配列。
      type: 'Node[]'
  - line: 2784
    params:
      - name: child
        description: |
          <p>現在の要素に追加するID、DOMノード、または<a href="#/p5.Element">p5.Element</a></p>
        type: String|p5.Element
        optional: true
    chainable: 1
chainable: false
---


# child

