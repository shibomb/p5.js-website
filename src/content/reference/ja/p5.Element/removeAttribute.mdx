---
title: removeAttribute
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素から属性を削除します。</p>

  <p>パラメータ<code>attr</code>は、属性名を文字列で指定します。例えば、

  <code>myElement.removeAttribute('align')</code>を呼び出すと、設定されている場合に

  <code>align</code>属性を削除します。</p>
line: 3341
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let p;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 段落要素を作成し、キャンバスの中央に配置します。
      // "align"属性を"center"に設定します。
      p = createP('hi');
      p.position(0, 20);
      p.attribute('align', 'center');

      describe('灰色の正方形の中央に黒で書かれた"hi"というテキスト。ダブルクリックするとテキストが左端に移動します。');
    }

    // ユーザーが段落をダブルクリックしたときに'align'属性を削除します。
    function doubleClicked() {
      p.removeAttribute('align');
    }
    </code>
    </div>
class: p5.Element
params:
  - name: attr
    description: |
      <p>削除する属性。</p>
    type: String
chainable: true
---


# removeAttribute

