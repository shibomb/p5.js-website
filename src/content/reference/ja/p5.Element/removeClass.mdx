---
title: removeClass
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>要素からクラスを削除します。</p>
line: 2575
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    // この例では、divが作成されたときにクラスが設定され、
    // マウスが押されたときに削除されます。これはCSSスタイルルールと
    // リンクして、スタイルプロパティを切り替えることができます。

    let div;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      div = createDiv('div');

      // divにクラスを追加します。
      div.addClass('myClass');

      describe('灰色の正方形。');
    }

    // ユーザーがマウスを押したときに、divから'myClass'を削除します。
    function mousePressed() {
      div.removeClass('myClass');
    }
    </code>
    </div>
class: p5.Element
params:
  - name: class
    description: |
      <p>削除するクラスの名前。</p>
    type: String
chainable: true
---


# removeClass

