---
title: dragOver
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: |
  <p>ファイルが要素上にドラッグされたときに関数を呼び出します。</p>
  <p><code>myElement.dragOver(false)</code>を呼び出すと、この関数を無効にします。</p>
line: 881
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // テストするには、ファイルをキャンバス上にドラッグしてください。

    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // ファイルがキャンバス上に
      // ドラッグされたときに
      // helloFile()を呼び出します。
      cnv.dragOver(helloFile);

      describe('灰色の正方形。ファイルが正方形の上にドラッグされると「hello, file」というテキストが表示されます。');
    }

    function helloFile() {
      text('hello, file', 50, 50);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>ファイルが要素上にドラッグされたときに
                                     呼び出す関数。
                                     <code>false</code>を指定すると関数を無効にします。</p>
    type: Function|Boolean
chainable: true
---


# dragOver

