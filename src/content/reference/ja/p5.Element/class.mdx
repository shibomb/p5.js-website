---
title: class
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素に

  <a
  href="https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/class/"
  target="_blank">class属性</a>

  を与えられた文字列を使用して追加します。</p>

  <p><code>myElement.class()</code>を引数なしで呼び出すと、現在のクラスを含む文字列を返します。</p>
line: 301
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // キャンバス要素に"small"クラスを
      // 追加します。
      cnv.class('small');

      // キャンバス要素のクラスを取得し、
      // 表示します。
      let c = cnv.class();
      text(c, 35, 54);

      describe('灰色のキャンバスに黒で書かれた"small"という単語。');

    }
    </code>
    </div>
class: p5.Element
overloads:
  - line: 301
    params:
      - name: class
        description: |
          <p>追加するクラス。</p>
        type: String
    chainable: 1
  - line: 337
    params: []
    return:
      description: '要素のクラス（存在する場合）。'
      type: String
chainable: true
---


# class

