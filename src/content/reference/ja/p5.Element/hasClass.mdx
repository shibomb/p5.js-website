---
title: hasClass
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>要素に特定のクラスが既に適用されているかどうかを確認します。</p>
line: 2618
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    let div;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      div = createDiv('div');

      // divに'show'クラスを追加します。
      div.addClass('show');

      describe('灰色の正方形。');
    }

    // マウスが押されたときに'show'クラスを切り替えます。
    function mousePressed() {
      if (div.hasClass('show')) {
        div.addClass('show');
      } else {
        div.removeClass('show');
      }
    }
    </code>
    </div>
class: p5.Element
params:
  - name: c
    description: |
      <p>確認するクラスの名前。</p>
    type: String
return:
  description: 要素が指定されたクラスを持っているかどうかを示すブール値。
  type: Boolean
chainable: false
---

# hasClass

