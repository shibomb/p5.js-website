---
title: mouseOver
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: |
  <p>マウスが要素上に移動したときに関数を呼び出します。</p>
  <p><code>myElement.mouseOver(false)</code>を呼び出すと、この関数を無効にします。</p>
line: 665
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // マウスがキャンバス上に移動したときに
      // randomColor()を呼び出します。
      cnv.mouseOver(randomColor);

      describe('マウスがキャンバス上に移動すると、灰色の正方形が色を変えます。');
    }

    // 背景を赤、黄、青、緑の
    // いずれかでペイントします。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>マウスが要素上に移動したときに
                                     呼び出す関数。
                                     <code>false</code>を指定すると関数を無効にします。</p>
    type: Function|Boolean
chainable: true
---

# mouseOver
