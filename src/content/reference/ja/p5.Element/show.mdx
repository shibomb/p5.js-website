---
title: show
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>現在の要素を表示します。</p>
line: 3475
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let p;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 段落要素を作成し、非表示にします。
      p = createP('p5*js');
      p.position(10, 10);
      p.hide();

      describe('灰色の正方形。ユーザーが正方形をダブルクリックすると、"p5*js"というテキストが表示されます。');
    }

    // ユーザーがダブルクリックしたときに段落を表示します。
    function doubleClicked() {
      p.show();
    }
    </code>
    </div>
class: p5.Element
chainable: true
---


# show

