---
title: html
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素の内部HTMLを設定し、既存のHTMLを置き換えます。</p>

  <p>2番目のパラメータ<code>append</code>はオプションです。
  <code>myElement.html('hi', true)</code>のように<code>true</code>が渡された場合、
  HTMLは既存のHTMLを置き換えるのではなく、追加されます。</p>

  <p><code>myElement.html()</code>のように引数が渡されない場合、
  要素の内部HTMLが返されます。</p>
line: 2875
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    function setup() {
      createCanvas(100, 100);

      // div要素を作成し、サイズを設定します。
      let div = createDiv('');
      div.size(100, 100);

      // 内部HTMLを"hi"に設定します。
      div.html('hi');

      describe('グレーの正方形と、その下に"hi"という単語が書かれています。');
    }
    </code>
    </div>

    <div class='norender'>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成し、サイズを設定します。
      let div = createDiv('Hello ');
      div.size(100, 100);

      // divのHTMLに"World"を追加します。
      div.html('World', true);

      describe('グレーの正方形と、その下に"Hello World"というテキストが書かれています。');
    }
    </code>
    </div>

    <div class='norender'>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成します。
      let div = createDiv('Hello');

      // コンソールに"Hello"を出力します。
      print(div.html());

      describe('グレーの正方形と、その下に"Hello!"という単語が書かれています。');
    }
    </code>
    </div>
class: p5.Element
return:
  description: 要素の内部HTML
  type: String
overloads:
  - line: 2875
    params: []
    return:
      description: 要素の内部HTML
      type: String
  - line: 2944
    params:
      - name: html
        description: |
          <p>要素の内部に配置されるHTML</p>
        type: String
        optional: true
      - name: append
        description: |
          <p>既存のHTMLに追加するかどうか</p>
        type: Boolean
        optional: true
    chainable: 1
chainable: false
---


# html

