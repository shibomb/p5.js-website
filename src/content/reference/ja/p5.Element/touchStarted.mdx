---
title: touchStarted
module: DOM
submodule: DOM
file: src/core/p5.Element.js
description: >
  <p>要素がタッチされたときに関数を呼び出します。</p>

  <p><code>myElement.touchStarted(false)</code>を呼び出すと、関数が無効になります。</p>

  <p>注意：タッチ機能はモバイルデバイスでのみ動作します。</p>
line: 749
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // キャンバス要素を作成し、
      // cnvに割り当てます。
      let cnv = createCanvas(100, 100);

      background(200);

      // ユーザーがキャンバスをタッチしたときに
      // randomColor()を呼び出します。
      cnv.touchStarted(randomColor);

      describe('ユーザーがキャンバスをタッチすると、灰色の正方形の色が変わります。');
    }

    // 背景を赤、黄、青、緑のいずれかに
    // 塗ります。
    function randomColor() {
      let c = random(['red', 'yellow', 'blue', 'green']);
      background(c);
    }
    </code>
    </div>
class: p5.Element
params:
  - name: fxn
    description: |
      <p>タッチが開始されたときに呼び出す関数。
                                     <code>false</code>を指定すると関数が無効になります。</p>
    type: Function|Boolean
chainable: true
---


# touchStarted

