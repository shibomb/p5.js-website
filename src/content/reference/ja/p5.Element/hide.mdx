---
title: hide
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>現在の要素を非表示にします。</p>
line: 3511
isConstructor: false
itemtype: method
example:
  - |-

    let p;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 段落要素を作成します。
      p = createP('p5*js');
      p.position(10, 10);

      describe('灰色の正方形の中央にある"p5*js"というテキスト。ユーザーが正方形をダブルクリックするとテキストが消えます。');
    }

    // ユーザーがダブルクリックしたときに段落を非表示にします。
    function doubleClicked() {
      p.hide();
    }
    </code>
    </div>
class: p5.Element
chainable: true
---


# hide

