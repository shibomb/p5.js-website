---
title: value
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素の値を返すか設定します。</p>

  <p><code>myElement.value()</code>を呼び出すと、要素の現在の値を返します。</p>

  <p>パラメータ<code>value</code>は、オプションの数値または文字列です。
  <code>myElement.value(123)</code>のように提供された場合、
  要素の値を設定するために使用されます。</p>
line: 3392
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let input;

    function setup() {
      createCanvas(100, 100);

      // テキスト入力を作成し、キャンバスの下に配置します。
      // デフォルト値を "hello" に設定します。
      input = createInput('hello');
      input.position(0, 100);

      describe('入力ボックスからのテキストが灰色の四角形に表示されます。');
    }

    function draw() {
      background(200);

      // 入力の値を使用してメッセージを表示します。
      let msg = input.value();
      text(msg, 0, 55);
    }
    </code>
    </div>

    <div>
    <code>
    let input;

    function setup() {
      createCanvas(100, 100);

      // テキスト入力を作成し、キャンバスの下に配置します。
      // デフォルト値を "hello" に設定します。
      input = createInput('hello');
      input.position(0, 100);

      describe('入力ボックスからのテキストが灰色の四角形に表示されます。ユーザーが四角形をダブルクリックすると、テキストは "hello" にリセットされます。');
    }

    function draw() {
      background(200);

      // 入力の値を使用してメッセージを表示します。
      let msg = input.value();
      text(msg, 0, 55);
    }

    // 入力の値をリセットします。
    function doubleClicked() {
      input.value('hello');
    }
    </code>
    </div>
class: p5.Element
return:
  description: 要素の値。
  type: String|Number
overloads:
  - line: 3392
    params: []
    return:
      description: 要素の値。
      type: String|Number
  - line: 3459
    params:
      - name: value
        description: ''
        type: String|Number
    chainable: 1
chainable: false
---


# value

