---
title: stop
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>メディア要素を停止し、その現在の時間を0に設定します。</p>

  <p><code>media.play()</code>を呼び出すと、音声/動画の再生が最初から再開されます。</p>
line: 4263
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let beat;
    let isStopped = true;

    function setup() {
      createCanvas(100, 100);

      // createAudio()を使用してp5.MediaElementを作成します。
      beat = createAudio('/assets/beat.mp3');

      describe('灰色の背景に黒で書かれた「クリックして開始」というテキスト。ユーザーがマウスを押すとビートが開始または停止します。');
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 再生状態に基づいて異なる指示を表示します。
      if (isStopped === true) {
        text('クリックして開始', 50, 50);
      } else {
        text('クリックして停止', 50, 50);
      }
    }

    // ユーザーがマウスを押したときに再生を調整します。
    function mousePressed() {
      if (isStopped === true) {
        // ビートが停止している場合、再生します。
        beat.play();
        isStopped = false;
      } else {
        // ビートが再生中の場合、停止します。
        beat.stop();
        isStopped = true;
      }
    }
    </code>
    </div>
class: p5.MediaElement
chainable: true
---


# stop

