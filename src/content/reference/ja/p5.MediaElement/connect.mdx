---
title: connect
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>要素のオーディオを出力に送信します。</p>

  <p>パラメータ <code>audioNode</code> は、<code>AudioNode</code> または

  <code>p5.sound</code> ライブラリのオブジェクトです。</p>

  <p>要素が提供されない場合（<code>myElement.connect()</code> のように）、要素は

  メイン出力に接続されます。すべての接続は

  <code>.disconnect()</code> メソッドで削除されます。</p>

  <p>注意：このメソッドは p5.sound.js アドオンライブラリと一緒に使用することを想定しています。</p>
line: 5077
isConstructor: false
itemtype: method
class: p5.MediaElement
params:
  - name: audioNode
    description: |
      <p>Web Audio API の AudioNode、
      または p5.sound ライブラリのオブジェクト</p>
    type: AudioNode|Object
chainable: false
---


# connect

