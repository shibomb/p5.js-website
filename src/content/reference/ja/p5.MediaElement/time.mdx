---
title: time
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>メディア要素の再生時間を設定します。</p>

  <p>パラメータ<code>time</code>はオプションです。これは再生開始時にジャンプする時間を秒単位で指定する数値です。</p>

  <p>引数なしで<code>media.time()</code>を呼び出すと、オーディオ/ビデオが再生された秒数を返します。</p>

  <p>注意：ループするメディアが再開する際、時間は0にリセットされます。</p>
line: 4748
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let dragon;

    function setup() {
      createCanvas(100, 100);

      // createAudio()を使用してp5.MediaElementを作成します。
      dragon = createAudio('/assets/lucky_dragons.mp3');

      // デフォルトのメディアコントロールを表示します。
      dragon.showControls();

      describe('グレーの四角形上に "S秒" というテキストがあり、その下にメディアコントロールがあります。曲が再生されるにつれて、数字 "S" が増加します。');
    }

    function draw() {
      background(200);

      // 現在の再生時間を取得します。
      let s = dragon.time();

      // 表示用にsを小数点以下1桁に丸めます。
      s = round(s, 1);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 再生時間を表示します。
      text(`${s} 秒`, 50, 50);
    }
    </code>
    </div>

    <div>
    <code>
    let dragon;

    function setup() {
      createCanvas(100, 100);

      // createAudio()を使用してp5.MediaElementを作成します。
      dragon = createAudio('/assets/lucky_dragons.mp3');

      // デフォルトのメディアコントロールを表示します。
      dragon.showControls();

      // 開始時に2秒にジャンプします。
      dragon.time(2);

      describe('グレーの四角形上に "S秒" というテキストがあり、その下にメディアコントロールがあります。曲が再生されるにつれて、数字 "S" が増加します。');
    }

    function draw() {
      background(200);

      // 現在の再生時間を取得します。
      let s = dragon.time();

      // 表示用にsを小数点以下1桁に丸めます。
      s = round(s, 1);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 再生時間を表示します。
      text(`${s} 秒`, 50, 50);
    }
    </code>
    </div>
class: p5.MediaElement
return:
  description: 現在の時間（秒単位）。
  type: Number
overloads:
  - line: 4748
    params: []
    return:
      description: 現在の時間（秒単位）。
      type: Number
  - line: 4836
    params:
      - name: time
        description: |
          <p>ジャンプする時間（秒単位）。</p>
        type: Number
    chainable: 1
chainable: false
---


# time

