---
title: autoplay
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>音声/動画をロードされたら再生するように設定します。</p>

  <p>パラメータ <code>shouldAutoplay</code> はオプションです。引数なしで

  <code>media.autoplay()</code> を呼び出すと、メディアは自動的に再生されます。

  <code>media.autoplay(true)</code> のように <code>true</code> を渡すと、

  メディアは自動的に再生されます。<code>media.autoPlay(false)</code> のように

  <code>false</code> を渡すと、自動的には再生されません。</p>
line: 4520
isConstructor: false
itemtype: method
example:
  - |-

    <div class='notest'>
    <code>
    let video;

    function setup() {
      noCanvas();

      // 動画がロードされたらhandleVideo()を呼び出します。
      video = createVideo('/assets/fingers.mov', handleVideo);

      describe('トレッドミル上を歩く指の動画。');
    }

    // 動画のサイズを設定し、再生します。
    function handleVideo() {
      video.size(100, 100);
      video.autoplay();
    }
    </code>
    </div>

    <div class='notest'>
    <code>
    function setup() {
      noCanvas();

      // 動画をロードしますが、自動的には再生しません。
      let video = createVideo('/assets/fingers.mov', handleVideo);

      // ユーザーがクリックしたときに動画を再生します。
      video.mousePressed(handlePress);

      describe('トレッドミル上の指の画像。ユーザーがダブルクリックすると歩き始めます。');
    }
    </code>
    </div>

    // 動画のサイズと再生モードを設定します。
    function handleVideo() {
      video.size(100, 100);
      video.autoplay(false);
    }

    // 動画を再生します。
    function handleClick() {
      video.play();
    }
class: p5.MediaElement
params:
  - name: shouldAutoplay
    description: |
      <p>要素を自動再生するかどうか。</p>
    type: Boolean
    optional: true
chainable: true
---


# autoplay

