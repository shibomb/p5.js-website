---
title: play
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>メディア要素からオーディオまたはビデオを再生します。</p>
line: 4197
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let beat;

    function setup() {
      createCanvas(100, 100);

      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // メッセージを表示します。
      text('クリックして再生', 50, 50);

      // createAudio()を使用してp5.MediaElementを作成します。
      beat = createAudio('/assets/beat.mp3');

      describe('灰色の背景に黒で書かれた「クリックして再生」というテキスト。ユーザーが四角をクリックするとビートが再生されます。');
    }

    // ユーザーがマウスを押したときにビートを再生します。
    function mousePressed() {
      beat.play();
    }
    </code>
    </div>
class: p5.MediaElement
chainable: true
---


# play

