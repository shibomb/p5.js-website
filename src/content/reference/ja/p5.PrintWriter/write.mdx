---
title: write
module: IO
submodule: Output
file: src/io/files.js
description: >
  <p>改行を追加せずにデータをプリントストリームに書き込みます。</p>

  <p>パラメータ<code>data</code>は書き込むデータです。<code>data</code>は
  <code>myWriter.write('hi')</code>のように数値または文字列、
  あるいは<code>myWriter.write([1, 2, 3])</code>のように数値と文字列の
  配列にすることができます。配列要素がプリントストリームに追加される際、
  要素間にカンマが挿入されます。</p>
line: 1622
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textFont('Courier New');
      textSize(12);

      // 指示を表示します。
      text('ダブルクリックで保存', 5, 50, 90);

      describe('灰色の背景に黒字で「ダブルクリックで保存」というテキストが書かれています。');
    }

    // ユーザーがダブルクリックしたときにファイルを保存します。
    function doubleClicked() {
      // p5.PrintWriterオブジェクトを作成します。
      let myWriter = createWriter('numbers.txt');

      // プリントストリームにデータを追加します。
      myWriter.write('1,2,3,');
      myWriter.write(['4', '5', '6']);

      // ファイルを保存し、プリントストリームを閉じます。
      myWriter.close();
    }
    </code>
    </div>
class: p5.PrintWriter
params:
  - name: data
    description: |
      <p>文字列、数値、または文字列と数値の配列として
                                        書き込まれるデータ。</p>
    type: String|Number|Array
chainable: false
---


# write

