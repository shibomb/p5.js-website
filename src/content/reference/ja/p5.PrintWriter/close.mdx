---
title: close
module: IO
submodule: Output
file: src/io/files.js
description: |
  <p>ファイルを保存し、プリントストリームを閉じます。</p>
line: 1767
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textFont('Courier New');
      textSize(12);

      // 指示を表示します。
      text('ダブルクリックで保存', 5, 50, 90);

      describe('灰色の背景に黒字で「ダブルクリックで保存」と書かれたテキスト。');
    }

    // ユーザーがダブルクリックしたときにファイルを保存します。
    function doubleClicked() {
      // p5.PrintWriterオブジェクトを作成します。
      let myWriter = createWriter('cat.txt');

      // プリントストリームにデータを追加します。
      // ASCIIアートはWikipediaより提供:
      // https://en.wikipedia.org/wiki/ASCII_art
      myWriter.print(' (\\_/) ');
      myWriter.print("(='.'=)");
      myWriter.print('(")_(")');

      // ファイルを保存し、プリントストリームを閉じます。
      myWriter.close();
    }
    </code>
    </div>
class: p5.PrintWriter
chainable: false
---


# close

