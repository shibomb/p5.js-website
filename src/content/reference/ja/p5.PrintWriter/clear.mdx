---
title: clear
module: IO
submodule: Output
file: src/io/files.js
description: |
  <p>プリントストリームからすべてのデータをクリアします。</p>
line: 1722
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textFont('Courier New');
      textSize(12);

      // 指示を表示します。
      text('ダブルクリックで保存', 5, 50, 90);

      describe('グレーの背景に黒で書かれた "ダブルクリックで保存" というテキスト。');
    }

    // ユーザーがダブルクリックしたときにファイルを保存します。
    function doubleClicked() {
      // p5.PrintWriterオブジェクトを作成します。
      let myWriter = createWriter('numbers.txt');

      // プリントストリームにデータを追加します。
      myWriter.print('Hello p5*js!');

      // プリントストリームをクリアします。
      myWriter.clear();

      // ファイルを保存し、プリントストリームを閉じます。
      myWriter.close();
    }
    </code>
    </div>
class: p5.PrintWriter
chainable: false
---


# clear

