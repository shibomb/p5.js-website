---
title: listAttributes
module: IO
submodule: Input
file: src/io/p5.XML.js
description: >
  <p>要素の属性名を含む<code>Array</code>を返します。</p>

  <p>注意: 属性の値を取得するには、

  <a href="/reference/p5.XML/getString/">myXML.getString()</a>または

  <a href="/reference/p5.XML/getNum/">myXML.getNum()</a>を使用してください。</p>
line: 767
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myXML;

    // XMLを読み込み、p5.XMLオブジェクトを作成します。
    function preload() {
      myXML = loadXML('/assets/animals.xml');
    }

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 最初の子要素を取得します。
      let first = myXML.getChild(0);

      // 属性の数を取得します。
      let attributes = first.listAttributes();

      // テキストのスタイルを設定します。
      textAlign(CENTER, CENTER);
      textFont('Courier New');
      textSize(14);

      // 要素の属性を表示します。
      text(attributes, 50, 50);

      describe('灰色の背景に黒で書かれた "id,species" というテキスト。');
    }
    </code>
    </div>
class: p5.XML
return:
  description: 属性名。
  type: 'String[]'
chainable: false
---


# listAttributes

