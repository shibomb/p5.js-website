---
title: getParent
module: IO
submodule: Input
file: src/io/p5.XML.js
description: >
  <p>要素の親要素を新しい <a href="/reference/p5/p5.XML">p5.XML</a> オブジェクトとして返します。</p>
line: 77
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myXML;

    // XMLを読み込み、p5.XMLオブジェクトを作成します。
    function preload() {
      myXML = loadXML('/assets/animals.xml');
    }

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 全てのmammal要素を含む配列を取得します。
      let mammals = myXML.getChildren('mammal');

      // 最初のmammal要素を取得します。
      let firstMammal = mammals[0];

      // 親要素を取得します。
      let parent = firstMammal.getParent();

      // 親要素の名前を取得します。
      let name = parent.getName();

      // テキストのスタイルを設定します。
      textAlign(CENTER, CENTER);
      textFont('Courier New');
      textSize(14);

      // 親要素の名前を表示します。
      text(name, 50, 50);

      describe('灰色の背景に黒で書かれた"animals"という単語。');
    }
    </code>
    </div>
class: p5.XML
return:
  description: 親要素。
  type: p5.XML
chainable: false
---


# getParent

