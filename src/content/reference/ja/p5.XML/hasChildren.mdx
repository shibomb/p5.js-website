---
title: hasChildren
module: IO
submodule: Input
file: src/io/p5.XML.js
description: >
  <p>要素に子要素がある場合は <code>true</code> を返し、
  ない場合は <code>false</code> を返します。</p>
line: 242
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myXML;

    // XMLを読み込み、p5.XMLオブジェクトを作成します。
    function preload() {
      myXML = loadXML('/assets/animals.xml');
    }

    function setup() {
      createCanvas(100, 100);

      background(200);

      // 要素に子要素があるかどうかを確認します。
      let isParent = myXML.hasChildren();

      // テキストのスタイルを設定します。
      textAlign(CENTER, CENTER);
      textFont('Courier New');
      textSize(14);

      // テキストのスタイルを設定します。
      if (isParent === true) {
        text('Parent', 50, 50);
      } else {
        text('Not Parent', 50, 50);
      }

      describe('灰色の背景に黒で "Parent" という単語が書かれています。');
    }
    </code>
    </div>
class: p5.XML
return:
  description: 要素に子要素があるかどうか。
  type: Boolean
chainable: false
---


# hasChildren

