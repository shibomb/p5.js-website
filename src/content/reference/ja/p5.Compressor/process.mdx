---
title: process
module: p5.sound
submodule: p5.sound
file: lib/addons/p5.sound.js
description: >
  <p>connect と同じ機能を実行しますが、コンプレッサーの audioParams
  を設定するためのオプションパラメーターを受け取ることもできます。</p>

  <p></p>


  <hr/>

  <p>Performs the same function as .connect, but also accepts

  optional parameters to set compressor's audioParams</p>
line: 10084
isConstructor: false
itemtype: method
class: p5.Compressor
params:
  - name: src
    description: |
      <p>接続するサウンドソース</p>
      <p>Sound source to be connected</p>
    type: Object
  - name: attack
    description: |
      <p>(オプション) ゲインを10 dB 減少させる時間（秒単位）、デフォルトは.003、範囲は0-1</p>
      <p>The amount of time (in seconds) to reduce the gain by 10dB,
                                 default = .003, range 0 - 1</p>
    type: Number
    optional: true
  - name: knee
    description: >
      <p>(オプション)
      しきい値よりも上の範囲を表すデシベル値で、カーブが「ratio」部分に滑らかに移行する範囲を表します。デフォルトは30、範囲は0-40</p>

      <p>A decibel value representing the range above the
                                 threshold where the curve smoothly transitions to the "ratio" portion.
                                 default = 30, range 0 - 40</p>
    type: Number
    optional: true
  - name: ratio
    description: |
      <p>(オプション) 出力の 1dB の変化に対して入力が変化する dB の量を表します デフォルトは12、範囲は1-20</p>
      <p>The amount of dB change in input for a 1 dB change in output
                                 default = 12, range 1 - 20</p>
    type: Number
    optional: true
  - name: threshold
    description: |
      <p>(オプション) コンプレッションが効果を発揮し始めるしきい値以上のデシベル値。デフォルトは-24、範囲は-100〜0</p>
      <p>The decibel value above which the compression will start taking effect
                                 default = -24, range -100 - 0</p>
    type: Number
    optional: true
  - name: release
    description: |
      <p>(オプション) ゲインを10 dB 増加させる時間（秒単位）。デフォルトは.25、範囲は0-1</p>
      <p>The amount of time (in seconds) to increase the gain by 10dB
                                 default = .25, range 0 - 1</p>
    type: Number
    optional: true
chainable: false
---


# process
