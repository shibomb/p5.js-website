---
title: color
module: Color
submodule: Creating & Reading
file: src/color/creating_reading.js
description: >
  <p><a href="/reference/p5/p5.Color">p5.Color</a>オブジェクトを作成します。</p>

  <p>デフォルトでは、パラメータはRGB値として解釈されます。

  <code>color(255, 204, 0)</code>を呼び出すと、明るい黄色が返されます。これらの

  パラメータの解釈方法は、<a href="/reference/p5/colorMode/">colorMode()</a>関数で

  変更することができます。</p>

  <p>1つのパラメータを持つ<code>color()</code>のバージョンは、値を2つの方法で解釈します。

  パラメータが数値の場合、グレースケール値として解釈されます。

  パラメータが文字列の場合、CSS色文字列として解釈されます。</p>

  <p>2つのパラメータを持つ<code>color()</code>のバージョンは、最初のパラメータを

  グレースケール値として解釈します。2番目のパラメータはアルファ（透明度）値を設定します。</p>

  <p>3つのパラメータを持つ<code>color()</code>のバージョンは、現在の<code>colorMode()</code>に応じて、

  RGB、HSB、またはHSLカラーとして解釈します。</p>

  <p>4つのパラメータを持つ<code>color()</code>のバージョンは、現在の<code>colorMode()</code>に応じて、

  RGBA、HSBA、またはHSLAカラーとして解釈します。最後のパラメータは

  アルファ（透明度）値を設定します。</p>
line: 398
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // RGB値を使用してp5.Colorオブジェクトを作成します。
      let c = color(255, 204, 0);

      // 正方形を描画します。
      fill(c);
      noStroke();
      square(30, 20, 55);

      describe('灰色のキャンバス上の黄色い正方形。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // RGB値を使用してp5.Colorオブジェクトを作成します。
      let c1 = color(255, 204, 0);

      // 左の円を描画します。
      fill(c1);
      noStroke();
      circle(25, 25, 80);

      // グレースケール値を使用してp5.Colorオブジェクトを作成します。
      let c2 = color(65);

      // 右の円を描画します。
      fill(c2);
      circle(75, 75, 80);

      describe(
        '灰色のキャンバス上の2つの円。左上の円は黄色で、右下の円は灰色です。'
      );
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 名前付きカラーを使用してp5.Colorオブジェクトを作成します。
      let c = color('magenta');

      // 正方形を描画します。
      fill(c);
      noStroke();
      square(20, 20, 60);

      describe('灰色のキャンバス上のマゼンタ色の正方形。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 16進数カラーコードを使用してp5.Colorオブジェクトを作成します。
      let c1 = color('#0f0');

      // 左の長方形を描画します。
      fill(c1);
      noStroke();
      rect(0, 10, 45, 80);

      // 16進数カラーコードを使用してp5.Colorオブジェクトを作成します。
      let c2 = color('#00ff00');

      // 右の長方形を描画します。
      fill(c2);
      rect(55, 10, 45, 80);

      describe('灰色のキャンバス上の2つの明るい緑色の長方形。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // RGB色文字列を使用してp5.Colorオブジェクトを作成します。
      let c1 = color('rgb(0, 0, 255)');

      // 左上の正方形を描画します。
      fill(c1);
      square(10, 10, 35);

      // RGB色文字列を使用してp5.Colorオブジェクトを作成します。
      let c2 = color('rgb(0%, 0%, 100%)');

      // 右上の正方形を描画します。
      fill(c2);
      square(55, 10, 35);

      // RGBA色文字列を使用してp5.Colorオブジェクトを作成します。
      let c3 = color('rgba(0, 0, 255, 1)');

      // 左下の正方形を描画します。
      fill(c3);
      square(10, 55, 35);

      // RGBA色文字列を使用してp5.Colorオブジェクトを作成します。
      let c4 = color('rgba(0%, 0%, 100%, 1)');

      // 右下の正方形を描画します。
      fill(c4);
      square(55, 55, 35);

      describe('灰色のキャンバスの四隅にある4つの青い正方形。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // HSL色文字列を使用してp5.Colorオブジェクトを作成します。
      let c1 = color('hsl(160, 100%, 50%)');

      // 左の長方形を描画します。
      noStroke();
      fill(c1);
      rect(0, 10, 45, 80);

      // HSLA色文字列を使用してp5.Colorオブジェクトを作成します。
      let c2 = color('hsla(160, 100%, 50%, 0.5)');

      // 右の長方形を描画します。
      fill(c2);
      rect(55, 10, 45, 80);

      describe('2つのシーグリーンの長方形。左側にはより暗い長方形、右側にはより明るい長方形があります。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // HSB色文字列を使用してp5.Colorオブジェクトを作成します。
      let c1 = color('hsb(160, 100%, 50%)');

      // 左の長方形を描画します。
      noStroke();
      fill(c1);
      rect(0, 10, 45, 80);

      // HSBA色文字列を使用してp5.Colorオブジェクトを作成します。
      let c2 = color('hsba(160, 100%, 50%, 0.5)');

      // 右の長方形を描画します。
      fill(c2);
      rect(55, 10, 45, 80);

      describe('2つの緑色の長方形。左側にはより暗い長方形、右側にはより明るい長方形があります。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // RGB値を使用してp5.Colorオブジェクトを作成します。
      let c1 = color(50, 55, 100);

      // 左の長方形を描画します。
      fill(c1);
      rect(0, 10, 45, 80);

      // カラーモードをHSBに切り替えます。
      colorMode(HSB, 100);

      // HSB値を使用してp5.Colorオブジェクトを作成します。
      let c2 = color(50, 55, 100);

      // 右の長方形を描画します。
      fill(c2);
      rect(55, 10, 45, 80);

      describe('2つの青い長方形。左側にはより暗い長方形、右側にはより明るい長方形があります。');
    }
    </code>
    </div>
class: p5
return:
  description: 結果の色。
  type: p5.Color
overloads:
  - line: 398
    params:
      - name: gray
        description: |
          <p>白と黒の間の値を指定する数値。</p>
        type: Number
      - name: alpha
        description: |
          <p>現在の色範囲に対する相対的なアルファ値
                                          （デフォルトは0-255）。</p>
        type: Number
        optional: true
    return:
      description: 結果の色。
      type: p5.Color
  - line: 652
    params:
      - name: v1
        description: |
          <p>現在の色範囲に対する相対的な
                                          赤または色相の値。</p>
        type: Number
      - name: v2
        description: |
          <p>現在の色範囲に対する相対的な
                                          緑または彩度の値。</p>
        type: Number
      - name: v3
        description: |
          <p>現在の色範囲に対する相対的な
                                          青または明度の値。</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
    return:
      description: ''
      type: p5.Color
  - line: 664
    params:
      - name: value
        description: |
          <p>色を表す文字列。</p>
        type: String
    return:
      description: ''
      type: p5.Color
  - line: 670
    params:
      - name: values
        description: |
          <p>色の赤、緑、青、
                                          アルファ成分を含む配列。</p>
        type: 'Number[]'
    return:
      description: ''
      type: p5.Color
  - line: 677
    params:
      - name: color
        description: ''
        type: p5.Color
    return:
      description: ''
      type: p5.Color
chainable: false
---


# color

