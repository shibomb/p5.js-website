---
title: describe
module: Environment
submodule: Environment
file: src/accessibility/describe.js
description: >
  <p>キャンバスのスクリーンリーダーでアクセス可能な説明を作成します。</p>

  <p>最初のパラメータ <code>text</code> は、キャンバスの説明です。</p>

  <p>2番目のパラメータ <code>display</code> はオプションです。説明の表示方法を決定します。
  <code>LABEL</code> が渡された場合、例えば
  <code>describe('説明文。', LABEL)</code> のように、説明はキャンバスの隣の
  div要素に表示されます。<code>FALLBACK</code> が渡された場合、例えば
  <code>describe('説明文。', FALLBACK)</code> のように、説明はスクリーンリーダーにのみ
  表示されます。これがデフォルトのモードです。</p>

  <p>スケッチをアクセシブルにする方法について詳しく学ぶには、
  <a href="/learn/accessible-labels.html">アクセシブルなキャンバスの説明を書く</a>
  をお読みください。</p>
line: 18
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      background('pink');

      // ハートを描く。
      fill('red');
      noStroke();
      circle(67, 67, 20);
      circle(83, 67, 20);
      triangle(91, 73, 75, 95, 59, 73);

      // キャンバスの一般的な説明を追加する。
      describe('右下隅に赤いハートがある、ピンク色の正方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      background('pink');

      // ハートを描く。
      fill('red');
      noStroke();
      circle(67, 67, 20);
      circle(83, 67, 20);
      triangle(91, 73, 75, 95, 59, 73);

      // キャンバスの一般的な説明を追加し、
      // デバッグ用に表示する。
      describe('右下隅に赤いハートがある、ピンク色の正方形。', LABEL);
    }
    </code>
    </div>

    <div>
    <code>
    function draw() {
      background(200);

      // 式
      // frameCount % 100
      // は、xを0から99まで増加させ、
      // その後0から再開させる。
      let x = frameCount % 100;

      // 円を描く。
      fill(0, 255, 0);
      circle(x, 50, 40);

      // キャンバスの一般的な説明を追加する。
      describe(`灰色の正方形上で、(${x}, 50)の位置にある緑の円が左から右に移動する。`);
    }
    </code>
    </div>

    <div>
    <code>
    function draw() {
      background(200);

      // 式
      // frameCount % 100
      // は、xを0から99まで増加させ、
      // その後0から再開させる。
      let x = frameCount % 100;

      // 円を描く。
      fill(0, 255, 0);
      circle(x, 50, 40);

      // キャンバスの一般的な説明を追加し、
      // デバッグ用に表示する。
      describe(`灰色の正方形上で、(${x}, 50)の位置にある緑の円が左から右に移動する。`, LABEL);
    }
    </code>
    </div>
class: p5
params:
  - name: text
    description: |
      <p>キャンバスの説明。</p>
    type: String
  - name: display
    description: |
      <p>LABELまたはFALLBACK。</p>
    type: Constant
    optional: true
chainable: false
---


# describe

