---
title: displayHeight
module: Environment
submodule: Environment
file: src/core/environment.js
description: >
  <p><code>Number</code>型の変数で、画面ディスプレイの高さを格納します。</p>

  <p><code>displayHeight</code>はフルスクリーンプログラムの実行に便利です。その値は

  現在の<a href="/reference/p5/pixelDensity/">pixelDensity()</a>に依存します。</p>

  <p>注意：実際の画面の高さは

  <code>displayHeight * pixelDensity()</code>として計算できます。</p>
line: 621
isConstructor: false
itemtype: property
alt: この例は何も描画しません。
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // キャンバスの幅と高さを
      // ディスプレイの寸法を使用して設定します。
      createCanvas(displayWidth, displayHeight);

      background(200);

      describe('ディスプレイと同じサイズのグレーのキャンバス。');
    }
    </code>
    </div>
class: p5
type: Number
---


# displayHeight

