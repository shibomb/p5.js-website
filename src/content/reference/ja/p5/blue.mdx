---
title: blue
module: Color
submodule: Creating & Reading
file: src/color/creating_reading.js
description: >
  <p>色の青の値を取得します。</p>

  <p><code>blue()</code>は<a href="/reference/p5/p5.Color">p5.Color</a>オブジェクト、色成分の配列、

  またはCSSの色文字列から青の値を抽出します。</p>

  <p>デフォルトでは、<code>blue()</code>は色の青の値を0から255の範囲で返します。

  <a href="/reference/colorMode/">colorMode()</a>がRGBに設定されている場合、

  指定された範囲内で青の値を返します。</p>
line: 117
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // RGB値を使用してp5.Colorオブジェクトを作成します。
      let c = color(175, 100, 220);

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'blueValue'を220に設定します。
      let blueValue = blue(c);

      // 右の長方形を描画します。
      fill(0, 0, blueValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側は薄紫色で、右側はロイヤルブルーです。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 色の配列を作成します。
      let c = [175, 100, 220];

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'blueValue'を220に設定します。
      let blueValue = blue(c);

      // 右の長方形を描画します。
      fill(0, 0, blueValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側は薄紫色で、右側はロイヤルブルーです。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // CSS色文字列を作成します。
      let c = 'rgb(175, 100, 220)';

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'blueValue'を220に設定します。
      let blueValue = blue(c);

      // 右の長方形を描画します。
      fill(0, 0, blueValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側は薄紫色で、右側はロイヤルブルーです。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 0-100の範囲の値でRGB色を使用します。
      colorMode(RGB, 100);

      // RGB値を使用してp5.Colorオブジェクトを作成します。
      let c = color(69, 39, 86);

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'blueValue'を86に設定します。
      let blueValue = blue(c);

      // 右の長方形を描画します。
      fill(0, 0, blueValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側は薄紫色で、右側はロイヤルブルーです。');
    }
    </code>
    </div>
class: p5
params:
  - name: color
    description: |
      <p><a href="#/p5.Color">p5.Color</a>オブジェクト、色成分の配列、
                                              またはCSS色文字列。</p>
    type: 'p5.Color|Number[]|String'
return:
  description: 青の値。
  type: Number
chainable: false
---


# blue

