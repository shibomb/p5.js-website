---
title: turnAxis
module: Events
submodule: Acceleration
file: src/events/acceleration.js
description: >
  <p>デバイスが回転したとき、<a href="/reference/p5/deviceTurned/">deviceTurned()</a>
  メソッドをトリガーする軸がturnAxis変数に格納されます。
  turnAxis変数は
  deviceTurned()のスコープ内でのみ定義されます。</p>
line: 378
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // このサンプルをモバイルデバイスで実行してください
    // X軸で90度デバイスを回転させると
    // 値が変化します。

    let value = 0;
    function draw() {
      fill(value);
      rect(25, 25, 50, 50);
      describe(`キャンバスの中央に50x50の黒い四角形。
        モバイルでデバイスを回転させると白くなる`);
      describe(`キャンバスの中央に50x50の黒い四角形。
        モバイルでx軸を回転させると白くなる`);
    }
    function deviceTurned() {
      if (turnAxis === 'X') {
        if (value === 0) {
          value = 255;
        } else if (value === 255) {
          value = 0;
        }
      }
    }
    </code>
    </div>
class: p5
type: String
---


# turnAxis

