---
title: clearStorage
module: Data
submodule: LocalStorage
file: src/data/local_storage.js
description: >
  <p>ウェブブラウザのローカルストレージにある全てのアイテムを削除します。</p>

  <p>ウェブブラウザは、組み込みの
  <a
  href="https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage/"
  target="_blank">localStorage オブジェクト</a>
  を使用して少量のデータを保存できます。
  <code>localStorage</code>に保存されたデータは、ページの更新やブラウザの再起動後でも
  いつでも取得できます。データはキーと値のペアとして保存されます。
  <code>clearStorage()</code>を呼び出すと、<code>localStorage</code>から
  全てのデータが削除されます。</p>

  <p>注意：パスワードや個人情報などの機密データは<code>localStorage</code>に
  保存すべきではありません。</p>
line: 313
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // ダブルクリックでlocalStorageをクリアします。

    function setup() {
      createCanvas(100, 100);

      // プレイヤーの名前を保存します。
      storeItem('name', 'Feist');

      // プレイヤーのスコアを保存します。
      storeItem('score', 1234);

      describe(
        'グレーの背景に黒で "Feist: 