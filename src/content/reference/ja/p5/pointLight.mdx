---
title: pointLight
module: 3D
submodule: Lights
file: src/webgl/light.js
description: >
  <p>全方向に光を放つ点光源を作成します。</p>

  <p>点光源は、あらゆる方向に光を放つ電球のようなものです。異なる位置に配置することで、

  さまざまな照明効果を得ることができます。一度に最大5つの点光源を

  アクティブにすることができます。</p>

  <p><code>pointLight()</code>を呼び出す方法には、光の色と位置を設定するパラメータを

  使用する4つの方法があります。</p>

  <p><code>pointLight()</code>を呼び出す最初の方法は、6つのパラメータを使用します。最初の3つの

  パラメータ、<code>v1</code>、<code>v2</code>、<code>v3</code>は、現在の

  <a href="/reference/p5/colorMode/">colorMode()</a>を使用して光の色を設定します。残りの3つのパラメータ、

  <code>x</code>、<code>y</code>、<code>z</code>は、光の位置を設定します。例えば、

  <code>pointLight(255, 0, 0, 50, 0, 0)</code>は、座標<code>(50, 0, 0)</code>から

  赤色<code>(255, 0, 0)</code>の光を放つ点光源を作成します。</p>

  <p><code>pointLight()</code>を呼び出す2番目の方法は、4つのパラメータを使用します。最初の3つの

  パラメータ、<code>v1</code>、<code>v2</code>、<code>v3</code>は、現在の

  <a href="/reference/p5/colorMode/">colorMode()</a>を使用して光の色を設定します。最後のパラメータ、position は

  <a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを使用して光の位置を設定します。

  例えば、<code>pointLight(255, 0, 0, lightPos)</code>は、<code>lightPos</code>ベクトルで

  設定された位置から赤色<code>(255, 0, 0)</code>の光を放つ点光源を作成します。</p>

  <p><code>pointLight()</code>を呼び出す3番目の方法は、4つのパラメータを使用します。最初の

  パラメータ、<code>color</code>は、<a href="/reference/p5/p5.Color">p5.Color</a>オブジェクトまたは

  色値の配列を使用して光の色を設定します。残りの3つのパラメータ、<code>x</code>、<code>y</code>、<code>z</code>は、

  光の位置を設定します。例えば、<code>directionalLight(myColor, 50, 0, 0)</code>は、

  座標<code>(50, 0, 0)</code>から<code>myColor</code>の色値で光を放つ点光源を作成します。</p>

  <p><code>pointLight()</code>を呼び出す4番目の方法は、2つのパラメータを使用します。最初の

  パラメータ、<code>color</code>は、<a href="/reference/p5/p5.Color">p5.Color</a>オブジェクトまたは

  色値の配列を使用して光の色を設定します。2番目のパラメータ、<code>position</code>は、

  <a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを使用して光の位置を設定します。

  例えば、<code>directionalLight(myColor, lightPos)</code>は、<code>lightPos</code>ベクトルで

  設定された位置から<code>myColor</code>の色値で光を放つ点光源を作成します。</p>
line: 682
isConstructor: false
itemtype: method
example:
  - |-


    <div>
    <code>
    // マウスをクリックしてドラッグすると、異なる角度からシーンを見ることができます。
    // ダブルクリックすると点光源がオンになります。

    let isLit = false;

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に描かれた球体。ユーザーがダブルクリックすると、上から赤い光が当たり始めます。');
    }

    function draw() {
      background(200);

      // マウスで軌道制御を有効にします。
      orbitControl();

      // 光を制御します。
      if (isLit === true) {
        // 上から赤い点光源を追加します。
        // RGB値とXYZ座標を使用します。
        pointLight(255, 0, 0, 0, -150, 0);
      }

      // 球体のスタイルを設定します。
      noStroke();

      // 球体を描画します。
      sphere(30);
    }

    // ユーザーがダブルクリックすると点光源をオンにします。
    function doubleClicked() {
      isLit = true;
    }
    </code>
    </div>

    <div>
    <code>
    // マウスをクリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に描かれた球体。球体の上部は明るい赤色に見えます。色は底部に向かって暗くなります。');
    }

    function draw() {
      background(200);

      // マウスで軌道制御を有効にします。
      orbitControl();

      // 上から赤い点光源を追加します。
      // p5.ColorオブジェクトとXYZ方向を使用します。
      let c = color(255, 0, 0);
      pointLight(c, 0, -150, 0);

      // 球体のスタイルを設定します。
      noStroke();

      // 球体を描画します。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // マウスをクリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に描かれた球体。球体の上部は明るい赤色に見えます。色は底部に向かって暗くなります。');
    }

    function draw() {
      background(200);

      // マウスで軌道制御を有効にします。
      orbitControl();

      // 上から赤い点光源を追加します。
      // p5.Colorオブジェクトとp5.Vectorオブジェクトを使用します。
      let c = color(255, 0, 0);
      let lightPos = createVector(0, -150, 0);
      pointLight(c, lightPos);

      // 球体のスタイルを設定します。
      noStroke();

      // 球体を描画します。
      sphere(30);
    }
    </code>
    </div>

    <div>
    <code>
    // マウスをクリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に正方形に配置された4つの球体。球体は正方形の中心に向かって明るい赤色に見えます。色は正方形の角に向かって暗くなります。');
    }

    function draw() {
      background(200);

      // マウスで軌道制御を有効にします。
      orbitControl();

      // シーンの中心を指す赤い点光源を追加します。
      // p5.Colorオブジェクトとp5.Vectorオブジェクトを使用します。
      let c = color(255, 0, 0);
      let lightPos = createVector(0, 0, 65);
      pointLight(c, lightPos);

      // 球体のスタイルを設定します。
      noStroke();

      // 左上に球体を描画します。
      push();
      translate(-25, -25, 25);
      sphere(10);
      pop();

      // 右上に球体を描画します。
      push();
      translate(25, -25, 25);
      sphere(10);
      pop();

      // 左下に球体を描画します。
      push();
      translate(-25, 25, 25);
      sphere(10);
      pop();

      // 右下に球体を描画します。
      push();
      translate(25, 25, 25);
      sphere(10);
      pop();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 682
    params:
      - name: v1
        description: |
          <p>現在の<a href="#/p5/colorMode">colorMode()</a>での
                                 赤または色相の値。</p>
        type: Number
      - name: v2
        description: |
          <p>現在の<a href="#/p5/colorMode">colorMode()</a>での
                                 緑または彩度の値。</p>
        type: Number
      - name: v3
        description: |
          <p>現在の<a href="#/p5/colorMode">colorMode()</a>での
                                 青、明度、または輝度の値。</p>
        type: Number
      - name: x
        description: |
          <p>光のx座標。</p>
        type: Number
      - name: 'y'
        description: |
          <p>光のy座標。</p>
        type: Number
      - name: z
        description: |
          <p>光のz座標。</p>
        type: Number
    chainable: 1
  - line: 889
    params:
      - name: v1
        description: ''
        type: Number
      - name: v2
        description: ''
        type: Number
      - name: v3
        description: ''
        type: Number
      - name: position
        description: |
          <p><a href="#/p5.Vector">p5.Vector</a>オブジェクトとしての
                                        光の位置。</p>
        type: p5.Vector
    chainable: 1
  - line: 899
    params:
      - name: color
        description: |
          <p><a href="#/p5.Color">p5.Color</a>オブジェクト、
                                                   色値の配列、またはCSS文字列としての色。</p>
        type: 'p5.Color|Number[]|String'
      - name: x
        description: ''
        type: Number
      - name: 'y'
        description: ''
        type: Number
      - name: z
        description: ''
        type: Number
    chainable: 1
  - line: 909
    params:
      - name: color
        description: ''
        type: 'p5.Color|Number[]|String'
      - name: position
        description: ''
        type: p5.Vector
    chainable: 1
chainable: true
---


# pointLight

