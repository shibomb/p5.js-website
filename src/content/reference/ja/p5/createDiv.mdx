---
title: createDiv
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p><code><div></div></code>要素を作成します。</p>

  <p><code><div></div></code>要素は、他の要素のコンテナとしてよく使用されます。</p>

  <p>パラメータ<code>html</code>はオプションです。新しい<code><div></div></code>の
  内部HTMLを設定する文字列を受け入れます。</p>
line: 491
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div要素を作成し、位置を設定します。
      let div = createDiv('p5*js');
      div.position(25, 35);

      describe('中央に"p5*js"と書かれたグレーの正方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // div内にh3要素を作成します。
      let div = createDiv('<h3>p5*js</h3>');
      div.position(20, 5);

      describe('中央に"p5*js"と書かれたグレーの正方形。');
    }
    </code>
    </div>
class: p5
params:
  - name: html
    description: >
      <p>新しい<code>&lt;div&gt;&lt;/div&gt;</code>要素の内部HTML。</p>
    type: String
    optional: true
return:
  description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
  type: p5.Element
chainable: false
---


# createDiv

