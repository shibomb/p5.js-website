---
title: shorten
module: Data
submodule: Array Functions
file: src/utilities/array_functions.js
description: |
  <p>配列をひとつの要素で減らし、短くなった配列を返します。</p>
  <p>Array.pop() に対応します。</p>

  <hr/>
  <p>Decreases an array by one element and returns the shortened array,
  maps to Array.pop().</p>
line: 161
isConstructor: false
itemtype: method
example:
  - |-

    <div class = 'norender'><code>
    function setup() {
      let myArray = ['A', 'B', 'C'];
      print(myArray); // ['A', 'B', 'C']
      let newArray = shorten(myArray);
      print(myArray); // ['A','B','C']
      print(newArray); // ['A','B']
    }
    </code></div>
class: p5
params:
  - name: list
    description: |
      <p>短くする配列</p>
      <p>Array to shorten</p>
    type: Array
return:
  description: |-
    短くなった配列
    shortened Array
  type: Array
chainable: false
---


# shorten
