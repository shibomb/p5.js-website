---
title: ceil
module: Math
submodule: Calculation
file: src/math/calculation.js
description: >
  <p>数値以上の最も近い整数値を計算します。</p>

  <p>例えば、<code>ceil(9.03)</code>と<code>ceil(9.97)</code>を呼び出すと、
  どちらも値10を返します。</p>
line: 48
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 0から1の値でRGBカラーを使用します。
      colorMode(RGB, 1);

      noStroke();

      // 左の長方形を描画します。
      let r = 0.3;
      fill(r, 0, 0);
      rect(0, 0, 50, 100);

      // rを1に切り上げます。
      r = ceil(r);

      // 右の長方形を描画します。
      fill(r, 0, 0);
      rect(50, 0, 50, 100);

      describe('2つの長方形。左側は暗い赤色で、右側は明るい赤色です。');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>切り上げる数値。</p>
    type: Number
return:
  description: 切り上げられた数値。
  type: Integer
chainable: false
---


# ceil

