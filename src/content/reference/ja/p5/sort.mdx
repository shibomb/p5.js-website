---
title: sort
module: Data
submodule: Array Functions
file: src/utilities/array_functions.js
description: |
  <p>配列内の数値を最小から最大へ、または単語の配列をアルファベット順に並べ替えます。
  元の配列は変更されず、並べ替えられた新しい配列が返されます。countパラメータは並べ替える要素の数を指定します。例えば、配列に12個の要素があり、countが5に設定されている場合、配列の最初の5つの要素のみが並べ替えられます。</p>
line: 300
isConstructor: false
itemtype: method
example:
  - |-

    <div class = 'norender'><code>
    function setup() {
      let words = ['banana', 'apple', 'pear', 'lime'];
      print(words); // ['banana', 'apple', 'pear', 'lime']
      let count = 4; // 配列の長さ

      words = sort(words, count);
      print(words); // ['apple', 'banana', 'lime', 'pear']
    }
    </code></div>
    <div class = 'norender'><code>
    function setup() {
      let numbers = [2, 6, 1, 5, 14, 9, 8, 12];
      print(numbers); // [2, 6, 1, 5, 14, 9, 8, 12]
      let count = 5; // 配列の長さより少ない

      numbers = sort(numbers, count);
      print(numbers); // [1,2,5,6,14,9,8,12]
    }
    </code></div>
class: p5
params:
  - name: list
    description: |
      <p>並べ替える配列</p>
    type: Array
  - name: count
    description: |
      <p>並べ替える要素の数（0から開始）</p>
    type: Integer
    optional: true
return:
  description: 並べ替えられたリスト
  type: Array
chainable: false
---

# sort