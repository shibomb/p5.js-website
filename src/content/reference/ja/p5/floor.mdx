---
title: floor
module: Math
submodule: Calculation
file: src/math/calculation.js
description: |
  <p>数値以下の最も近い整数値を計算します。</p>
line: 294
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // 0から1の値でRGBカラーを使用します。
      colorMode(RGB, 1);

      noStroke();

      // 左の長方形を描画します。
      let r = 0.8;
      fill(r, 0, 0);
      rect(0, 0, 50, 100);

      // rを0に切り下げます。
      r = floor(r);

      // 右の長方形を描画します。
      fill(r, 0, 0);
      rect(50, 0, 50, 100);

      describe('2つの長方形。左側は明るい赤色で、右側は黒色です。');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>切り下げる数値。</p>
    type: Number
return:
  description: 切り下げられた数値。
  type: Integer
chainable: false
---

# floor
