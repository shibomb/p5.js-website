---
title: displayWidth
module: Environment
submodule: Environment
file: src/core/environment.js
description: >
  <p><code>Number</code>型の変数で、スクリーンディスプレイの幅を格納します。</p>

  <p><code>displayWidth</code>はフルスクリーンプログラムの実行に便利です。その値は

  現在の<a href="/reference/p5/pixelDensity/">pixelDensity()</a>に依存します。</p>

  <p>注意：実際の画面幅は

  <code>displayWidth * pixelDensity()</code>として計算できます。</p>
line: 590
isConstructor: false
itemtype: property
alt: この例は何もレンダリングしません。
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // キャンバスの幅と高さを
      // ディスプレイの寸法を使用して設定します。
      createCanvas(displayWidth, displayHeight);

      background(200);

      describe('ディスプレイと同じサイズのグレーのキャンバス。');
    }
    </code>
    </div>
class: p5
type: Number
---


# displayWidth

