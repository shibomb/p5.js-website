---
title: endClip
module: Color
submodule: Setting
file: src/color/setting.js
description: >
  <p>クリッピング領域を終了します。</p>

  <p><code>endClip()</code>は<code>beginClip()</code>と一緒に使用され、

  クリッピング領域の終わりを示します。</p>

  <p><code>endClip()</code>は、対応する<code>beginClip()</code>の後に呼び出す必要があります。</p>
line: '2237'
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
    }

    function draw() {
      background(220);

      push();
      beginClip();
      circle(50, 50, 40);
      endClip();

      // この長方形は円の内側にのみ表示されます
      fill('red');
      rect(0, 0, 100, 100);
      pop();

      describe('赤い円。円は長方形でクリップされています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
    }

    function draw() {
      background(220);

      push();
      beginClip();
      square(25, 25, 50);
      endClip();

      // この円は正方形の内側にのみ表示されます
      fill('blue');
      circle(50, 50, 80);
      pop();

      describe('青い正方形。正方形は円でクリップされています。');
    }
    </code>
    </div>
class: p5
overloads:
  - line: '2237'
    params: []
    chainable: 1
chainable: true
---

# endClip