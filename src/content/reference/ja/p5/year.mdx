---
title: year
module: IO
submodule: Time & Date
file: src/utilities/time_date.js
description: |
  <p>現在の年を1999のような数字で返します。</p>
line: 311
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 現在の年を取得します。
      let y = year();

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textSize(12);
      textFont('Courier New');

      // 年を表示します。
      text(`Current year: ${y}`, 10, 50, 80);

      describe(`灰色の背景に黒字で'Current year: ${y}'というテキストが書かれています。`);
    }
    </code>
    </div>
class: p5
return:
  description: 現在の年。
  type: Integer
chainable: false
---


# year

