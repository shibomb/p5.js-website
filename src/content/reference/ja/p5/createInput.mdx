---
title: createInput
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>テキスト<code><input></input></code>要素を作成します。</p>

  <p><code>myInput.size()</code>を呼び出して、テキストボックスの長さを設定します。</p>

  <p>最初のパラメータ<code>value</code>はオプションです。これは入力のデフォルト値を設定する文字列です。
  デフォルトでは入力は空白です。</p>

  <p>2番目のパラメータ<code>type</code>もオプションです。これは入力されるテキストの
  タイプを指定する文字列です。MDNの
  <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Input/"
  target="_blank">オプション一覧</a>を参照してください。
  デフォルトは<code>'text'</code>です。</p>
line: 1841
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myInput;

    function setup() {
      createCanvas(100, 100);

      // 入力要素を作成し、
      // キャンバスの下に配置します。
      myInput = createInput();
      myInput.position(0, 100);

      describe('グレーの正方形と、その下にテキストボックスがあります。ユーザーが入力バーに新しい内容を入力すると、正方形内のテキストが変更されます。');
    }

    function draw() {
      background(200);

      // 入力を使用してメッセージを表示します。
      let msg = myInput.value();
      text(msg, 25, 55);
    }
    </code>
    </div>

    <div>
    <code>
    let myInput;

    function setup() {
      createCanvas(100, 100);

      // 入力要素を作成し、キャンバスの下に配置します。
      // デフォルトのテキストを "hello!" に設定します。
      myInput = createInput('hello!');
      myInput.position(0, 100);

      describe('グレーの正方形の中央に "hello!" というテキストが書かれています。正方形の下にあるテキストボックスにも "hello!" と表示されています。ユーザーが入力バーに新しい内容を入力すると、正方形内のテキストが変更されます。');
    }

    function draw() {
      background(200);

      // 入力を使用してメッセージを表示します。
      let msg = myInput.value();
      text(msg, 25, 55);
    }
    </code>
    </div>
class: p5
return:
  description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
  type: p5.Element
overloads:
  - line: 1841
    params:
      - name: value
        description: >
          <p>入力ボックスのデフォルト値。デフォルトは空の文字列<code>''</code>です。</p>
        type: String
        optional: true
      - name: type
        description: |
          <p>入力のタイプ。デフォルトは<code>'text'</code>です。</p>
        type: String
        optional: true
    return:
      description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
      type: p5.Element
  - line: 1911
    params:
      - name: value
        description: ''
        type: String
        optional: true
    return:
      description: ''
      type: p5.Element
chainable: false
---


# createInput

