---
title: triangle
module: Shape
submodule: 2D Primitives
file: src/core/shape/2d_primitives.js
description: >
  <p>三角形を描きます。</p>

  <p>三角形は3つの点で定義される3辺の形です。最初の2つのパラメータは三角形の最初の点<code>(x1, y1)</code>を指定します。
  中間の2つのパラメータは2番目の点<code>(x2, y2)</code>を指定します。そして最後の2つのパラメータは3番目の点<code>(x3, y3)</code>を指定します。</p>
line: 1370
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      triangle(30, 75, 58, 20, 86, 75);

      describe('灰色のキャンバスに黒い輪郭の白い三角形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      triangle(-20, 25, 8, -30, 36, 25);

      describe('灰色のキャンバスに黒い輪郭の白い三角形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色のキャンバスに白い三角形が回転する。');
    }

    function draw() {
      background(200);

      // y軸を中心に回転します。
      rotateY(frameCount * 0.01);

      // 三角形を描きます。
      triangle(-20, 25, 8, -30, 36, 25);
    }
    </code>
    </div>
class: p5
params:
  - name: x1
    description: |
      <p>最初の点のx座標。</p>
    type: Number
  - name: y1
    description: |
      <p>最初の点のy座標。</p>
    type: Number
  - name: x2
    description: |
      <p>2番目の点のx座標。</p>
    type: Number
  - name: y2
    description: |
      <p>2番目の点のy座標。</p>
    type: Number
  - name: x3
    description: |
      <p>3番目の点のx座標。</p>
    type: Number
  - name: y3
    description: |
      <p>3番目の点のy座標。</p>
    type: Number
chainable: true
---


# triangle
