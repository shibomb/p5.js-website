---
title: drawingContext
module: Rendering
submodule: Rendering
file: src/core/rendering.js
description: >
  <p>スケッチの<code><canvas></code>要素に直接アクセスできるシステム変数です。</p>

  <p><code><canvas></code>要素は、p5.jsライブラリに含まれていない多くの特殊機能を提供します。
  <code>drawingContext</code>システム変数は、スケッチの
  <a
  href="https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/">CanvasRenderingContext2D</a>
  オブジェクトを公開することで、これらの機能へのアクセスを提供します。</p>
line: 1196
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 影を使って円のスタイルを設定します。
      drawingContext.shadowOffsetX = 5;
      drawingContext.shadowOffsetY = -5;
      drawingContext.shadowBlur = 10;
      drawingContext.shadowColor = 'black';

      // 円を描画します。
      circle(50, 50, 40);

      describe("灰色の背景に白い円。円の縁は影がついています。");
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background('skyblue');

      // カラーグラデーションを使って円のスタイルを設定します。
      let myGradient = drawingContext.createRadialGradient(50, 50, 3, 50, 50, 40);
      myGradient.addColorStop(0, 'yellow');
      myGradient.addColorStop(0.6, 'orangered');
      myGradient.addColorStop(1, 'yellow');
      drawingContext.fillStyle = myGradient;
      drawingContext.strokeStyle = 'rgba(0, 0, 0, 0)';

      // 円を描画します。
      circle(50, 50, 40);

      describe('薄い青色の背景に描かれた炎のような太陽。');
    }
    </code>
    </div>
class: p5
---


# drawingContext

