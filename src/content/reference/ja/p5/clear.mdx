---
title: clear
module: Color
submodule: Setting
file: src/color/setting.js
description: >
  <p>キャンバスをクリアします。
  この関数は、キャンバス上のピクセルデータを

  クリアします。</p>

  <p>デフォルトでは、<code>clear()</code>はキャンバスを完全に透明にします。</p>

  <p>オプションのパラメータを使用して、キャンバスを特定の色や透明度にクリアすることができます。

  パラメータは<a href="/reference/p5/background">background()</a>と同じ方法で解釈されます。</p>
line: '1037'
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
    }

    function draw() {
      background(220);
      ellipse(50, 50, 20);

      // 1秒ごとにキャンバスをクリア
      if (frameCount % 60 === 0) {
        clear();
      }

      describe('灰色の背景に黒い円が描かれ、1秒ごとに消えます。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
    }

    function draw() {
      background(220);
      ellipse(50, 50, 20);

      // 1秒ごとにキャンバスを半透明の赤でクリア
      if (frameCount % 60 === 0) {
        clear(255, 0, 0, 100);
      }

      describe('灰色の背景に黒い円が描かれ、1秒ごとに半透明の赤で覆われます。');
    }
    </code>
    </div>
class: p5
overloads:
  - line: '1037'
    params:
      - name: r
        description: |
          <p>赤の値</p>
        type: Number
        optional: true
      - name: g
        description: |
          <p>緑の値</p>
        type: Number
        optional: true
      - name: b
        description: |
          <p>青の値</p>
        type: Number
        optional: true
      - name: a
        description: |
          <p>アルファ値</p>
        type: Number
        optional: true
    chainable: 1
chainable: true
---

# clear