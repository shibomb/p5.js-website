---
title: DEGREES
module: Constants
submodule: Constants
file: src/core/constants.js
description: >
  <p><a href="/reference/p5/angleMode/">angleMode()</a>を設定するために使用される<code>String</code>定数です。</p>

  <p>デフォルトでは、<a href="/reference/p5/rotate/">rotate()</a>や

  <a href="/reference/p5/sin/">sin()</a>などの関数は、ラジアン単位で測定された角度を想定しています。

  <code>angleMode(DEGREES)</code>を呼び出すことで、角度が度単位で

  測定されることを保証します。</p>

  <p>注意：<code>TWO_PI</code>ラジアンは360˚に等しいです。</p>
line: 547
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 0からHALF_PIラジアンまでの赤い弧を描画します。
      fill(255, 0, 0);
      arc(50, 50, 80, 80, 0, HALF_PI);

      // 度を使用します。
      angleMode(DEGREES);

      // 90˚から180˚までの青い弧を描画します。
      fill(0, 0, 255);
      arc(50, 50, 80, 80, 90, 180);

      describe('灰色の背景に描かれた円の下半分。右下の四分の一は赤色。左下の四分の一は青色。');
    }
    </code>
    </div>
class: p5
type: String
---


# DEGREES

