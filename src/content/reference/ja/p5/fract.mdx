---
title: fract
module: Math
submodule: Calculation
file: src/math/calculation.js
description: |
  <p>数値の小数部分を計算します。</p>
  <p>数値の小数部分には、その小数値が含まれます。例えば、
  <code>fract(12.34)</code> は 0.34 を返します。</p>
line: 1048
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 元の数値。
      let n = 56.78;
      text(n, 50, 33);

      // 小数部分。
      let f = fract(n);
      text(f, 50, 67);

      describe('数値56.78が上に、数値0.78が下に書かれています。');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>小数部分を求める数値。</p>
    type: Number
return:
  description: nの小数部分。
  type: Number
chainable: false
---


# fract

