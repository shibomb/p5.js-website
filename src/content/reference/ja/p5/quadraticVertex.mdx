---
title: quadraticVertex
module: Shape
submodule: Vertex
file: src/core/shape/vertex.js
description: >
  <p>カスタム形状に2次ベジエ曲線セグメントを追加します。</p>

  <p><code>quadraticVertex()</code>はカスタム形状に曲線セグメントを追加します。作成される

  ベジエ曲線セグメントは、<a href="/reference/p5/bezierVertex/">bezierVertex()</a>関数で

  作成されるものに似ています。<code>quadraticVertex()</code>は

  <a href="/reference/p5/beginShape/">beginShape()</a>と

  <a href="/reference/p5/endShape/">endShape()</a>関数の間で呼び出す必要があります。曲線セグメントは

  前の頂点を最初のアンカーポイントとして使用するため、<code>quadraticVertex()</code>を

  使用する前に少なくとも1回の<a href="/reference/p5/vertex/">vertex()</a>の呼び出しが

  必要です。</p>

  <p>最初の2つのパラメータ、<code>cx</code>と<code>cy</code>は、曲線の制御点を設定します。

  制御点は曲線をその方向に「引っ張る」役割をします。</p>

  <p>最後の2つのパラメータ、<code>x3</code>と<code>y3</code>は、最後のアンカーポイントを設定します。

  最後のアンカーポイントは曲線が終わる場所です。</p>

  <p>ベジエ曲線はWebGLモードを使用して3Dでも描画できます。3Dバージョンの

  <code>bezierVertex()</code>は8つの引数を持ちます。各点がx、y、z座標を

  持つためです。</p>

  <p>注意：<code>quadraticVertex()</code>は

  <a href="/reference/p5/beginShape/">beginShape()</a>に引数が渡された場合は

  機能しません。</p>
line: 1569
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 曲線のスタイルを設定します。
      noFill();

      // 形状の描画を開始します。
      beginShape();

      // 最初のアンカーポイントを追加します。
      vertex(20, 20);

      // 2次ベジエ頂点を追加します。
      quadraticVertex(80, 20, 50, 50);

      // 形状の描画を停止します。
      endShape();

      // アンカーポイントと制御点のスタイルを設定します。
      strokeWeight(5);

      // アンカーポイントを黒で描画します。
      stroke(0);
      point(20, 20);
      point(50, 50);

      // 制御点を赤で描画します。
      stroke(255, 0, 0);
      point(80, 20);

      describe('灰色の背景に黒い曲線。曲線の端点は黒い点で、制御点は赤い点でマークされています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 曲線のスタイルを設定します。
      noFill();

      // 形状の描画を開始します。
      beginShape();

      // 最初のアンカーポイントを追加します。
      vertex(20, 20);

      // 2次ベジエ頂点を追加します。
      quadraticVertex(80, 20, 50, 50);

      // 2つ目の2次ベジエ頂点を追加します。
      quadraticVertex(20, 80, 80, 80);

      // 形状の描画を停止します。
      endShape();

      // アンカーポイントと制御点のスタイルを設定します。
      strokeWeight(5);

      // アンカーポイントを黒で描画します。
      stroke(0);
      point(20, 20);
      point(50, 50);
      point(80, 80);

      // 制御点を赤で描画します。
      stroke(255, 0, 0);
      point(80, 20);
      point(20, 80);

      describe('灰色の背景に黒いS字曲線。曲線の端点は黒い点で、制御点は赤い点でマークされています。');
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に白いパズルピースの形が2つ描かれています。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // 最初のパズルピースのスタイルを設定します。
      noStroke();

      // 最初のパズルピースを描画します。
      beginShape();
      vertex(-30, -30, 20);
      quadraticVertex(30, -30, 20, 0, 0, 20);
      quadraticVertex(-30, 30, 20, 30, 30, 20);
      vertex(30, -40, 20);
      vertex(-30, -40, 20);
      vertex(-30, -30, 20);
      endShape();

      // 2つ目のパズルピースのスタイル
      endShape();

      // 2つ目のパズルピースのスタイル
      fill(255, 0, 0, 100);

      // 2つ目のパズルピースを描画します。
      beginShape();
      vertex(-30, -30, -20);
      quadraticVertex(30, -30, -20, 0, 0, -20);
      quadraticVertex(-30, 30, -20, 30, 30, -20);
      vertex(30, -40, -20);
      vertex(-30, -40, -20);
      vertex(-30, -30, -20);
      endShape();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1569
    params:
      - name: cx
        description: |
          <p>制御点のx座標。</p>
        type: Number
      - name: cy
        description: |
          <p>制御点のy座標。</p>
        type: Number
      - name: x3
        description: |
          <p>アンカーポイントのx座標。</p>
        type: Number
      - name: y3
        description: |
          <p>アンカーポイントのy座標。</p>
        type: Number
    chainable: 1
  - line: 1801
    params:
      - name: cx
        description: ''
        type: Number
      - name: cy
        description: ''
        type: Number
      - name: cz
        description: |
          <p>制御点のz座標。</p>
        type: Number
      - name: x3
        description: ''
        type: Number
      - name: y3
        description: ''
        type: Number
      - name: z3
        description: |
          <p>アンカーポイントのz座標。</p>
        type: Number
chainable: true
---


# quadraticVertex
