---
title: mouseIsPressed
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p><code>Boolean</code>型のシステム変数で、マウスが押されている場合は<code>true</code>、

  押されていない場合は<code>false</code>となります。</p>
line: 761
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に"false"という単語が書かれた灰色の正方形。ユーザーがマウスボタンを押すと、単語が"true"に変わります。'
      );
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // mouseIsPressed変数を表示します。
      text(mouseIsPressed, 25, 50);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に白い正方形がある灰色の正方形。ユーザーがマウスを押すと、内側の正方形が黒くなります。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定します。
      if (mouseIsPressed === true) {
        fill(0);
      } else {
        fill(255);
      }

      // 正方形を描画します。
      square(25, 25, 50);
    }
    </code>
    </div>
class: p5
type: Boolean
---


# mouseIsPressed

