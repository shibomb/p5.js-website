---
title: min
module: Math
submodule: Calculation
file: src/math/calculation.js
description: >
  <p>一連の数値の中で最小の値を返します。</p>

  <p>1つのパラメータを持つ<code>min()</code>のバージョンは、それを数値の配列として解釈し、

  最小の数値を返します。</p>

  <p>2つ以上のパラメータを持つ<code>min()</code>のバージョンは、それらを

  個別の数値として解釈し、最小の数値を返します。</p>
line: 695
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 10、5、20の最小値を計算します。
      let m = min(10, 5, 20);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 最小値を表示します。
      text(m, 50, 50);

      describe('灰色の正方形の中央に数字5が書かれています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 数値の配列を作成します。
      let numbers = [10, 5, 20];

      // 配列の最小値を計算します。
      let m = min(numbers);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // 最小値を表示します。
      text(m, 50, 50);

      describe('灰色の正方形の中央に数字5が書かれています。');
    }
    </code>
    </div>
class: p5
return:
  description: 最小の数値。
  type: Number
overloads:
  - line: 695
    params:
      - name: n0
        description: |
          <p>比較する最初の数値。</p>
        type: Number
      - name: n1
        description: |
          <p>比較する2番目の数値。</p>
        type: Number
    return:
      description: 最小の数値。
      type: Number
  - line: 757
    params:
      - name: nums
        description: |
          <p>比較する数値。</p>
        type: 'Number[]'
    return:
      description: ''
      type: Number
chainable: false
---


# min

