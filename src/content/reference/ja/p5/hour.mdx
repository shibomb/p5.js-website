---
title: hour
module: IO
submodule: Time & Date
file: src/utilities/time_date.js
description: |
  <p>現在の時間を0から23の数値で返します。</p>
line: 44
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 現在の時間を取得します。
      let h = hour();

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textSize(12);
      textFont('Courier New');

      // 時間を表示します。
      text(`現在の時間: ${h}`, 20, 50, 60);

      describe(`灰色の背景に黒字で '現在の時間: ${h}' と書かれたテキスト。`);
    }
    </code>
    </div>
class: p5
return:
  description: 0から23の間の現在の時間。
  type: Integer
chainable: false
---


# hour

