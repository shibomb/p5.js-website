---
title: second
module: IO
submodule: Time & Date
file: src/utilities/time_date.js
description: |
  <p>現在の秒を0から59の数値として返します。</p>
line: 277
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 現在の秒を取得します。
      let s = second();

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textSize(12);
      textFont('Courier New');

      // 秒を表示します。
      text(`現在の秒: ${s}`, 10, 50, 80);

      describe(`灰色の背景に黒字で '現在の秒: ${s}' と書かれたテキスト。`);
    }
    </code>
    </div>
class: p5
return:
  description: 0から59の間の現在の秒。
  type: Integer
chainable: false
---


# second

