---
title: textWidth
module: Typography
submodule: Attributes
file: src/typography/attributes.js
description: |
  <p>与えられた文字列の幅を計算します。</p>
line: 608
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    background(200);
    textSize(28);

    let aChar = 'P';
    let aString = 'p5.js';

    // 文字'P'の幅
    let cw = textWidth(aChar);
    // 文字列'p5.js'の幅
    let sw = textWidth(aString);

    line(0, 0, 0, 100);
    line(cw, 0, cw, 100);
    line(sw, 0, sw, 100);

    fill(0);

    text(aChar, 0, 40);
    text(aString, 0, 85);

    describe('文字"P"と文字列"p5.js"が表示されています。各テキストの終わりに垂直線が引かれています。');
    </code>
    </div>
class: p5
params:
  - name: str
    description: |
      <p>幅を計算する文字列</p>
    type: String
return:
  description: 文字列の幅
  type: Number
chainable: false
---

# textWidth