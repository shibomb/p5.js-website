---
title: displayDensity
module: Environment
submodule: Environment
file: src/core/environment.js
description: |
  <p>ディスプレイの現在のピクセル密度を返します。</p>
line: 1078
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      // ピクセル密度を1に設定します。
      pixelDensity(1);

      // キャンバスを作成し、
      // 円を描画します。
      createCanvas(100, 100);
      background(200);
      circle(50, 50, 70);

      describe('灰色の背景に描かれたぼやけた白い円。マウスが押されると円がシャープになります。');
    }

    function mousePressed() {
      // 現在のディスプレイ密度を取得します。
      let d = displayDensity();

      // ディスプレイ密度を使用して
      // スケッチのピクセル密度を設定します。
      pixelDensity(d);

      // 背景を塗り、
      // 円を描画します。
      background(200);
      circle(50, 50, 70);
    }
    </code>
    </div>
class: p5
return:
  description: ディスプレイの現在のピクセル密度。
  type: Number
chainable: false
---


# displayDensity

