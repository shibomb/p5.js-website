---
title: httpPost
module: IO
submodule: Input
file: src/io/files.js
description: >
  <p>HTTP POSTリクエストを実行するためのメソッドです。
  データ型が指定されていない場合、

  p5はURLに基づいて推測を試み、デフォルトではテキストとなります。これは

  <code>httpDo(path, 'POST')</code>を呼び出すのと同等です。</p>
line: 1119
isConstructor: false
itemtype: method
example:
  - >-

    <div>

    <code>

    // 例ではMockデータAPIとしてjsonplaceholder.typicode.comを使用しています


    let url = 'https://jsonplaceholder.typicode.com/posts';

    let postData = { userId: 1, title: 'p5がクリックされました！', body: 'p5.jsはとてもクールです。' };


    function setup() {
      createCanvas(100, 100);
      background(200);
    }


    function mousePressed() {
      httpPost(url, 'json', postData, function(result) {
        strokeWeight(2);
        text(result.body, mouseX, mouseY);
      });
    }

    </code>

    </div>


    <div><code>

    let url = 'ttps://invalidURL'; // エラーを引き起こす不正なURL

    let postData = { title: 'p5がクリックされました！', body: 'p5.jsはとてもクールです。' };


    function setup() {
      createCanvas(100, 100);
      background(200);
    }


    function mousePressed() {
      httpPost(
        url,
        'json',
        postData,
        function(result) {
          // ... 呼び出されません
        },
        function(error) {
          strokeWeight(2);
          text(error.toString(), mouseX, mouseY);
        }
      );
    }

    </code></div>
class: p5
return:
  description: |-
    操作が正常に完了したときにデータで解決するか、
                      エラーが発生した後にエラーで拒否するPromise。
  type: Promise
overloads:
  - line: 1119
    params:
      - name: path
        description: |
          <p>読み込むファイルまたはURLの名前</p>
        type: String
      - name: datatype
        description: |
          <p>"json"、"jsonp"、"xml"、または"text"。
                                             省略された場合、<a href="#/p5/httpPost">httpPost()</a>が推測します。</p>
        type: String
        optional: true
      - name: data
        description: |
          <p>リクエストと共に送信されるパラメータデータ</p>
        type: Object|Boolean
        optional: true
      - name: callback
        description: |
          <p><a href="#/p5/httpPost">httpPost()</a>が完了した後に実行される関数、
                                             データは最初の引数として渡されます</p>
        type: Function
        optional: true
      - name: errorCallback
        description: |
          <p>エラーがある場合に実行される関数、
                                             レスポンスは最初の引数として渡されます</p>
        type: Function
        optional: true
    return:
      description: |-
        操作が正常に完了したときにデータで解決するか、
                          エラーが発生した後にエラーで拒否するPromise。
      type: Promise
  - line: 1186
    params:
      - name: path
        description: ''
        type: String
      - name: data
        description: ''
        type: Object|Boolean
      - name: callback
        description: ''
        type: Function
        optional: true
      - name: errorCallback
        description: ''
        type: Function
        optional: true
    return:
      description: ''
      type: Promise
  - line: 1194
    params:
      - name: path
        description: ''
        type: String
      - name: callback
        description: ''
        type: Function
      - name: errorCallback
        description: ''
        type: Function
        optional: true
    return:
      description: ''
      type: Promise
chainable: false
---


# httpPost

