---
title: circle
module: Shape
submodule: 2D Primitives
file: src/core/shape/2d_primitives.js
description: >
  <p>円を描画します。</p>

  <p>円は<code>x</code>、<code>y</code>、<code>d</code>パラメータで定義される丸い形状です。

  <code>x</code>と<code>y</code>は円の中心の位置を設定します。
  <code>d</code>は円の幅と高さ（直径）を設定します。

  円の縁上のすべての点は、中心から同じ距離<code>0.5 * d</code>にあります。

  <code>0.5 * d</code>（直径の半分）が円の半径です。

  位置を設定する他の方法については、<a href="/reference/p5/ellipseMode/">ellipseMode()</a>を参照してください。</p>
line: 490
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      circle(50, 50, 25);

      describe('灰色のキャンバスの中央に黒い輪郭の白い円。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      circle(0, 0, 25);

      describe('灰色のキャンバスの中央に黒い輪郭の白い円。');
    }
    </code>
    </div>
class: p5
params:
  - name: x
    description: |
      <p>円の中心のx座標。</p>
    type: Number
  - name: 'y'
    description: |
      <p>円の中心のy座標。</p>
    type: Number
  - name: d
    description: |
      <p>円の直径。</p>
    type: Number
chainable: true
---


# circle
