---
title: cos
module: Math
submodule: Trigonometry
file: src/math/trigonometry.js
description: >
  <p>角度のコサインを計算します。</p>

  <p><code>cos()</code>はクリエイティブコーディングにおける多くの幾何学的タスクに役立ちます。
  返される値は、入力される角度が増加するにつれて-1から1の間で振動します。
  <code>cos()</code>は現在の<a
  href="/reference/p5/angleMode/">angleMode()</a>を考慮します。</p>
line: 281
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('白い球が紐に付いて左右に振動します。');
    }

    function draw() {
      background(200);

      // 座標を計算します。
      let x = 30 * cos(frameCount * 0.05) + 50;
      let y = 50;

      // 振動子を描画します。
      line(50, y, x, y);
      circle(x, y, 20);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('一連の黒い点が波のパターンを形成します。');
    }

    function draw() {
      // 座標を計算します。
      let x = frameCount;
      let y = 30 * cos(x * 0.1) + 50;

      // 点を描画します。
      point(x, y);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('一連の黒い点が無限大記号を形成します。');
    }

    function draw() {
      // 座標を計算します。
      let x = 30 * cos(frameCount * 0.1) + 50;
      let y = 10 * sin(frameCount * 0.2) + 50;

      // 点を描画します。
      point(x, y);
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: |
      <p>角度。</p>
    type: Number
return:
  description: 角度のコサイン。
  type: Number
chainable: false
---


# cos

