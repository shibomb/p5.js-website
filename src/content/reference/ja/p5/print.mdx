---
title: print
module: Environment
submodule: Environment
file: src/core/environment.js
description: >
  <p>ウェブブラウザのコンソールにテキストを表示します。</p>

  <p><code>print()</code>は、デバッグ中に値を表示するのに役立ちます。
  <code>print()</code>を呼び出すたびに、新しい行のテキストが作成されます。</p>

  <p>注意：空白行を表示するには<code>print('\n')</code>を呼び出します。
  引数なしで<code>print()</code>を呼び出すと、ブラウザの文書印刷ダイアログが開きます。</p>
line: 21
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code class="norender">
    function setup() {
      // コンソールに "hello, world" と表示します。
      print('hello, world');
    }
    </code>
    </div>

    <div>
    <code class="norender">
    function setup() {
      let name = 'ada';
      // コンソールに "hello, ada" と表示します。
      print(`hello, ${name}`);
    }
    </code>
    </div>
class: p5
params:
  - name: contents
    description: |
      <p>コンソールに表示する内容。</p>
    type: Any
chainable: false
---


# print

