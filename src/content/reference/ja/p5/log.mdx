---
title: log
module: Math
submodule: Calculation
file: src/math/calculation.js
description: >
  <p>数値の自然対数（底eの対数）を計算します。</p>

  <p><code>log()</code>は<code>n</code>パラメータが0より大きい値であることを期待します。
  なぜなら、自然対数はそのように定義されているからです。</p>
line: 423
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 左上
      let d = log(50);
      circle(33, 33, d);

      // 右下
      d = log(500000000);
      circle(67, 67, d);

      describe('2つの白い円。左上の円は小さく、右下の円は約5倍大きい。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('左から右に向かってゆっくりと高くなる一連の黒い点。');
    }

    function draw() {
      // y軸を反転
      scale(1, -1);
      translate(0, -100);

      // 座標を計算
      let x = frameCount;
      let y = 15 * log(x);

      // 点を描画
      point(x, y);
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>0より大きい数値。</p>
    type: Number
return:
  description: nの自然対数。
  type: Number
chainable: false
---


# log

