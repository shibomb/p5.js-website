---
title: createRadio
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>ラジオボタン要素を作成します。</p>

  <p>パラメータはオプションです。文字列が渡された場合、例えば

  <code>let myRadio = createSelect('food')</code>のように、各ラジオオプションは

  <code>"food"</code>を<code>name</code>パラメータとして持ちます：<code><input
  name="food"></input></code>。

  既存の<code><div></div></code>または<code><span></span></code>

  要素が渡された場合、例えば<code>let myRadio = createSelect(container)</code>のように、それが

  ラジオボタンの親要素になります。</p>

  <p>ラジオボタンは<a href="/reference/p5/p5.Element">p5.Element</a>クラスを拡張し、

  オプションを管理するためのいくつかの便利なメソッドを提供します：</p>

  <ul>

  <li><code>myRadio.option(value, [label])</code>はメニューにオプションを追加します。
  最初のパラメータ<code>value</code>は、オプションの値とラベルを設定する文字列です。2番目のパラメータ<code>label</code>はオプションです。
  提供された場合、<code>value</code>に対して表示されるラベルを設定します。
  <code>value</code>を持つオプションが既に存在する場合、そのラベルが変更され、その値が返されます。</li>

  <li><code>myRadio.value()</code>は現在選択されているオプションの値を返します。</li>

  <li><code>myRadio.selected()</code>は現在選択されているオプションを返します。</li>

  <li><code>myRadio.selected(value)</code>は指定されたオプションを選択し、
  <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/"
  target="_blank"><code>HTMLInputElement</code></a>として返します。</li>

  <li><code>myRadio.disable(shouldDisable)</code>は<code>true</code>が渡された場合にラジオボタン全体を有効にし、<code>false</code>が渡された場合に無効にします。</li>

  </ul>
line: 1440
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myRadio;

    function setup() {
      createCanvas(100, 100);

      // ラジオボタン要素を作成し、
      // 左上隅に配置します。
      myRadio = createRadio();
      myRadio.position(0, 0);
      myRadio.size(60);

      // いくつかの色オプションを追加します。
      myRadio.option('red');
      myRadio.option('yellow');
      myRadio.option('blue');

      // デフォルトのオプションを選択します。
      myRadio.selected('yellow');

      describe('黄色の正方形と3つの色オプション "red"、"yellow"、"blue" が表示されています。ユーザーが新しいオプションを選択すると、正方形の色が変わります。');
    }

    function draw() {
      // ラジオボタンを使用して背景色を設定します。
      let g = myRadio.value();
      background(g);
    }
    </code>
    </div>

    <div>
    <code>
    let myRadio;

    function setup() {
      createCanvas(100, 100);

      // ラジオボタン要素を作成し、
      // 左上隅に配置します。
      myRadio = createRadio();
      myRadio.position(0, 0);
      myRadio.size(50);

      // いくつかの色オプションを追加します。
      // 色の値には、それらが喚起する
      // 感情でラベルを付けています。
      myRadio.option('red', 'love');
      myRadio.option('yellow', 'joy');
      myRadio.option('blue', 'trust');

      // デフォルトのオプションを選択します。
      myRadio.selected('yellow');

      describe('黄色の正方形と3つのオプション "love"、"joy"、"trust" が表示されています。ユーザーが新しいオプションを選択すると、正方形の色が変わります。');
    }

    function draw() {
      // ラジオボタンを使用して背景色を設定します。
      let c = myRadio.value();
      background(c);
    }
    </code>
    </div>

    <div>
    <code>
    let myRadio;

    function setup() {
      createCanvas(100, 100);

      // ラジオボタン要素を作成し、
      // 左上隅に配置します。
      myRadio = createRadio();
      myRadio.position(0, 0);
      myRadio.size(50);

      // いくつかの色オプションを追加します。
      myRadio.option('red');
      myRadio.option('yellow');
      myRadio.option('blue');

      // デフォルトのオプションを選択します。
      myRadio.selected('yellow');

      // ボタンを作成し、キャンバスの下に配置します。
      let btn = createButton('disable');
      btn.position(0, 100);

      // btnが押されたときにdisableRadio()を呼び出します。
      btn.mousePressed(disableRadio);

      describe('黄色の正方形と3つのオプション "red"、"yellow"、"blue" が表示されています。ユーザーが新しいオプションを選択すると、正方形の色が変わります。キャンバスの下にある "disable" ボタンを押すと、色オプションが無効になります。');
    }

    function draw() {
      // ラジオボタンを使用して背景色を設定します。
      let c = myRadio.value();
      background(c);
    }

    // myRadioを無効にします。
    function disableRadio() {
      myRadio.disable(true);
    }
    </code>
    </div>
class: p5
return:
  description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
  type: p5.Element
overloads:
  - line: 1440
    params:
      - name: containerElement
        description: >
          <p>コンテナHTMLエレメント、<code>&lt;div&gt;&lt;/div&gt;</code>

          または<code>&lt;span&gt;&lt;/span&gt;</code>のいずれか。</p>
        type: Object
        optional: true
    return:
      description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
      type: p5.Element
  - line: 1574
    params:
      - name: name
        description: >
          <p>各オプションの<code>&lt;input&gt;&lt;/input&gt;</code>要素に割り当てられる
          nameパラメータ。</p>
        type: String
        optional: true
    return:
      description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
      type: p5.Element
  - line: 1579
    params: []
    return:
      description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
      type: p5.Element
chainable: false
---


# createRadio

