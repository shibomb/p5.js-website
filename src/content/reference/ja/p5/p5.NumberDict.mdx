---
title: p5.NumberDict
module: Data
submodule: Dictionary
file: src/data/p5.TypedDict.js
description: |
  <p>数値のためのシンプルな辞書クラス。</p>
line: 415
isConstructor: true
methods:
  add:
    description: |
      <p>指定されたキーに現在格納されている値に、与えられた数値を加算します。
      その合計が、以前に辞書に格納されていた値を置き換えます。</p>
    path: p5.NumberDict/add
  sub:
    description: >
      <p>指定されたキーに現在格納されている値から、与えられた数値を減算します。

      その差が、以前に辞書に格納されていた値を置き換えます。</p>
    path: p5.NumberDict/sub
  mult:
    description: >
      <p>指定されたキーに現在格納されている値に、与えられた数値を乗算します。

      その積が、以前に辞書に格納されていた値を置き換えます。</p>
    path: p5.NumberDict/mult
  div:
    description: >
      <p>指定されたキーに現在格納されている値を、与えられた数値で除算します。

      その商が、以前に辞書に格納されていた値を置き換えます。</p>
    path: p5.NumberDict/div
  minValue:
    description: |
      <p>辞書に現在格納されている最小の数値を返します。</p>
    path: p5.NumberDict/minValue
  maxValue:
    description: |
      <p>辞書に現在格納されている最大の数値を返します。</p>
    path: p5.NumberDict/maxValue
  minKey:
    description: |
      <p>辞書で現在使用されている最小のキーを返します。</p>
    path: p5.NumberDict/minKey
  maxKey:
    description: |
      <p>辞書で現在使用されている最大のキーを返します。</p>
    path: p5.NumberDict/maxKey
chainable: false
---


# p5.NumberDict

