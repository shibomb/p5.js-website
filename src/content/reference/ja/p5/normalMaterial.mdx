---
title: normalMaterial
module: 3D
submodule: Material
file: src/webgl/material.js
description: >
  <p>現在のマテリアルをノーマルマテリアルに設定します。</p>

  <p>ノーマルマテリアルは、x軸に向いている面を赤、y軸に向いている面を緑、z軸に向いている面を青に設定します。ノーマルマテリアルは光の影響を受けません。デバッグ時のプレースホルダーマテリアルとしてよく使用されます。</p>

  <p>注意：<code>normalMaterial()</code>はWebGLモードでのみ使用できます。</p>
line: 1510
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に描かれたマルチカラーのトーラス。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // トーラスのスタイルを設定します。
      normalMaterial();

      // トーラスを描画します。
      torus(30);
    }
    </code>
    </div>
class: p5
chainable: true
---


# normalMaterial

