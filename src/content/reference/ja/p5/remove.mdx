---
title: remove
module: Structure
submodule: Structure
file: src/core/main.js
description: >
  <p>スケッチをウェブページから削除します。</p>

  <p><code>remove()</code>を呼び出すと、描画ループを停止し、キャンバスを含む

  スケッチによって作成されたすべてのHTML要素を削除します。新しいスケッチは

  <a href="/reference/p5/p5/">p5()</a>コンストラクタを使用して作成できます。

  例えば、<code>new p5()</code>のようにします。</p>
line: 560
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // ダブルクリックでキャンバスを削除します。

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの背景に白い円。円はユーザーがマウスを動かすと追従します。ユーザーがダブルクリックするとスケッチが消えます。'
      );
    }

    function draw() {
      // 背景を繰り返し描画します。
      background(200);

      // 円を繰り返し描画します。
      circle(mouseX, mouseY, 40);
    }

    // ユーザーがダブルクリックするとスケッチを削除します。
    function doubleClicked() {
      remove();
    }
    </code>
    </div>
class: p5
chainable: false
---


# remove

