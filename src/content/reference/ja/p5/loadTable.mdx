---
title: loadTable
module: IO
submodule: Input
file: src/io/files.js
description: >
  <p>ファイルまたはURLの内容を読み込み、その値で<a
  href="/reference/p5/p5.Table">p5.Table</a>オブジェクトを作成します。

  ファイルが指定された場合、スケッチの「data」フォルダに配置されている必要があります。

  filenameパラメータには、オンラインで見つかるファイルへのURLも指定できます。

  デフォルトでは、ファイルはカンマ区切り（CSV形式）であると想定されます。

  'header'オプションが含まれている場合のみ、Tableはヘッダー行を探します。</p>

  <p>このメソッドは非同期であり、スケッチの次の行が実行される前に

  完了しない可能性があります。<a href="/reference/p5/preload/">preload()</a>内で

  <a href="/reference/p5/loadTable/">loadTable()</a>を呼び出すことで、

  <a href="/reference/p5/setup/">setup()</a>と<a href="/reference/p5/draw/">draw()</a>が呼び出される前に

  操作が完了することが保証されます。

  <a href="/reference/p5/preload/">preload()</a>の外部では、オブジェクトを処理するために

  コールバック関数を提供することができます：</p>

  <p>読み込まれて保存されるすべてのファイルはUTF-8エンコーディングを使用します。このメソッドは64MBまでのサイズのファイルを取得するのに適しています。</p>
line: 505
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'>
    <code>
    // プロジェクトの "assets" フォルダにある
    // 以下のCSVファイル "mammals.csv" が与えられた場合：
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leopard
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      //私のテーブルはカンマ区切り値 "csv" で
      //列ラベルを指定するヘッダーがあります
      table = loadTable('/assets/mammals.csv', 'csv', 'header');
      //ファイルはリモートでも可能です
      //table = loadTable("http://p5js.org/reference//assets/mammals.csv",
      //                  "csv", "header");
    }

    function setup() {
      //列をカウントします
      print(table.getRowCount() + ' テーブル内の総行数');
      print(table.getColumnCount() + ' テーブル内の総列数');

      print(table.getColumn('name'));
      //["Goat", "Leopard", "Zebra"]

      //テーブルを巡回します
      for (let r = 0; r < table.getRowCount(); r++)
        for (let c = 0; c < table.getColumnCount(); c++) {
          print(table.getString(r, c));
        }
      describe(`ファイルからランダムに生成されたテキスト、
        例えば "私はバターの匂いがする"`);
    }
    </code>
    </div>
class: p5
params:
  - name: filename
    description: |
      <p>読み込むファイルまたはURLの名前</p>
    type: String
  - name: extension
    description: |
      <p>テーブルをカンマ区切り値 "csv"、セミコロン区切り値 "ssv"、
                                           またはタブ区切り値 "tsv" で解析します</p>
    type: String
    optional: true
  - name: header
    description: |
      <p>テーブルにヘッダー行があることを示す "header"</p>
    type: String
    optional: true
  - name: callback
    description: |
      <p><a href="#/p5/loadTable">loadTable()</a>が完了した後に
                                           実行される関数。成功した場合、
                                           <a href="#/p5.Table">Table</a>オブジェクトが
                                           最初の引数として渡されます。</p>
    type: Function
    optional: true
  - name: errorCallback
    description: |
      <p>エラーがある場合に実行される関数、
                                           レスポンスが最初の引数として渡されます</p>
    type: Function
    optional: true
return:
  description: データを含む<a href="#/p5.Table">Table</a>オブジェクト
  type: Object
chainable: false
---


# loadTable

