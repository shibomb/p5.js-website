---
title: pRotationY
module: Events
submodule: Acceleration
file: src/events/acceleration.js
description: >
  <p>システム変数pRotationYは、常に現在のフレームの前のフレームにおける

  デバイスのY軸周りの回転を含んでいます。

  スケッチの<a href="/reference/p5/angleMode/">angleMode()</a>がDEGREESに設定されている場合、

  値は-90から90になります。RADIANSに設定されている場合、値は

  -PI/2からPI/2になります。</p>

  <p>pRotationYは、rotationYと共に使用して、デバイスのY軸周りの回転

  方向を判断することもできます。</p>
line: 278
isConstructor: false
itemtype: property
example:
  - |-

    <div class='norender'>
    <code>
    // rotationY - pRotationY < 0 が真かどうかを
    // 単純にif文で確認するだけで、ほとんどの場合
    // 回転方向を判断するのに十分です。

    // 角度が一周する場合に対応するために
    // 追加のロジックが必要です。
    let rotateDirection = 'clockwise';

    // 物事を単純化するための簡単な範囲変換。
    // これは絶対に必要ではありませんが、
    // そうでない場合はロジックが異なります。

    let rY = rotationY + 180;
    let pRY = pRotationY + 180;

    if ((rY - pRY > 0 && rY - pRY < 270) || rY - pRY < -270) {
      rotateDirection = 'clockwise';
    } else if (rY - pRY < 0 || rY - pRY > 270) {
      rotateDirection = 'counter-clockwise';
    }
    print(rotateDirection);
    describe('表示する画像はありません。');
    </code>
    </div>
class: p5
type: Number
---


# pRotationY

