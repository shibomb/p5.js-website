---
title: blend
module: Image
submodule: Pixels
file: src/image/pixels.js
description: >
  <p>ある画像から別の画像へピクセルの領域をコピーします。</p>

  <p>最初のパラメータ <code>srcImage</code> は、ブレンドする

  <a href="/reference/p5/p5.Image">p5.Image</a> オブジェクトです。</p>

  <p>次の4つのパラメータ <code>sx</code>、<code>sy</code>、<code>sw</code>、<code>sh</code> は、

  ソース画像からブレンドする領域を決定します。<code>(sx, sy)</code> は領域の

  左上隅です。<code>sw</code> と <code>sh</code> は領域の幅と

  高さです。</p>

  <p>次の4つのパラメータ <code>dx</code>、<code>dy</code>、<code>dw</code>、<code>dh</code> は、

  ブレンド先のキャンバスの領域を決定します。<code>(dx, dy)</code> は領域の

  左上隅です。<code>dw</code> と <code>dh</code> は領域の幅と

  高さです。</p>

  <p>10番目のパラメータ <code>blendMode</code> は、画像の色をブレンドする

  際に使用するエフェクトを設定します。オプションには <code>BLEND</code>、<code>DARKEST</code>、

  <code>LIGHTEST</code>、<code>DIFFERENCE</code>、<code>MULTIPLY</code>、<code>EXCLUSION</code>、

  <code>SCREEN</code>、<code>REPLACE</code>、<code>OVERLAY</code>、<code>HARD_LIGHT</code>、

  <code>SOFT_LIGHT</code>、<code>DODGE</code>、<code>BURN</code>、<code>ADD</code>、または <code>NORMAL</code> があります。</p>
line: 148
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let img0;
    let img1;

    // 画像を読み込みます。
    function preload() {
      img0 = loadImage('/assets/rockies.jpg');
      img1 = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // 山々を背景として使用します。
      background(img0);

      // レンガを表示します。
      image(img1, 0, 0);

      // レンガを風景にフェードさせて表示します。
      blend(img1, 0, 0, 33, 100, 67, 0, 33, 100, LIGHTEST);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側にフェードして表示されています。');
    }
    </code>
    </div>

    <div>
    <code>
    let img0;
    let img1;

    // 画像を読み込みます。
    function preload() {
      img0 = loadImage('/assets/rockies.jpg');
      img1 = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // 山々を背景として使用します。
      background(img0);

      // レンガを表示します。
      image(img1, 0, 0);

      // レンガを部分的に透明にして表示します。
      blend(img1, 0, 0, 33, 100, 67, 0, 33, 100, DARKEST);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側に透明に表示されています。');
    }
    </code>
    </div>

    <div>
    <code>
    let img0;
    let img1;

    // 画像を読み込みます。
    function preload() {
      img0 = loadImage('/assets/rockies.jpg');
      img1 = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // 山々を背景として使用します。
      background(img0);

      // レンガを表示します。
      image(img1, 0, 0);

      // レンガを風景に溶け込ませて表示します。
      blend(img1, 0, 0, 33, 100, 67, 0, 33, 100, ADD);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側に溶け込んで表示されています。');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 148
    params:
      - name: srcImage
        description: |
          <p>ソース画像。</p>
        type: p5.Image
      - name: sx
        description: |
          <p>ソースの左上隅のx座標。</p>
        type: Integer
      - name: sy
        description: |
          <p>ソースの左上隅のy座標。</p>
        type: Integer
      - name: sw
        description: |
          <p>ソース画像の幅。</p>
        type: Integer
      - name: sh
        description: |
          <p>ソース画像の高さ。</p>
        type: Integer
      - name: dx
        description: |
          <p>目的地の左上隅のx座標。</p>
        type: Integer
      - name: dy
        description: |
          <p>目的地の左上隅のy座標。</p>
        type: Integer
      - name: dw
        description: |
          <p>目的地の画像の幅。</p>
        type: Integer
      - name: dh
        description: |
          <p>目的地の画像の高さ。</p>
        type: Integer
      - name: blendMode
        description: |
          <p>ブレンドモード。BLEND、DARKEST、LIGHTEST、DIFFERENCE、
              MULTIPLY、EXCLUSION、SCREEN、REPLACE、OVERLAY、HARD_LIGHT、
              SOFT_LIGHT、DODGE、BURN、ADD、またはNORMALのいずれか。</p>
        type: Constant
  - line: 267
    params:
      - name: sx
        description: ''
        type: Integer
      - name: sy
        description: ''
        type: Integer
      - name: sw
        description: ''
        type: Integer
      - name: sh
        description: ''
        type: Integer
      - name: dx
        description: ''
        type: Integer
      - name: dy
        description: ''
        type: Integer
      - name: dw
        description: ''
        type: Integer
      - name: dh
        description: ''
        type: Integer
      - name: blendMode
        description: ''
        type: Constant
chainable: false
---


# blend

