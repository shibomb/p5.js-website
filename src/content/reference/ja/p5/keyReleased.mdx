---
title: keyReleased
module: Events
submodule: Keyboard
file: src/events/keyboard.js
description: >
  <p>任意のキーが離されたときに一度呼び出される関数です。</p>

  <p><code>keyReleased()</code>関数を宣言すると、ユーザーが任意のキーを離したときに
  自動的に一度実行されるコードブロックを設定します：</p>

  <pre><code class="language-js">function keyReleased() {
    // 実行するコード
  }

  </code></pre>

  <p><a href="/reference/p5/key/">key</a>変数と<a
  href="/reference/p5/keyCode/">keyCode</a>変数は、

  p5.jsによって<code>keyReleased()</code>が呼び出されたときに、

  最後に離されたキーの値で更新されます：</p>

  <pre><code class="language-js">function keyReleased() {
    if (key === 'c') {
      // 実行するコード
    }

    if (keyCode === ENTER) {
      // 実行するコード
    }
  }

  </code></pre>

  <p>パラメータ<code>event</code>はオプションです。<code>keyReleased()</code>には常に

  キー押下イベントを説明するプロパティを持つ<a href="https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/"
  target="_blank">KeyboardEvent</a>

  オブジェクトが渡されます：</p>

  <pre><code class="language-js">function keyReleased(event) {
    // イベントを使用して実行するコード
    console.log(event);
  }

  </code></pre>

  <p>ブラウザは様々なキーイベントにデフォルトの動作を関連付けている場合があります。

  このイベントのデフォルトの動作を防ぐには、関数の最後に<code>return false;</code>を

  追加してください。</p>
line: 461
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に黒い正方形がある灰色の正方形。ユーザーがキーを離すと内側の正方形の色が変わります。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定
      fill(value);

      // 正方形を描画
      square(25, 25, 50);
    }

    // ユーザーがキーを離したときにvalueを切り替えます。
    function keyReleased() {
      if (value === 0) {
        value = 255;
      } else {
        value = 0;
      }
      // デフォルトの動作を防ぐにはコメントを解除してください。
      // return false;
    }
    </code>
    </div>

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に黒い正方形がある灰色の正方形。ユーザーが"w"キーを離すと内側の正方形が白くなります。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定
      fill(value);

      // 正方形を描画
      square(25, 25, 50);
    }

    // ユーザーが'w'キーを離したときにvalueを255に設定します。
    function keyReleased() {
      if (key === 'w') {
        value = 255;
      }
      // デフォルトの動作を防ぐにはコメントを解除してください。
      // return false;
    }
    </code>
    </div>

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に黒い正方形がある灰色の正方形。ユーザーが左矢印キーを押して離すと内側の正方形が白くなります。右矢印キーを押して離すと黒くなります。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定
      fill(value);

      // 正方形を描画
      square(25, 25, 50);
    }

    // ユーザーが矢印キーを離したときに背景色を切り替えます。
    function keyReleased() {
      if (keyCode === LEFT_ARROW) {
        value = 255;
      } else if (keyCode === RIGHT_ARROW) {
        value = 0;
      }
      // デフォルトの動作を防ぐにはコメントを解除してください。
      // return false;
    }
    </code>
    </div>
class: p5
params:
  - name: event
    description: |
      <p>オプションの<code>KeyboardEvent</code>コールバック引数。</p>
    type: KeyboardEvent
    optional: true
chainable: false
---


# keyReleased

