---
title: focused
module: Environment
submodule: Environment
file: src/core/environment.js
description: >
  <p>ブラウザがフォーカスされている場合は<code>true</code>、そうでない場合は<code>false</code>となる<code>Boolean</code>変数です。</p>

  <p>注意：ブラウザウィンドウは、フォーカスされている場合にのみ入力を受け付けることができます。</p>
line: 174
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // この例を2つの別々のブラウザウィンドウで
    // 並べて開いて確認してください。

    function setup() {
      createCanvas(100, 100);

      describe('ブラウザウィンドウがフォーカスを失うと、四角形の色が緑から赤に変わります。');
    }

    function draw() {
      // ブラウザウィンドウが
      // フォーカスを得たり失ったりすると
      // 背景色を変更します。
      if (focused === true) {
        background(0, 255, 0);
      } else {
        background(255, 0, 0);
      }
    }
    </code>
    </div>
class: p5
type: Boolean
---


# focused

