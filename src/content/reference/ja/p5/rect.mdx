---
title: rect
module: Shape
submodule: 2D Primitives
file: src/core/shape/2d_primitives.js
description: >
  <p>四角形を描きます。</p>

  <p>四角形は、<code>x</code>、<code>y</code>、<code>w</code>、および<code>h</code>のパラメーターで定義される四辺形です。<code>x</code>と<code>y</code>はその左上隅の位置を設定します。<code>w</code>はその幅を設定し、<code>h</code>はその高さを設定します。四角形のすべての角度は90˚です。四角形を定義する他の方法については、<a href="/reference/p5/rectMode/">rectMode()</a>を参照してください。</p>

  <p>5つのパラメーターを持つ<code>rect()</code>のバージョンは、角が丸い四角形を作成します。5番目のパラメーターは、すべての角の半径を設定します。</p>

  <p>8つのパラメーターを持つ<code>rect()</code>のバージョンも、角が丸い四角形を作成します。最後の4つのパラメーターはそれぞれの角の半径を設定します。半径は左上の角から始まり、時計回りに四角形を回ります。これらのパラメーターのいずれかが省略された場合、それらは最後に設定された半径の値に設定されます。</p>
line: 1085
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rect(30, 20, 55, 55);

      describe('灰色のキャンバスに黒い輪郭の白い正方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      rect(30, 20, 55, 40);

      describe('灰色のキャンバスに黒い輪郭の白い長方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // すべての角に半径20を与える。
      rect(30, 20, 55, 50, 20);

      describe('灰色のキャンバスに黒い輪郭と丸い角を持つ白い長方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 各角にユニークな半径を与える。
      rect(30, 20, 55, 50, 20, 15, 10, 5);

      describe('異なる半径の丸い角を持つ黒い輪郭の白い長方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      rect(-20, -30, 55, 55);

      describe('灰色のキャンバスに黒い輪郭の白い正方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色のキャンバスに白い正方形が回転する。');
    }

    function draw() {
      background(200);

      // y軸を中心に回転する。
      rotateY(frameCount * 0.01);

      // 四角形を描く。
      rect(-20, -30, 55, 55);
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1085
    params:
      - name: x
        description: |
          <p>四角形のx座標。</p>
        type: Number
      - name: 'y'
        description: |
          <p>四角形のy座標。</p>
        type: Number
      - name: w
        description: |
          <p>四角形の幅。</p>
        type: Number
      - name: h
        description: |
          <p>四角形の高さ。</p>
        type: Number
        optional: true
      - name: tl
        description: |
          <p>左上の角のオプションの半径。</p>
        type: Number
        optional: true
      - name: tr
        description: |
          <p>右上の角のオプションの半径。</p>
        type: Number
        optional: true
      - name: br
        description: |
          <p>右下の角のオプションの半径。</p>
        type: Number
        optional: true
      - name: bl
        description: |
          <p>左下の角のオプションの半径。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 1208
    params:
      - name: x
        description: ''
        type: Number
      - name: 'y'
        description: ''
        type: Number
      - name: w
        description: ''
        type: Number
      - name: h
        description: ''
        type: Number
      - name: detailX
        description: |
          <p>（WebGLモード用）x方向のセグメント数。</p>
        type: Integer
        optional: true
      - name: detailY
        description: |
          <p>（WebGLモード用）y方向のセグメント数。</p>
        type: Integer
        optional: true
    chainable: 1
chainable: true
---


# rect
