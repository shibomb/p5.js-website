---
title: saveTable
module: IO
submodule: Output
file: src/io/files.js
description: >
  <p><a href="/reference/p5/p5.Table">Table</a>オブジェクトの内容をファイルに書き込みます。
  デフォルトではカンマ区切り値（'csv'）のテキストファイルですが、
  タブ区切り（'tsv'）を使用したり、HTMLテーブル（'html'）を生成したりすることもできます。
  ファイルの保存プロセスと保存されたファイルの場所は、
  Webブラウザによって異なります。</p>
line: 2275
isConstructor: false
itemtype: method
example:
  - |-

    <div><code>
     let table;

     function setup() {
     table = new p5.Table();

     table.addColumn('id');
     table.addColumn('species');
     table.addColumn('name');

     let newRow = table.addRow();
     newRow.setNum('id', table.getRowCount() - 1);
     newRow.setString('species', 'Panthera leo');
     newRow.setString('name', 'Lion');

     // 保存するには、次の行のコメントを解除して'実行'をクリックしてください
     // saveTable(table, 'new.csv');

     describe('画像は表示されません');
     }

     // 以下の内容が'new.csv'というファイルに保存されます：
     // id,species,name
     // 0,Panthera leo,Lion
     </code></div>
class: p5
params:
  - name: Table
    description: |
      <p>ファイルに保存する<a href="#/p5.Table">Table</a>オブジェクト</p>
    type: p5.Table
  - name: filename
    description: |
      <p>Tableを保存するファイル名</p>
    type: String
  - name: options
    description: |
      <p>"tsv"、"csv"、または"html"のいずれかを指定できます</p>
    type: String
    optional: true
chainable: false
---


# saveTable

