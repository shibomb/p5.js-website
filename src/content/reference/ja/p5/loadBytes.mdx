---
title: loadBytes
module: IO
submodule: Input
file: src/io/files.js
description: |
  <p>このメソッドは、最大64MBまでのサイズのファイルを取得するのに適しています。</p>
line: 986
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'><code>
    let data;

    function preload() {
      data = loadBytes('/assets/mammals.xml');
    }

    function setup() {
      for (let i = 0; i < 5; i++) {
        console.log(data.bytes[i].toString(16));
      }
      describe('画像は表示されません');
    }
    </code></div>
class: p5
params:
  - name: file
    description: |
      <p>読み込むファイルの名前またはURL</p>
    type: String
  - name: callback
    description: |
      <p><a href="#/p5/loadBytes">loadBytes()</a>が完了した後に実行される関数</p>
    type: Function
    optional: true
  - name: errorCallback
    description: |
      <p>エラーが発生した場合に実行される関数</p>
    type: Function
    optional: true
return:
  description: bytesプロパティが読み込まれたバッファーになるオブジェクト
  type: Object
chainable: false
---


# loadBytes
