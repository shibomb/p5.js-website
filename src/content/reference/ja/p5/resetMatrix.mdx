---
title: resetMatrix
module: Transform
submodule: Transform
file: src/core/transform.js
description: |
  <p>座標系に適用されたすべての変換をクリアします。</p>
line: 246
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの背景に2つの円が描かれています。青い円が左上に、赤い円が右下にあります。'
      );
    }

    function draw() {
      background(200);

      // 原点を中心に移動します。
      translate(50, 50);

      // 座標(25, 25)に青い円を描きます。
      fill('blue');
      circle(25, 25, 20);

      // すべての変換をクリアします。
      // 原点は今、左上の角にあります。
      resetMatrix();

      // 座標(25, 25)に赤い円を描きます。
      fill('red');
      circle(25, 25, 20);
    }
    </code>
    </div>
class: p5
chainable: true
---


# resetMatrix

