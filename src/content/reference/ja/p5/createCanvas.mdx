---
title: createCanvas
module: Rendering
submodule: Rendering
file: src/core/rendering.js
description: >
  <p>ウェブページにキャンバス要素を作成します。</p>

  <p><code>createCanvas()</code>はスケッチのメインの描画キャンバスを作成します。
  これは<a href="/reference/p5/setup/">setup()</a>の最初に一度だけ呼び出すべきです。
  <code>createCanvas()</code>を複数回呼び出すと予期せぬ動作を引き起こします。</p>

  <p>最初の2つのパラメータ、<code>width</code>と<code>height</code>はオプションです。これらは
  キャンバスの寸法と<a href="/reference/p5/width/">width</a>および
  <a href="/reference/p5/height/">height</a>システム変数の値を設定します。
  例えば、<code>createCanvas(900, 500)</code>を呼び出すと、900×500ピクセルの
  キャンバスが作成されます。デフォルトでは、<code>width</code>と<code>height</code>は
  どちらも100です。</p>

  <p>3番目のパラメータもオプションです。<code>P2D</code>または<code>WEBGL</code>の
  いずれかの定数が渡された場合（例：<code>createCanvas(900, 500, WEBGL)</code>）、
  スケッチのレンダリングモードが設定されます。既存の
  <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/" target="_blank">HTMLCanvasElement</a>
  が渡された場合（例：<code>createCanvas(900, 500, myCanvas)</code>）、
  それがスケッチによって使用されます。</p>

  <p>4番目のパラメータもオプションです。既存の
  <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/" target="_blank">HTMLCanvasElement</a>
  が渡された場合（例：<code>createCanvas(900, 500, WEBGL, myCanvas)</code>）、
  それがスケッチによって使用されます。</p>

  <p>注意：WebGLモードでは、ブラウザがサポートしている場合、キャンバスはWebGL2コンテキストを
  使用します。使用されているバージョンを確認するには<a href="/reference/p5/webglVersion/">webglVersion</a>
  システム変数をチェックするか、<code>setAttributes({ version: 1 })</code>を呼び出して
  WebGL1コンテキストを作成してください。</p>
line: 15
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 対角線を描画します。
      line(0, 0, width, height);

      describe('灰色の背景に左上から右下に引かれた対角線。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 50);

      background(200);

      // 対角線を描画します。
      line(0, 0, width, height);

      describe('灰色の背景に左上から右下に引かれた対角線。');
    }
    </code>
    </div>

    <div>
    <code>
    // WebGLモードを使用します。

    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      // 対角線を描画します。
      line(-width / 2, -height / 2, width / 2, height / 2);

      describe('灰色の背景に左上から右下に引かれた対角線。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      // p5.Renderオブジェクトを作成します。
      let cnv = createCanvas(50, 50);

      // キャンバスを配置します。
      cnv.position(10, 20);

      background(200);

      // 対角線を描画します。
      line(0, 0, width, height);

      describe('灰色の背景に左上から右下に引かれた対角線。');
    }
    </code>
    </div>
class: p5
return:
  description: キャンバスを保持する新しい`p5.Renderer`。
  type: p5.Renderer
overloads:
  - line: 15
    params:
      - name: width
        description: |
          <p>キャンバスの幅。デフォルトは100です。</p>
        type: Number
        optional: true
      - name: height
        description: |
          <p>キャンバスの高さ。デフォルトは100です。</p>
        type: Number
        optional: true
      - name: renderer
        description: |
          <p>P2DまたはWEBGL。デフォルトは<code>P2D</code>です。</p>
        type: Constant
        optional: true
      - name: canvas
        description: |
          <p>スケッチに使用される既存のキャンバス要素。</p>
        type: HTMLCanvasElement
        optional: true
    return:
      description: キャンバスを保持する新しい`p5.Renderer`。
      type: p5.Renderer
  - line: 119
    params:
      - name: width
        description: ''
        type: Number
        optional: true
      - name: height
        description: ''
        type: Number
        optional: true
      - name: canvas
        description: ''
        type: HTMLCanvasElement
        optional: true
    return:
      description: ''
      type: p5.Renderer
chainable: false
---


# createCanvas

