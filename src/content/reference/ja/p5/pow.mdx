---
title: pow
module: Math
submodule: Calculation
file: src/math/calculation.js
description: |
  <p><var>2<sup>3</sup></var>のような指数表現を計算します。</p>
  <p>例えば、<code>pow(2, 3)</code>は2 × 2 × 2という式を評価します。
  <code>pow(2, -3)</code>は1 ÷ (2 × 2 × 2)を評価します。</p>
line: 815
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 指数の底を設定します。
      let base = 3;

      // 左上
      let d = pow(base, 1);
      circle(10, 10, d);

      // 左中央
      d = pow(base, 2);
      circle(20, 20, d);

      // 右中央
      d = pow(base, 3);
      circle(40, 40, d);

      // 右下
      d = pow(base, 4);
      circle(80, 80, d);

      describe('左上から右下に向かって指数関数的に大きくなる一連の円。');
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>指数表現の底。</p>
    type: Number
  - name: e
    description: |
      <p>底を累乗する指数。</p>
    type: Number
return:
  description: n^e
  type: Number
chainable: false
---


# pow

