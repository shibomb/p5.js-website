---
title: beginShape
module: Shape
submodule: Vertex
file: src/core/shape/vertex.js
description: >
  <p>カスタム形状に頂点の追加を開始します。</p>

  <p><code>beginShape()</code>と<a
  href="/reference/p5/endShape/">endShape()</a>関数は、

  2Dまたは3Dでカスタム形状を作成することができます。<code>beginShape()</code>は

  カスタム形状への頂点の追加を開始し、<a
  href="/reference/p5/endShape/">endShape()</a>は

  その追加を停止します。</p>

  <p>パラメータ<code>kind</code>は、作成する形状の種類を設定します。デフォルトでは、

  任意の不規則な多角形を描画できます。kindに使用できるモードは以下の通りです：</p>

  <ul>

  <li><code>POINTS</code>：一連の点を描画します。</li>

  <li><code>LINES</code>：一連の接続されていない線分を描画します。</li>

  <li><code>TRIANGLES</code>：一連の個別の三角形を描画します。</li>

  <li><code>TRIANGLE_FAN</code>：最初の頂点を共有する一連の接続された三角形をファン状に描画します。</li>

  <li><code>TRIANGLE_STRIP</code>：一連の接続された三角形をストリップ状に描画します。</li>

  <li><code>QUADS</code>：一連の個別の四辺形（クアッド）を描画します。</li>

  <li><code>QUAD_STRIP</code>：隣接する辺を使用して次のクアッドを形成するクアッドストリップを描画します。</li>

  <li><code>TESS</code>：明示的なテッセレーションによって塗りつぶし曲線を作成します（WebGLのみ）。</li>

  </ul>

  <p><code>beginShape()</code>を呼び出した後、

  <a href="/reference/p5/vertex/">vertex()</a>、

  <a href="/reference/p5/bezierVertex/">bezierVertex()</a>、

  <a href="/reference/p5/quadraticVertex/">quadraticVertex()</a>、および/または

  <a href="/reference/p5/curveVertex/">curveVertex()</a>を呼び出すことで形状を構築できます。

  <a href="/reference/p5/endShape/">endShape()</a>を呼び出すと、形状への頂点の

  追加が停止します。各形状は現在のストロークカラーで輪郭が描かれ、

  現在のフィルカラーで塗りつぶされます。</p>

  <p><a href="/reference/p5/translate/">translate()</a>、

  <a href="/reference/p5/rotate/">rotate()</a>、

  <a href="/reference/p5/scale/">scale()</a>などの変換は、<code>beginShape()</code>と

  <a href="/reference/p5/endShape/">endShape()</a>の間では機能しません。また、

  <a href="/reference/p5/ellipse/">ellipse()</a>や

  <a href="/reference/p5/rect/">rect()</a>などの他の形状を<code>beginShape()</code>と

  <a href="/reference/p5/endShape/">endShape()</a>の間で使用することもできません。</p>
line: 129
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      beginShape();

      // 頂点を追加します。
      vertex(30, 20);
      vertex(85, 20);
      vertex(85, 75);
      vertex(30, 75);

      // 形状の描画を停止します。
      endShape(CLOSE);

      describe('灰色の背景に白い正方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 頂点（点）のみを描画します。
      beginShape(POINTS);

      // 頂点を追加します。
      vertex(30, 20);
      vertex(85, 20);
      vertex(85, 75);
      vertex(30, 75);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に正方形を形成する4つの黒い点が描かれています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 交互の頂点のペアの間に線のみを描画します。
      beginShape(LINES);

      // 頂点を追加します。
      vertex(30, 20);
      vertex(85, 20);
      vertex(85, 75);
      vertex(30, 75);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に2本の水平な黒い線。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状のスタイルを設定します。
      noFill();

      // 形状の描画を開始します。
      beginShape();

      // 頂点を追加します。
      vertex(30, 20);
      vertex(85, 20);
      vertex(85, 75);
      vertex(30, 75);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に横向きのU字形を形成する3本の黒い線。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状のスタイルを設定します。
      noFill();

      // 形状の描画を開始します。
      beginShape();

      // 頂点を追加します。
      vertex(30, 20);
      vertex(85, 20);
      vertex(85, 75);
      vertex(30, 75);

      // 形状の描画を停止します。
      // 最初と最後の頂点を接続します。
      endShape(CLOSE);

      describe('灰色の背景に描かれた正方形の黒い輪郭。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 一連の三角形を描画します。
      beginShape(TRIANGLES);

      // 左の三角形。
      vertex(30, 75);
      vertex(40, 20);
      vertex(50, 75);

      // 右の三角形。
      vertex(60, 20);
      vertex(70, 75);
      vertex(80, 20);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に描かれた2つの白い三角形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 一連の三角形を描画します。
      beginShape(TRIANGLE_STRIP);

      // 頂点を追加します。
      vertex(30, 75);
      vertex(40, 20);
      vertex(50, 75);
      vertex(60, 20);
      vertex(70, 75);
      vertex(80, 20);
      vertex(90, 75);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に描かれた5つの白い三角形が交互に配置されています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 最初の頂点を共有する一連の三角形を描画します。
      beginShape(TRIANGLE_FAN);

      // 頂点を追加します。
      vertex(57.5, 50);
      vertex(57.5, 15);
      vertex(92, 50);
      vertex(57.5, 85);
      vertex(22, 50);
      vertex(57.5, 15);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に正方形を形成する4つの白い三角形が描かれています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 一連の四辺形を描画します。
      beginShape(QUADS);

      // 左の長方形。
      vertex(30, 20);
      vertex(30, 75);
      vertex(50, 75);
      vertex(50, 20);

      // 右の長方形。
      vertex(65, 20);
      vertex(65, 75);
      vertex(85, 75);
      vertex(85, 20);

      // 形状の描画を停止します。
      vertex(85, 20);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に描かれた2つの白い長方形。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 形状の描画を開始します。
      // 一連の四辺形を描画します。
      beginShape(QUAD_STRIP);

      // 頂点を追加します。
      vertex(30, 20);
      vertex(30, 75);
      vertex(50, 20);
      vertex(50, 75);
      vertex(65, 20);
      vertex(65, 75);
      vertex(85, 20);
      vertex(85, 75);

      // 形状の描画を停止します。
      endShape();

      describe('灰色の背景に辺を共有する3つの白い長方形が描かれています。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      background(200);

      // 形状の描画を開始します。
      // 一連の四辺形を描画します。
      beginShape(TESS);

      // 頂点を追加します。
      vertex(-30, -30, 0);
      vertex(30, -30, 0);
      vertex(30, -10, 0);
      vertex(-10, -10, 0);
      vertex(-10, 10, 0);
      vertex(30, 10, 0);
      vertex(30, 30, 0);
      vertex(-30, 30, 0);

      // 形状の描画を停止します。
      // 最初と最後の頂点を接続します。
      endShape(CLOSE);

      describe('灰色の背景に白で描かれたブロック状のC形。');
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に赤、青、緑で描かれたブロック状のC形。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // 形状の描画を開始します。
      // 一連の四辺形を描画します。
      beginShape(TESS);

      // 頂点を追加します。
      fill('red');
      stroke('red');
      vertex(-30, -30, 0);
      vertex(30, -30, 0);
      vertex(30, -10, 0);
      fill('green');
      stroke('green');
      vertex(-10, -10, 0);
      vertex(-10, 10, 0);
      vertex(30, 10, 0);
      fill('blue');
      stroke('blue');
      vertex(30, 30, 0);
      vertex(-30, 30, 0);

      // 形状の描画を停止します。
      // 最初と最後の頂点を接続します。
      endShape(CLOSE);
    }
    </code>
    </div>
class: p5
params:
  - name: kind
    description: |
      <p>POINTS、LINES、TRIANGLES、TRIANGLE_FAN、
                                     TRIANGLE_STRIP、QUADS、QUAD_STRIP、またはTESSのいずれか。</p>
    type: Constant
    optional: true
chainable: true
---


# beginShape

