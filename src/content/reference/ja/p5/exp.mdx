---
title: exp
module: Math
submodule: Calculation
file: src/math/calculation.js
description: |
  <p>オイラー数e（2.71828...）を指定された数値で累乗した値を計算します。</p>
line: 230
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 左上
      let d = exp(1);
      circle(10, 10, d);

      // 左中央
      d = exp(2);
      circle(20, 20, d);

      // 右中央
      d = exp(3);
      circle(40, 40, d);

      // 右下
      d = exp(4);
      circle(80, 80, d);

      describe('左上から右下に向かって指数関数的に大きくなる一連の円。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('左から右に向かって指数関数的に大きくなる一連の黒い点。');
    }

    function draw() {
      // y軸を反転
      scale(1, -1);
      translate(0, -100);

      // 座標を計算
      let x = frameCount;
      let y = 0.005 * exp(x * 0.1);

      // 点を描画
      point(x, y);
    }
    </code>
    </div>
class: p5
params:
  - name: 'n'
    description: |
      <p>累乗する指数。</p>
    type: Number
return:
  description: e^n
  type: Number
chainable: false
---


# exp

