---
title: month
module: IO
submodule: Time & Date
file: src/utilities/time_date.js
description: |
  <p>現在の月を1〜12の数字で返します。</p>
line: 242
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 現在の月を取得します。
      let m = month();

      // テキストのスタイルを設定します。
      textAlign(LEFT, CENTER);
      textSize(12);
      textFont('Courier New');

      // 月を表示します。
      text(`現在の月: ${m}`, 10, 50, 80);

      describe(`灰色の背景に黒字で '現在の月: ${m}' と書かれたテキスト。`);
    }
    </code>
    </div>
class: p5
return:
  description: 1から12の間の現在の月。
  type: Integer
chainable: false
---


# month

