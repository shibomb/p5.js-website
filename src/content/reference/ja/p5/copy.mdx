---
title: copy
module: Image
submodule: Pixels
file: src/image/pixels.js
description: >
  <p>キャンバスの領域にソース画像からピクセルをコピーします。</p>

  <p>最初のパラメータ <code>srcImage</code> は、ブレンドする

  <a href="/reference/p5/p5.Image">p5.Image</a> オブジェクトです。ソース画像は

  キャンバス自体または

  <a href="/reference/p5/p5.Image">p5.Image</a> オブジェクトにすることができます。<code>copy()</code> は

  ソース領域が目的の領域と同じサイズでない場合、ピクセルをスケーリングします。</p>

  <p>次の4つのパラメータ <code>sx</code>、<code>sy</code>、<code>sw</code>、<code>sh</code> は

  ソース画像からコピーする領域を決定します。<code>(sx, sy)</code> は

  領域の左上隅です。<code>sw</code> と <code>sh</code> は領域の幅と高さです。</p>

  <p>次の4つのパラメータ <code>dx</code>、<code>dy</code>、<code>dw</code>、<code>dh</code> は

  キャンバス上のコピー先の領域を決定します。<code>(dx, dy)</code> は

  領域の左上隅です。<code>dw</code> と <code>dh</code> は領域の幅と高さです。</p>
line: 288
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // 画像を読み込みます。
    function preload() {
      img = loadImage('/assets/rockies.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // 山々を背景として使用します。
      background(img);

      // ピクセルの領域を別の場所にコピーします。
      copy(img, 7, 22, 10, 10, 35, 25, 50, 50);

      // コピーした領域の輪郭を描きます。
      stroke(255);
      noFill();
      square(7, 22, 10);

      describe('山の風景の画像。白い輪郭の正方形領域があります。より大きな正方形には、輪郭で囲まれた領域のピクセル化されたビューが含まれています。');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 288
    params:
      - name: srcImage
        description: |
          <p>ソース画像。</p>
        type: p5.Image|p5.Element
      - name: sx
        description: |
          <p>ソースの左上隅のx座標。</p>
        type: Integer
      - name: sy
        description: |
          <p>ソースの左上隅のy座標。</p>
        type: Integer
      - name: sw
        description: |
          <p>ソース画像の幅。</p>
        type: Integer
      - name: sh
        description: |
          <p>ソース画像の高さ。</p>
        type: Integer
      - name: dx
        description: |
          <p>目的地の左上隅のx座標。</p>
        type: Integer
      - name: dy
        description: |
          <p>目的地の左上隅のy座標。</p>
        type: Integer
      - name: dw
        description: |
          <p>目的地の画像の幅。</p>
        type: Integer
      - name: dh
        description: |
          <p>目的地の画像の高さ。</p>
        type: Integer
  - line: 345
    params:
      - name: sx
        description: ''
        type: Integer
      - name: sy
        description: ''
        type: Integer
      - name: sw
        description: ''
        type: Integer
      - name: sh
        description: ''
        type: Integer
      - name: dx
        description: ''
        type: Integer
      - name: dy
        description: ''
        type: Integer
      - name: dw
        description: ''
        type: Integer
      - name: dh
        description: ''
        type: Integer
chainable: false
---


# copy

