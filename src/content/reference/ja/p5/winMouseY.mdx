---
title: winMouseY
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p><code>Number</code>型の変数で、ブラウザ内のマウスの垂直位置を追跡します。</p>

  <p><code>winMouseY</code>は、ブラウザウィンドウの左上隅を基準としたマウスの位置を
  追跡します。例えば、マウスがブラウザの上端から50ピクセルの位置にある場合、
  <code>winMouseY</code>は50になります。</p>

  <p>タッチスクリーンデバイスでは、<code>winMouseY</code>は最新のタッチポイントの
  y座標を保持します。</p>

  <p>注意：キャンバス内のマウスのy座標を追跡するには、<a href="/reference/p5/mouseY/">mouseY</a>を
  使用してください。</p>
line: 510
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe("灰色の正方形。ユーザーがマウスを動かすと、マウスのx座標とy座標が表示されます。");
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // ブラウザウィンドウ内のマウスの座標を表示します。
      text(`x: ${winMouseX} y: ${winMouseY}`, 50, 50);
    }
    </code>
    </div>
class: p5
type: Number
---


# winMouseY

