---
title: frameCount
module: Environment
submodule: Environment
file: src/core/environment.js
description: >
  <p>スケッチが開始されてから描画されたフレーム数を追跡する<code>Number</code>変数です。</p>

  <p><code>frameCount</code>の値は<a href="/reference/p5/setup/">setup()</a>内では0です。
  <a href="/reference/p5/draw/">draw()</a>内のコードの実行が終わるたびに1ずつ増加します。</p>
line: 69
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // frameCountの値を表示します。
      textSize(30);
      textAlign(CENTER, CENTER);
      text(frameCount, 50, 50);

      describe('灰色の正方形の中央に黒で書かれた数字0。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // フレームレートを30に設定します。
      frameRate(30);

      textSize(30);
      textAlign(CENTER, CENTER);

      describe('灰色の正方形の中央に黒で書かれた数字。その値が急速に増加します。');
    }

    function draw() {
      background(200);

      // frameCountの値を表示します。
      text(frameCount, 50, 50);
    }
    </code>
    </div>
class: p5
type: Integer
---


# frameCount

