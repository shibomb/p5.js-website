---
title: deviceMoved
module: Events
submodule: Acceleration
file: src/events/acceleration.js
description: >
  <p><a href="/reference/p5/deviceMoved/">deviceMoved()</a>関数は、デバイスがX、Y、またはZ軸に沿って閾値以上に動かされたときに呼び出されます。
  デフォルトの閾値は0.5に設定されています。閾値は<a href="https://p5js.org/reference/#/p5/setMoveThreshold/">setMoveThreshold()</a>を使用して変更できます。</p>
line: 501
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    // このサンプルをモバイルデバイスで実行してください
    // デバイスを動かして
    // 値を変更します。

    let value = 0;
    function draw() {
      fill(value);
      rect(25, 25, 50, 50);
      describe(`キャンバスの中央に50x50の黒い四角形。
        モバイルでデバイスを動かすと白くなります`);
    }
    function deviceMoved() {
      value = value + 5;
      if (value > 255) {
        value = 0;
      }
    }
    </code>
    </div>
class: p5
chainable: false
---


# deviceMoved

