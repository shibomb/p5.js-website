---
title: httpGet
module: IO
submodule: Input
file: src/io/files.js
description: >
  <p>HTTP GETリクエストを実行するためのメソッドです。
  データ型が指定されていない場合、

  p5はURLに基づいて推測を試み、デフォルトではテキストになります。これは

  <code>httpDo(path, 'GET')</code>を呼び出すのと同等です。'binary'データ型は

  Blobオブジェクトを返し、'arrayBuffer'データ型は型付き配列（Uint8Arrayなど）の

  初期化に使用できるArrayBufferを返します。</p>
line: 1043
isConstructor: false
itemtype: method
example:
  - |-

    <div class='norender'><code>
    // 例ではUSGS地震APIを使用しています：
    //   https://earthquake.usgs.gov/fdsnws/event/1/#methods
    let earthquakes;
    function preload() {
      // データベース内の最新の地震を取得
      let url =
       'https://earthquake.usgs.gov/fdsnws/event/1/query?' +
        'format=geojson&limit=1&orderby=time';
      httpGet(url, 'jsonp', false, function(response) {
        // HTTPリクエストが完了したら、視覚化に使用される地震データを保持する変数に値を設定します。
        earthquakes = response;
      });
    }

    function draw() {
      if (!earthquakes) {
        // 地震データが読み込まれるまで描画を待機します。
        return;
      }
      background(200);
      // 読み込まれたJSONから地震のマグニチュードと名前を取得
      let earthquakeMag = earthquakes.features[0].properties.mag;
      let earthquakeName = earthquakes.features[0].properties.place;
      ellipse(width / 2, height / 2, earthquakeMag * 10, earthquakeMag * 10);
      textAlign(CENTER);
      text(earthquakeName, 0, height - 30, width, 30);
      noLoop();
    }
    </code></div>
class: p5
return:
  description: |-
    操作が正常に完了したときにデータで解決するか、
                      エラーが発生した後に拒否するPromise。
  type: Promise
overloads:
  - line: 1043
    params:
      - name: path
        description: |
          <p>読み込むファイルまたはURLの名前</p>
        type: String
      - name: datatype
        description: |
          <p>"json"、"jsonp"、"binary"、"arrayBuffer"、
                                             "xml"、または"text"</p>
        type: String
        optional: true
      - name: data
        description: |
          <p>リクエストと共に送信されるパラメータデータ</p>
        type: Object|Boolean
        optional: true
      - name: callback
        description: |
          <p><a href="#/p5/httpGet">httpGet()</a>が完了した後に実行される関数、
                                             データは最初の引数として渡されます</p>
        type: Function
        optional: true
      - name: errorCallback
        description: |
          <p>エラーがある場合に実行される関数、
                                             レスポンスは最初の引数として渡されます</p>
        type: Function
        optional: true
    return:
      description: |-
        操作が正常に完了したときにデータで解決するか、
                          エラーが発生した後に拒否するPromise。
      type: Promise
  - line: 1097
    params:
      - name: path
        description: ''
        type: String
      - name: data
        description: ''
        type: Object|Boolean
      - name: callback
        description: ''
        type: Function
        optional: true
      - name: errorCallback
        description: ''
        type: Function
        optional: true
    return:
      description: ''
      type: Promise
  - line: 1105
    params:
      - name: path
        description: ''
        type: String
      - name: callback
        description: ''
        type: Function
      - name: errorCallback
        description: ''
        type: Function
        optional: true
    return:
      description: ''
      type: Promise
chainable: false
---


# httpGet

