---
title: rotateX
module: Transform
submodule: Transform
file: src/core/transform.js
description: >
  <p>WebGLモードでx軸周りに座標系を回転させます。</p>

  <p>パラメータ<code>angle</code>は回転量です。例えば、
  <code>rotateX(1)</code>を呼び出すと、座標系をx軸周りに1ラジアン回転させます。
  <code>rotateX()</code>は現在の<a href="/reference/p5/angleMode/">angleMode()</a>
  を使用して角度値を解釈します。</p>

  <p>デフォルトでは、変換は累積されます。例えば、<code>rotateX(1)</code>を
  2回呼び出すことは、<code>rotateX(2)</code>を1回呼び出すのと同じ効果があります。
  <a href="/reference/p5/push/">push()</a>関数と<a href="/reference/p5/pop/">pop()</a>関数
  を使用して、個別の描画グループ内で変換を分離することができます。</p>

  <p>注意：変換はdrawループの開始時にリセットされます。<a href="/reference/p5/draw/">draw()</a>関数内で
  <code>rotateX(1)</code>を呼び出しても、図形は回転し続けません。</p>

line: 470
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に白い立方体。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // 座標系を1/8回転させます。
      rotateX(QUARTER_PI);

      // ボックスを描画します。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に白い立方体。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // 座標系を1/16回転させます。
      rotateX(QUARTER_PI / 2);

      // 座標系をさらに1/16回転させます。
      rotateX(QUARTER_PI / 2);

      // ボックスを描画します。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      // 度数法を使用します。
      angleMode(DEGREES);

      describe('灰色の背景に白い立方体。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // 座標系を1/8回転させます。
      rotateX(45);

      // ボックスを描画します。
      box();
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('灰色の背景に対してゆっくりと回転する白い立方体。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // フレームごとに座標系を少しずつ回転させます。
      let angle = frameCount * 0.01;
      rotateX(angle);

      // ボックスを描画します。
      box();
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: >
      <p>現在の<a href="#/p5/angleMode">angleMode()</a>での回転角度。</p>
    type: Number
chainable: true
---


# rotateX

