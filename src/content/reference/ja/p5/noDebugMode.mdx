---
title: noDebugMode
module: 3D
submodule: Interaction
file: src/webgl/interaction.js
description: >
  <p>3Dスケッチで<a href="/reference/p5/debugMode/">debugMode()</a>をオフにします。</p>
line: 725
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      // デバッグモードを有効にします。
      debugMode();

      describe('グレーの背景に多色のボックス。ボックスの近くにグリッドと軸のアイコンが表示されています。ユーザーがダブルクリックするとそれらは消えます。');
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // ボックスのスタイルを設定します。
      normalMaterial();

      // ボックスを描画します。
      box(20, 40);
    }

    // ユーザーがダブルクリックするとデバッグモードを無効にします。
    function doubleClicked() {
      noDebugMode();
    }
    </code>
    </div>
class: p5
chainable: false
---


# noDebugMode

