---
title: doubleClicked
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p>マウスボタンが素早く2回クリックされたときに1回呼び出される関数です。</p>

  <p><code>doubleClicked()</code>関数を宣言すると、ユーザーがマウスボタンを素早く2回押して離したときに

  自動的に実行されるコードブロックを設定します：</p>

  <pre><code class="language-js">function doubleClicked() {
    // 実行するコード
  }

  </code></pre>

  <p><a href="/reference/p5/mouseX/">mouseX</a>や

  <a href="/reference/p5/mouseY/">mouseY</a>などのマウスシステム変数は、

  p5.jsによって<code>doubleClicked()</code>が呼び出されたときに

  最新の値で更新されます：</p>

  <pre><code class="language-js">function doubleClicked() {
    if (mouseX < 50) {
      // マウスが左側にある場合に実行するコード
    }

    if (mouseY > 50) {
      // マウスが下部付近にある場合に実行するコード
    }
  }

  </code></pre>

  <p>パラメータ<code>event</code>はオプションです。
  <code>doubleClicked()</code>には常に

  ダブルクリックイベントを説明するプロパティを持つ

  <a href="https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/"
  target="_blank">MouseEvent</a>

  オブジェクトが渡されます：</p>

  <pre><code class="language-js">function doubleClicked(event) {
    // イベントを使用して実行するコード
    console.log(event);
  }

  </code></pre>

  <p>タッチスクリーンデバイスでは、<code>doubleClicked()</code>に配置されたコードは

  短時間内に2回のタッチが発生した後に実行されます。</p>

  <p>ブラウザは、様々なマウスイベントにデフォルトの動作を関連付けている場合があります。

  例えば、一部のブラウザでは、ユーザーがマウスボタンを押しながらマウスを動かすと

  テキストがハイライトされます。このイベントのデフォルトの動作を防ぐには、

  関数の最後に<code>return false;</code>を追加してください。</p>
line: 1585
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの正方形の中央に黒い正方形がある。ユーザーがダブルクリックすると内側の正方形の色が変わる。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定
      fill(value);

      // 正方形を描画
      square(25, 25, 50);
    }

    // ユーザーがダブルクリックしたときに正方形の色を切り替える
    function doubleClicked() {
      if (value === 0) {
        value = 255;
      } else {
        value = 0;
      }
      // デフォルトの動作を防ぐにはコメントを解除してください
      // return false;
    }
    </code>
    </div>

    <div>
    <code>
    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの正方形の中央に黒い円がある。ユーザーが円をダブルクリックすると、円の色が白に変わる。'
      );
    }

    function draw() {
      background(200);

      // 円のスタイルを設定
      fill(value);

      // 円を描画
      circle(50, 50, 80);
    }

    // ユーザーが円をダブルクリックしたときにvalueを255に再代入する
    function doubleClicked() {
      if (dist(50, 50, mouseX, mouseY) < 40) {
        value = 255;
      }
      // デフォルトの動作を防ぐにはコメントを解除してください
      // return false;
    }
    </code>
    </div>
class: p5
params:
  - name: event
    description: |
      <p>オプションの<code>MouseEvent</code>引数。</p>
    type: MouseEvent
    optional: true
chainable: false
---


# doubleClicked

