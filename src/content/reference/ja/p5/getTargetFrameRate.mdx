---
title: getTargetFrameRate
module: Environment
submodule: Environment
file: src/core/environment.js
description: |
  <p>目標フレームレートを返します。</p>
  <p>この値は、システムのフレームレートか、<a href="/reference/p5/frameRate/">frameRate()</a>に最後に渡された値のいずれかです。</p>
line: 447
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の背景に黒で書かれた数字20。');
    }

    function draw() {
      background(200);

      // フレームレートを20に設定する。
      frameRate(20);

      // 目標フレームレートを取得し、
      // 表示する。
      let fps = getTargetFrameRate();
      text(fps, 43, 54);
    }
    </code>
    </div>
class: p5
return:
  description: _targetFrameRate
  type: Number
chainable: false
---

# getTargetFrameRate

