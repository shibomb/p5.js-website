---
title: pmouseX
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p><code>Number</code>型のシステム変数で、マウスの前回の水平位置を追跡します。</p>

  <p>2Dモードでは、<code>pmouseX</code>はキャンバスの左上隅を基準にしたマウスの位置を
  追跡します。その値は前のフレームの<a href="/reference/p5/mouseX/">mouseX</a>です。
  例えば、前のフレームでマウスがキャンバスの左端から50ピクセルの位置にあった場合、
  <code>pmouseX</code>は50になります。</p>

  <p>WebGLモードでは、<code>pmouseX</code>はキャンバスの中心を基準にしたマウスの位置を
  追跡します。例えば、前のフレームでマウスがキャンバスの中心から右に50ピクセルの
  位置にあった場合、<code>pmouseX</code>は50になります。</p>

  <p>マウスの代わりにタッチが使用された場合、<code>pmouseX</code>は最後のタッチポイントの
  x座標を保持します。</p>

  <p>注意：<code>pmouseX</code>は各タッチイベントの開始時に現在の<a
  href="/reference/p5/mouseX/">mouseX</a>の値にリセットされます。</p>
line: 331
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // フレームレートを遅くする
      frameRate(10);

      describe('マウスが動くにつれて線が追従します。動きが速いほど線は長くなります。');
    }

    function draw() {
      background(200);

      line(pmouseX, pmouseY, mouseX, mouseY);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);

      describe('マウスが動くにつれて線が追従します。動きが速いほど線は長くなります。');
    }

    function draw() {
      background(200);

      // WebGLモードの座標を調整する
      // 原点(0, 0)はキャンバスの中心にある
      let pmx = pmouseX - 50;
      let pmy = pmouseY - 50;
      let mx = mouseX - 50;
      let my = mouseY - 50;

      // 線を描画する
      line(pmx, pmy, mx, my);
    }
    </code>
    </div>
class: p5
type: Number
---


# pmouseX

