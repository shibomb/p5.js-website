---
title: tan
module: Math
submodule: Trigonometry
file: src/math/trigonometry.js
description: >
  <p>角度の正接を計算します。</p>

  <p><code>tan()</code>はクリエイティブコーディングにおける多くの幾何学的タスクに有用です。
  返される値は-無限大から無限大の範囲で、
  入力角度が増加するにつれて周期的に繰り返されます。<code>tan()</code>は現在の
  <a href="/reference/p5/angleMode/">angleMode()</a>を考慮します。</p>
line: 441
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      describe('黒い点で描かれた一連の同一の曲線。各曲線はキャンバスの上部から始まり、わずかな角度で下に続き、キャンバスの中央で平坦になり、その後底部まで続きます。');
    }

    function draw() {
      // 座標を計算します。
      let x = frameCount;
      let y = 5 * tan(x * 0.1) + 50;

      // 点を描画します。
      point(x, y);
    }
    </code>
    </div>
class: p5
params:
  - name: angle
    description: |
      <p>角度。</p>
    type: Number
return:
  description: 角度の正接。
  type: Number
chainable: false
---


# tan

