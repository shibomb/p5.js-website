---
title: scale
module: Transform
submodule: Transform
file: src/core/transform.js
description: >
  <p>座標系を拡大縮小します。</p>

  <p>デフォルトでは、図形は元のスケールで描画されます。幅50ピクセルの長方形は、
  幅100ピクセルのキャンバスの半分の幅を占めるように見えます。
  <code>scale()</code>関数は座標系を縮小または拡大して、
  図形が異なるサイズで表示されるようにすることができます。<code>scale()</code>を
  呼び出す際には、スケール係数を設定するパラメータを使用する2つの方法があります。</p>

  <p>最初の方法は、数値を使用してスケーリングの量を設定します。
  最初のパラメータ<code>s</code>は、各軸のスケーリング量を設定します。例えば、
  <code>scale(2)</code>を呼び出すと、x軸、y軸、z軸が2倍に拡大されます。
  次の2つのパラメータ<code>y</code>と<code>z</code>はオプションです。これらは
  y軸とz軸のスケーリング量を設定します。例えば、<code>scale(2, 0.5, 1)</code>を
  呼び出すと、x軸が2倍に拡大され、y軸が0.5倍に縮小され、z軸は変更されません。</p>

  <p><code>scale()</code>を呼び出す2つ目の方法は、<a href="/reference/p5/p5.Vector">p5.Vector</a>
  オブジェクトを使用してスケール係数を設定します。例えば、<code>scale(myVector)</code>
  を呼び出すと、<code>myVector</code>のx、y、z成分を使用して、x軸、y軸、z軸の
  スケーリング量を設定します。これは<code>scale(myVector.x, myVector.y, myVector.z)</code>
  を呼び出すのと同じです。</p>

  <p>デフォルトでは、変換は累積されます。例えば、<code>scale(1)</code>を2回
  呼び出すことは、<code>scale(2)</code>を1回呼び出すのと同じ効果があります。
  <a href="/reference/p5/push/">push()</a>関数と<a href="/reference/p5/pop/">pop()</a>関数
  を使用して、変換を個別の描画グループ内に分離することができます。</p>

  <p>注意：変換はdrawループの開始時にリセットされます。<a href="/reference/p5/draw/">draw()</a>関数
  内で<code>scale(2)</code>を呼び出しても、図形が連続的に大きくなることはありません。</p>
line: 878
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe(
        '灰色の背景に2つの白い正方形。大きな正方形が上部中央に表示されます。小さな正方形が左上に表示されます。'
      );
    }

    function draw() {
      background(200);

      // (30, 20)に正方形を描画します。
      square(30, 20, 40);

      // 座標系を0.5倍にスケーリングします。
      scale(0.5);

      // (30, 20)に正方形を描画します。
      // スケーリング後は(15, 10)に表示されます。
      square(30, 20, 40);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の背景に白で描かれた長方形と正方形。');
    }

    function draw() {
      background(200);

      // (30, 20)に正方形を描画します。
      square(30, 20, 40);

      // 座標系を
      // x軸方向に0.5倍、
      // y軸方向に1.3倍にスケーリングします。
      scale(0.5, 1.3);

      // (30, 20)に正方形を描画します。
      // スケーリング後は長方形として(15, 26)に表示されます。
      square(30, 20, 40);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の背景に白で描かれた長方形と正方形。');
    }

    function draw() {
      background(200);

      // (30, 20)に正方形を描画します。
      square(30, 20, 40);

      // p5.Vectorオブジェクトを作成します。
      let v = createVector(0.5, 1.3);

      // 座標系を
      // x軸方向に0.5倍、
      // y軸方向に1.3倍にスケーリングします。
      scale(v);

      // (30, 20)に正方形を描画します。
      // スケーリング後は長方形として(15, 26)に表示されます。
      square(30, 20, 40);
    }
    </code>
    </div>

    <div>
    <code>
    // クリックしてドラッグすると、異なる角度からシーンを見ることができます。

    function setup() {
      createCanvas(100, 100, WEBGL);

      describe(
        '灰色の背景に描かれた赤い箱と青い箱。赤い箱が青い箱に埋め込まれているように見えます。'
      );
    }

    function draw() {
      background(200);

      // マウスでの軌道制御を有効にします。
      orbitControl();

      // ライトをオンにします。
      lights();

      // 球体のスタイルを設定します。
      noStroke();

      // 赤い球体を描画します。
      fill('red');
      box();

      // 座標系を
      // x軸方向に0.5倍、
      // y軸方向に1.3倍、
      // z軸方向に2倍にスケーリングします。
      scale(0.5, 1.3, 2);

      // 青い球体を描画します。
      fill('blue');
      box();
    }
    </code>
    </div>
class: p5
overloads:
  - line: 878
    params:
      - name: s
        description: |
          <p>x軸の正の方向にスケーリングする量。</p>
        type: 'Number|p5.Vector|Number[]'
      - name: 'y'
        description: >
          <p>y軸の正の方向にスケーリングする量。デフォルトは<code>s</code>です。</p>
        type: Number
        optional: true
      - name: z
        description: >
          <p>z軸の正の方向にスケーリングする量。デフォルトは<code>y</code>です。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 1039
    params:
      - name: scales
        description: |
          <p>スケーリングに使用する成分を持つベクトル。</p>
        type: 'p5.Vector|Number[]'
    chainable: 1
chainable: true
---


# scale

