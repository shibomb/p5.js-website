---
title: createP
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p><code><p></p></code>要素を作成します。</p>

  <p><code><p></p></code>要素は一般的に段落の長さのテキストに使用されます。</p>

  <p>パラメータ<code>html</code>はオプションです。新しい<code><p></p></code>の

  内部HTMLを設定する文字列を受け付けます。</p>
line: 543
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 段落要素を作成し、位置を設定します。
      let p = createP('物語を聞かせて。');
      p.position(5, 0);

      describe('黒字で「物語を聞かせて。」と書かれたグレーの正方形。');
    }
    </code>
    </div>
class: p5
params:
  - name: html
    description: |
      <p>新しい<code>&lt;p&gt;&lt;/p&gt;</code>要素の内部HTML。</p>
    type: String
    optional: true
return:
  description: 新しい<a href="#/p5.Element">p5.Element</a>オブジェクト。
  type: p5.Element
chainable: false
---


# createP

