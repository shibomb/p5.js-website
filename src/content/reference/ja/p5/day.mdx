---
title: day
module: IO
submodule: Time & Date
file: src/utilities/time_date.js
description: |
  <p>現在の日を1〜31の数字で返します。</p>
line: 10
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 現在の日を取得する
      let d = day();

      // テキストのスタイルを設定する
      textAlign(LEFT, CENTER);
      textSize(12);
      textFont('Courier New');

      // 日を表示する
      text(`現在の日: ${d}`, 20, 50, 60);

      describe(`灰色の背景に黒字で '現在の日: ${d}' と書かれたテキスト。`);
    }
    </code>
    </div>
class: p5
return:
  description: 1から31の間の現在の日。
  type: Integer
chainable: false
---


# day

