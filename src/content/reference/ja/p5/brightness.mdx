---
title: brightness
module: Color
submodule: Creating & Reading
file: src/color/creating_reading.js
description: >
  <p>色の明るさの値を取得します。</p>

  <p><code>brightness()</code>は<a href="/reference/p5/p5.Color">p5.Color</a>オブジェクト、色成分の配列、

  またはCSSの色文字列からHSBの明るさの値を抽出します。</p>

  <p>デフォルトでは、<code>brightness()</code>は色のHSBの明るさを0から100の範囲で返します。

  <a href="/reference/colorMode/">colorMode()</a>がHSBに設定されている場合、

  指定された範囲内で明るさの値を返します。</p>
line: 253
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // HSB色を使用します。
      colorMode(HSB);

      // p5.Colorオブジェクトを作成します。
      let c = color(0, 50, 100);

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'brightValue'を100に設定します。
      let brightValue = brightness(c);

      // 右の長方形を描画します。
      fill(brightValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側はサーモンピンクで、右側は白です。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // HSB色を使用します。
      colorMode(HSB);

      // 色の配列を作成します。
      let c = [0, 50, 100];

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'brightValue'を100に設定します。
      let brightValue = brightness(c);

      // 右の長方形を描画します。
      fill(brightValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側はサーモンピンクで、右側は白です。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // HSB色を使用します。
      colorMode(HSB);

      // CSS色文字列を作成します。
      let c = 'rgb(255, 128, 128)';

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'brightValue'を100に設定します。
      let brightValue = brightness(c);

      // 右の長方形を描画します。
      fill(brightValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側はサーモンピンクで、右側は白です。');
    }
    </code>
    </div>
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 0-255の範囲の値でHSB色を使用します。
      colorMode(HSB, 255);

      // p5.Colorオブジェクトを作成します。
      let c = color(0, 127, 255);

      // 左の長方形を描画します。
      noStroke();
      fill(c);
      rect(15, 15, 35, 70);

      // 'brightValue'を255に設定します。
      let brightValue = brightness(c);

      // 右の長方形を描画します。
      fill(brightValue);
      rect(50, 15, 35, 70);

      describe('2つの長方形。左側はサーモンピンクで、右側は白です。');
    }
    </code>
    </div>
class: p5
params:
  - name: color
    description: |
      <p><a href="#/p5.Color">p5.Color</a>オブジェクト、色成分の配列、
                                              またはCSS色文字列。</p>
    type: 'p5.Color|Number[]|String'
return:
  description: 明るさの値。
  type: Number
chainable: false
---


# brightness
