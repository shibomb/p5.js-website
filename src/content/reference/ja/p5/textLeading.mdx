---
title: textLeading
module: Typography
submodule: Attributes
file: src/typography/attributes.js
description: |
  <p>テキストの行間を設定します。
  この値は、ベースラインからベースラインまでの
  距離をピクセル単位で測定します。</p>
line: 453
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // テキスト
    let lines = 'L1\nL2\nL3';

    textSize(12);

    // 行間を10ピクセルに設定
    textLeading(10);
    text(lines, 10, 25);

    // 行間を20ピクセルに設定
    textLeading(20);
    text(lines, 50, 25);

    // 行間を30ピクセルに設定
    textLeading(30);
    text(lines, 90, 25);

    describe('3つの列に"L1 L2 L3"と表示されています。左の列は行間が狭く、中央の列は中程度、右の列は広くなっています。');
    </code>
    </div>
class: p5
params:
  - name: leading
    description: |
      <p>ベースラインからベースラインまでの距離（ピクセル単位）</p>
    type: Number
chainable: true
---

# textLeading