---
title: mouseDragged
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p>マウスのボタンが押されている間にマウスが移動したときに呼び出される関数です。</p>

  <p><code>mouseDragged()</code>関数を宣言すると、ユーザーがマウスをクリックしてドラッグしたときに
  自動的に実行されるコードブロックを設定できます：</p>

  <pre><code class="language-js">function mouseDragged() {
    // 実行するコード
  }

  </code></pre>

  <p><a href="/reference/p5/mouseX/">mouseX</a>や
  <a href="/reference/p5/mouseY/">mouseY</a>などのマウスシステム変数は、
  p5.jsによって<code>mouseDragged()</code>が呼び出されたときに
  最新の値に更新されます：</p>

  <pre><code class="language-js">function mouseDragged() {
    if (mouseX < 50) {
      // マウスが左側にある場合に実行するコード
    }

    if (mouseY > 50) {
      // マウスが下部付近にある場合に実行するコード
    }
  }

  </code></pre>

  <p>パラメータ<code>event</code>はオプションです。<code>mouseDragged()</code>には常に
  マウスドラッグイベントを説明するプロパティを持つ
  <a href="https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/"
  target="_blank">MouseEvent</a>
  オブジェクトが渡されます：</p>

  <pre><code class="language-js">function mouseDragged(event) {
    // イベントを使用して実行するコード
    console.log(event);
  }

  </code></pre>

  <p>タッチスクリーンデバイスでは、<a href="/reference/p5/touchMoved/">touchMoved()</a>が
  宣言されていない場合、ユーザーがタッチポイントを移動させると<code>mouseDragged()</code>が
  実行されます。<a href="/reference/p5/touchMoved/">touchMoved()</a>が宣言されている場合、
  ユーザーがタッチポイントを移動させると<a href="/reference/p5/touchMoved/">touchMoved()</a>が
  実行され、<code>mouseDragged()</code>は実行されません。</p>

  <p>ブラウザは様々なマウスイベントにデフォルトの動作を関連付けている場合があります。
  例えば、一部のブラウザでは、ユーザーがマウスボタンを押しながらマウスを移動させると
  テキストがハイライトされます。このイベントのデフォルトの動作を防ぐには、
  関数の最後に<code>return false;</code>を追加してください。</p>
line: 968
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let value = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの正方形の中央に黒い正方形がある。ユーザーがマウスをドラッグすると、内側の正方形が明るくなる。'
      );
    }

    function draw() {
      background(200);

      // 正方形のスタイルを設定
      fill(value);

      // 正方形を描画
      square(25, 25, 50);
    }

    function mouseDragged() {
      // グレースケール値を更新
      value += 5;

      // グレースケール値をリセット
      if (value > 255) {
        value = 0;
      }
      // デフォルトの動作を防ぐにはコメントを解除
      // return false;
    }
    </code>
    </div>
class: p5
params:
  - name: event
    description: |
      <p>オプションの<code>MouseEvent</code>引数。</p>
    type: MouseEvent
    optional: true
chainable: false
---


# mouseDragged

