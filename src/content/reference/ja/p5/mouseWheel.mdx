---
title: mouseWheel
module: Events
submodule: Mouse
file: src/events/mouse.js
description: >
  <p>マウスホイールが動いたときに一度呼び出される関数です。</p>

  <p><code>mouseWheel()</code>関数を宣言すると、ユーザーがマウスホイールでスクロールしたときに
  
  自動的に実行されるコードブロックを設定できます：</p>

  <pre><code class="language-js">function mouseWheel() {
    // 実行するコード
  }

  </code></pre>

  <p><a href="/reference/p5/mouseX/">mouseX</a>や

  <a href="/reference/p5/mouseY/">mouseY</a>などのマウスシステム変数は、

  p5.jsによって<code>mouseWheel()</code>が呼び出されたときに、

  最新の値で更新されます：</p>

  <pre><code class="language-js">function mouseWheel() {
    if (mouseX < 50) {
      // マウスが左側にある場合に実行するコード
    }

    if (mouseY > 50) {
      // マウスが下部付近にある場合に実行するコード
    }
  }

  </code></pre>

  <p>パラメータ<code>event</code>はオプションです。<code>mouseWheel()</code>には常に

  マウススクロールイベントを説明するプロパティを持つ

  <a href="https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/"
  target="_blank">MouseEvent</a>

  オブジェクトが渡されます：</p>

  <pre><code class="language-js">function mouseWheel(event) {
    // イベントを使用して実行するコード
    console.log(event);
  }

  </code></pre>

  <p><code>event</code>オブジェクトには多くのプロパティがあり、その中には
  <code>delta</code>という、ユーザーが

  スクロールした距離を含む<code>Number</code>があります。例えば、ユーザーが上にスクロールすると、

  <code>event.delta</code>の値は5になるかもしれません。<code>event.delta</code>は

  ユーザーが上にスクロールすると正の値、下にスクロールすると負の値になります。

  macOSで「ナチュラル」スクロールが有効になっている場合、符号は逆になります。</p>

  <p>ブラウザは様々なマウスイベントにデフォルトの動作を設定している場合があります。

  例えば、一部のブラウザでは、ユーザーがマウスボタンを押しながらマウスを動かすと

  テキストがハイライトされます。このイベントのデフォルトの動作を防ぐには、

  関数の最後に<code>return false;</code>を追加してください。</p>

  <p>注意：Safariでは、<code>mouseWheel()</code>が期待通りに動作するためには、

  関数の最後に<code>return false;</code>を追加する必要がある場合があります。</p>
line: 1732
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let circleSize = 0;

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの正方形。中央の白い円は、ユーザーがマウスホイールをスクロールすると大きくなります。'
      );
    }

    function draw() {
      background(200);

      // 円を描画
      circle(circleSize, 50, 50);
    }

    // ユーザーがマウスホイールをスクロールしたときにcircleSizeを増加させる
    function mouseWheel() {
      circleSize += 1;
      // デフォルトの動作を防ぐにはコメントを解除してください
      // return false;
    }
    </code>
    </div>

    <div>
    <code>
    let direction = '';

    function setup() {
      createCanvas(100, 100);

      describe(
        'グレーの正方形。中央の矢印は、ユーザーが上にスクロールすると上を指し、下にスクロールすると下を指します。'
      );
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定
      textAlign(CENTER);
      textSize(16);

      // マウスが最後にスクロールした方向を
      // 指す矢印を描画
      text(direction, 50, 50);
    }

    // ユーザーがマウスホイールをスクロールしたときに方向を変更
    function mouseWheel(event) {
      if (event.delta > 0) {
        direction = '▲';
      } else {
        direction = '▼';
      }
      // デフォルトの動作を防ぐにはコメントを解除してください
      // return false;
    }
    </code>
    </div>
class: p5
params:
  - name: event
    description: |
      <p>オプションの<code>WheelEvent</code>引数。</p>
    type: WheelEvent
    optional: true
chainable: false
---


# mouseWheel

