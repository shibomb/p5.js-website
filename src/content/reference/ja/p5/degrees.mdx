---
title: degrees
module: Math
submodule: Trigonometry
file: src/math/trigonometry.js
description: |
  <p>ラジアンで測定された角度を度数法の値に変換します。</p>
  <p>度数法とラジアンは両方とも角度を測定する単位です。1回転は360°です。
  1回転は2 × π（約6.28）ラジアンです。</p>
  <p>同じ角度はどちらの単位でも表現できます。例えば、90°は
  1回転の4分の1です。同じ角度は2 × π ÷ 4
  （約1.57）ラジアンです。</p>
line: 479
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 角度の変換を計算する
      let rad = QUARTER_PI;
      let deg = degrees(rad);

      // 変換結果を表示する
      text(`${round(rad, 2)} rad = ${deg}˚`, 10, 50);

      describe('テキスト "0.79 rad = 45˚".');
    }
    </code>
    </div>
class: p5
params:
  - name: radians
    description: |
      <p>度数法に変換するラジアン値。</p>
    type: Number
return:
  description: 変換された角度。
  type: Number
chainable: false
---

# degrees