---
title: keyIsPressed
module: Events
submodule: Keyboard
file: src/events/keyboard.js
description: >
  <p><code>Boolean</code>型のシステム変数で、何かキーが現在押されている場合は<code>true</code>、

  そうでない場合は<code>false</code>となります。</p>
line: 10
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に白い四角形がある灰色の四角形。ユーザーがキーを押すと白い四角形が黒くなります。'
      );
    }

    function draw() {
      background(200);

      // 四角形のスタイルを設定します。
      if (keyIsPressed === true) {
        fill(0);
      } else {
        fill(255);
      }

      // 四角形を描画します。
      square(25, 25, 50);
    }
    </code>
    </div>

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に白い四角形がある灰色の四角形。ユーザーがキーを押すと白い四角形が黒くなります。'
      );
    }

    function draw() {
      background(200);

      // 四角形のスタイルを設定します。
      if (keyIsPressed) {
        fill(0);
      } else {
        fill(255);
      }

      // 四角形を描画します。
      square(25, 25, 50);
    }
    </code>
    </div>

    <div>
    <code>
    // キャンバスをクリックしてキー押下の検出を開始します。

    function setup() {
      createCanvas(100, 100);

      describe(
        '中央に"false"という単語がある灰色の四角形。ユーザーがキーを押すと単語が"true"に切り替わります。'
      );
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // keyIsPressedの値を表示します。
      text(keyIsPressed, 50, 50);
    }
    </code>
    </div>
class: p5
type: Boolean
---


# keyIsPressed

