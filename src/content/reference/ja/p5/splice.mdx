---
title: splice
module: Data
submodule: Array Functions
file: src/utilities/array_functions.js
description: |
  <p>既存の配列に値または値の配列を挿入します。
  最初の
  パラメータは変更される元の配列を指定し、2番目の
  パラメータは挿入されるデータを定義します。3番目のパラメータはインデックス
  値で、データを挿入する配列の位置を指定します。
  （配列のインデックス番号は0から始まることを覚えておいてください。つまり、最初の位置は
  0、2番目の位置は1、というようになります。）</p>
line: 346
isConstructor: false
itemtype: method
example:
  - |-

    <div class = 'norender'><code>
    function setup() {
      let myArray = [0, 1, 2, 3, 4];
      let insArray = ['A', 'B', 'C'];
      print(myArray); // [0, 1, 2, 3, 4]
      print(insArray); // ['A','B','C']

      splice(myArray, insArray, 3);
      print(myArray); // [0,1,2,'A','B','C',3,4]
    }
    </code></div>
class: p5
params:
  - name: list
    description: |
      <p>挿入先の配列</p>
    type: Array
  - name: value
    description: |
      <p>挿入される値</p>
    type: Any
  - name: position
    description: |
      <p>データを挿入する配列内の位置</p>
    type: Integer
return:
  description: リスト
  type: Array
chainable: false
---

# splice