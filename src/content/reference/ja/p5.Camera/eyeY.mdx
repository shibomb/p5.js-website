---
title: eyeY
module: 3D
submodule: Camera
file: src/webgl/p5.Camera.js
description: |
  <p>カメラのy座標。</p>
  <p>デフォルトでは、カメラのy座標は「ワールド」空間で0に設定されています。</p>
line: 962
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを上部中央に配置します。
      cam.setPosition(0, -400, 800);

      // カメラを原点に向けます。
      cam.lookAt(0, 0, 0);

      describe(
        'グレーの背景に白い立方体。その下に黒字で "eyeY: -400" と書かれています。'
      );
    }

    function draw() {
      background(200);

      // ボックスのスタイルを設定します。
      fill(255);

      // ボックスを描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // eyeYの値を最も近い整数に丸めて表示します。
      text(`eyeX: ${round(cam.eyeY)}`, 0, 55);
    }
    </code>
    </div>

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを上部中央に配置します。
      cam.setPosition(0, -400, 800);

      // カメラを原点に向けます。
      cam.lookAt(0, 0, 0);

      describe(
        'グレーの背景に白い立方体。カメラが動くにつれて立方体が上下に動くように見えます。立方体の下に黒字で "eyeY: Y" と書かれています。Yは-374から-425の間で振動します。'
      );
    }

    function draw() {
      background(200);

      // ボックスのスタイルを設定します。
      fill(255);

      // ボックスを描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // 新しいy座標を計算します。
      let y = 25 * sin(frameCount * 0.01) - 400;

      // カメラの位置を設定します。
      cam.setPosition(0, y, 800);

      // eyeYの値を最も近い整数に丸めて表示します。
      text(`eyeY: ${round(cam.eyeY)}`, 0, 55);
    }
    </code>
    </div>
class: p5.Camera
type: Number
---


# eyeY

