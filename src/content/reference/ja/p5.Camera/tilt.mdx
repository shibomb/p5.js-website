---
title: tilt
module: 3D
submodule: Camera
file: src/webgl/p5.Camera.js
description: >
  <p>カメラを上下に回転させます。</p>

  <p>傾斜はカメラの位置を変えずに回転させます。回転は

  カメラの「ローカル」空間で行われます。</p>

  <p>パラメータ<code>angle</code>は、カメラが回転すべき角度です。
  正の角度を渡すと、例えば<code>myCamera.tilt(0.001)</code>のように、カメラは下に

  回転します。負の角度を渡すと、例えば<code>myCamera.tilt(-0.001)</code>のように、カメラは

  上に回転します。</p>

  <p>注意：角度は現在の<a href="/reference/p5/angleMode/">angleMode()</a>に基づいて

  解釈されます。</p>
line: 2604
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let cam;
    let delta = 0.001;

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを上部中央に配置します。
      cam.setPosition(0, -400, 800);

      // カメラを原点に向けます。
      cam.lookAt(0, 0, 0);

      describe(
        'グレーの背景に白い立方体。カメラが上下に傾くにつれて、立方体が視界に入ったり出たりします。'
      );
    }

    function draw() {
      background(200);

      // カメラで傾斜します。
      cam.tilt(delta);

      // 120フレームごとに方向を切り替えます。
      if (frameCount % 120 === 0) {
        delta *= -1;
      }

      // 箱を描画します。
      box();
    }
    </code>
    </div>
class: p5.Camera
params:
  - name: angle
    description: |
      <p>現在の<a href="#/p5/angleMode">angleMode()</a>での
                            回転量。</p>
    type: Number
chainable: false
---


# tilt

