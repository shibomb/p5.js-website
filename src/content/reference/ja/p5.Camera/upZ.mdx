---
title: upZ
module: 3D
submodule: Camera
file: src/webgl/p5.Camera.js
description: |
  <p>カメラの「上」ベクトルのz成分。</p>
  <p>カメラの「上」ベクトルはそのy軸の向きを決定します。デフォルトでは、「上」ベクトルは
  <code>(0, 1, 0)</code>なので、「ローカル」空間ではz成分は0です。</p>
line: 1747
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを右上に配置: (100, -400, 800)
      // 原点を向くように設定: (0, 0, 0)
      // 「上」ベクトルを設定: (0, 1, 0)
      cam.camera(100, -400, 800, 0, 0, 0, 0, 1, 0);

      describe(
        'グレーの背景に白い立方体。その下に黒字で "upZ: 0" と書かれています。'
      );
    }

    function draw() {
      background(200);

      // 立方体のスタイルを設定します。
      fill(255);

      // 立方体を描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // upZの値を小数点第1位まで四捨五入して表示します。
      text(`upZ: ${round(cam.upZ, 1)}`, 0, 55);
    }
    </code>
    </div>

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを右上に配置: (100, -400, 800)
      // 原点を向くように設定: (0, 0, 0)
      // 「上」ベクトルを設定: (0, 1, 0)
      cam.camera(100, -400, 800, 0, 0, 0, 0, 1, 0);

      describe(
        'グレーの背景に白い立方体。立方体が前後に揺れているように見えます。その下に黒字で "upZ: Z" と書かれています。Zは-1から1の間で振動します。'
      );
    }

    function draw() {
      background(200);

      // 立方体のスタイルを設定します。
      fill(255);

      // 立方体を描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // z成分を計算します。
      let z = sin(frameCount * 0.01);

      // カメラの「上」ベクトルを更新します。
      cam.camera(100, -400, 800, 0, 0, 0, 0, 1, z);

      // upZの値を小数点第1位まで四捨五入して表示します。
      text(`upZ: ${round(cam.upZ, 1)}`, 0, 55);
    }
    </code>
    </div>
class: p5.Camera
type: Number
---


# upZ

