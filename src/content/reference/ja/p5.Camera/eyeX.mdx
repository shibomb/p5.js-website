---
title: eyeX
module: 3D
submodule: Camera
file: src/webgl/p5.Camera.js
description: |
  <p>カメラのx座標。</p>
  <p>デフォルトでは、カメラのx座標は「ワールド」空間で0に設定されています。</p>
line: 850
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを上部中央に配置します。
      cam.setPosition(0, -400, 800);

      // カメラを原点に向けます。
      cam.lookAt(0, 0, 0);

      describe(
        'グレーの背景に白い立方体。その下に黒字で "eyeX: 0" と書かれています。'
      );
    }

    function draw() {
      background(200);

      // ボックスのスタイルを設定します。
      fill(255);

      // ボックスを描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // eyeXの値を最も近い整数に丸めて表示します。
      text(`eyeX: ${round(cam.eyeX)}`, 0, 55);
    }
    </code>
    </div>

    <div>
    <code>
    let cam;
    let font;

    // フォントを読み込み、p5.Fontオブジェクトを作成します。
    function preload() {
      font = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100, WEBGL);

      // p5.Cameraオブジェクトを作成します。
      cam = createCamera();

      // カメラを上部中央に配置します。
      cam.setPosition(0, -400, 800);

      // カメラを原点に向けます。
      cam.lookAt(0, 0, 0);

      describe(
        'グレーの背景に白い立方体。カメラが動くにつれて立方体が左右に動くように見えます。立方体の下に黒字で "eyeX: X" と書かれています。Xは-25から25の間で振動します。'
      );
    }

    function draw() {
      background(200);

      // ボックスのスタイルを設定します。
      fill(255);

      // ボックスを描画します。
      box();

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);
      textFont(font);
      fill(0);

      // 新しいx座標を計算します。
      let x = 25 * sin(frameCount * 0.01);

      // カメラの位置を設定します。
      cam.setPosition(x, -400, 800);

      // eyeXの値を最も近い整数に丸めて表示します。
      text(`eyeX: ${round(cam.eyeX)}`, 0, 55);
    }
    </code>
    </div>
class: p5.Camera
type: Number
---


# eyeX

