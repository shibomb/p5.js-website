---
title: getNum
module: IO
submodule: Table
file: src/io/p5.TableRow.js
description: |
  <p>TableRowの指定された列からFloat値を取得します。
  列はIDまたはタイトルで指定することができます。</p>
line: 231
isConstructor: false
itemtype: method
example:
  - |2-

     <div class="norender"><code>
     // プロジェクトの "assets" フォルダにある "mammals.csv" ファイルが与えられた場合：
     //
     // id,species,name
     // 0,Capra hircus,Goat
     // 1,Panthera pardus,Leopard
     // 2,Equus zebra,Zebra

     let table;

     function preload() {
     //私のテーブルはカンマ区切り値 "csv" で
     //列ラベルを指定するヘッダーがあります
     table = loadTable('/assets/mammals.csv', 'csv', 'header');
     }

     function setup() {
     let rows = table.getRows();
     let minId = Infinity;
     let maxId = -Infinity;
     for (let r = 0; r < rows.length; r++) {
       let id = rows[r].getNum('id');
       minId = min(minId, id);
       maxId = min(maxId, id);
     }
     print('最小id = ' + minId + '、最大id = ' + maxId);
     describe('画像は表示されません');
     }
     </code></div>
class: p5.TableRow
params:
  - name: column
    description: |
      <p>columnName（文字列）または
                                       ID（数値）</p>
    type: String|Integer
return:
  description: Float 浮動小数点数
  type: Number
chainable: false
---


# getNum

