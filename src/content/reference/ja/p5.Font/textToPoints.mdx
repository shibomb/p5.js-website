---
title: textToPoints
module: Typography
submodule: Loading & Displaying
file: src/typography/p5.Font.js
description: >
  <p>テキスト文字列のアウトラインを点の配列として返します。</p>

  <p>各点は、<code>x</code>座標、<code>y</code>座標、および文字のインデックスを含むオブジェクトです。</p>

  <p>返される点の数は、<code>sampleFactor</code>パラメータによって制御されます。
  値が小さいほど、より多くの点が返されます。</p>

  <p>オプションのオブジェクトパラメータには、以下のプロパティを含めることができます：</p>

  <ul>

  <li><code>sampleFactor</code>：カーブのサンプリング密度を設定します。デフォルトは0.1です。</li>

  <li><code>simplifyThreshold</code>：単純化のしきい値（0-1）を設定します。デフォルトは0です。</li>

  </ul>

  <p>テキストのアウトラインを描画したり、テキストに基づいてアニメーションを作成したりするのに便利です。</p>
line: 261
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myFont;
    let points;

    function preload() {
      myFont = loadFont('/assets/inconsolata.otf');
    }

    function setup() {
      createCanvas(100, 100);
      background(200);

      points = myFont.textToPoints('p5', 10, 60, 50, {
        sampleFactor: 0.1,
        simplifyThreshold: 0,
      });

      noFill();
      stroke(0);
      beginShape();
      for (let i = 0; i < points.length; i++) {
        let p = points[i];
        vertex(p.x, p.y);
      }
      endShape();

      describe('灰色の背景に「p5」という文字のアウトラインが黒い点で描かれています。');
    }
    </code>
    </div>
class: p5.Font
params:
  - name: str
    description: |
      <p>点に変換するテキスト文字列。</p>
    type: String
  - name: x
    description: |
      <p>テキストのx位置。</p>
    type: Number
  - name: 'y'
    description: |
      <p>テキストのy位置。</p>
    type: Number
  - name: fontSize
    description: |
      <p>テキストのフォントサイズ。</p>
    type: Number
  - name: options
    description: |
      <p>追加のオプションを含むオブジェクト。</p>
    type: Object
    optional: true
return:
  description: テキストのアウトラインを表す点の配列。
  type: Array
chainable: false
---

# textToPoints