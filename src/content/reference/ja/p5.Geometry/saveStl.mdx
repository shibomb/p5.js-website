---
title: saveStl
module: Shape
submodule: 3D Primitives
file: src/webgl/p5.Geometry.js
description: >
  <p><code>saveStl()</code>関数は<code>p5.Geometry</code>オブジェクトを
  STLステレオリソグラフィファイル形式で3Dモデルとしてエクスポートします。
  これにより、p5.jsで作成した3D形状を他のソフトウェアで
  レンダリング、アニメーション、3Dプリント、その他の用途に使用できます。</p>

  <p>エクスポートされた.stlファイルには、<code>p5.Geometry</code>の面、頂点、法線が含まれます。</p>

  <p>デフォルトでは、このメソッドはテキストベースの.stlファイルを保存します。
  あるいは、第2引数として<code>{ binary: true }</code>を渡すことで、
  よりコンパクトだが人間が読みにくいバイナリ.stlファイルを保存することもできます。</p>
line: 1028
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let myModel;
    let saveBtn1;
    let saveBtn2;
    function setup() {
      createCanvas(200, 200, WEBGL);
      myModel = buildGeometry(() => {
        for (let i = 0; i < 5; i++) {
          push();
          translate(
            random(-75, 75),
            random(-75, 75),
            random(-75, 75)
          );
          sphere(random(5, 50));
          pop();
        }
      });

      saveBtn1 = createButton('Save .stl');
      saveBtn1.mousePressed(function() {
        myModel.saveStl();
      });
      saveBtn2 = createButton('Save binary .stl');
      saveBtn2.mousePressed(function() {
        myModel.saveStl('model.stl', { binary: true });
      });

      describe('空間で回転する数個の球体');
    }

    function draw() {
      background(0);
      noStroke();
      lights();
      rotateX(millis() * 0.001);
      rotateY(millis() * 0.002);
      model(myModel);
    }
    </code>
    </div>
class: p5.Geometry
params:
  - name: fileName
    description: |
      <p>モデルを保存するファイル名。
                                             指定されない場合、デフォルトのファイル名は'model.stl'になります。</p>
    type: String
    optional: true
    optdefault: '''model.stl'''
  - name: options
    description: >
      <p>オプション設定。オプションにはブール値の<code>binary</code>プロパティを含めることができ、
      これはバイナリ.stlファイルを保存するかどうかを制御します。デフォルトはfalseです。</p>
    type: Object
    optional: true
chainable: false
---


# saveStl

