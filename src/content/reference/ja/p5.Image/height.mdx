---
title: height
module: Image
submodule: Image
file: src/image/p5.Image.js
description: |
  <p>画像の高さをピクセル単位で表します。</p>
line: 151
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    let img;

    // 画像を読み込みます。
    function preload() {
      img = loadImage('/assets/rockies.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // 画像を表示します。
      image(img, 0, 0);

      // 中心座標を計算します。
      let x = img.width / 2;
      let y = img.height / 2;

      // 画像の中心に円を描きます。
      circle(x, y, 20);

      describe('山の風景の画像に、中央に白い円が描かれています。');
    }
    </code>
    </div>
class: p5.Image
type: '{Number}'
---

# height