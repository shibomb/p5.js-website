---
title: numFrames
module: Image
submodule: Image
file: src/image/p5.Image.js
description: |
  <p>アニメーションGIFの総フレーム数を返します。</p>
  <p>静止画の場合は1を返します。</p>
line: 1639
itemtype: property
example:
  - |-

    <div>
    <code>
    let gif;

    // アニメーションGIFを読み込みます。
    function preload() {
      gif = loadImage('/assets/arnott-wallace-wink-loop-once.gif');
    }

    function setup() {
      createCanvas(100, 100);
      background(200);

      // GIFを表示します。
      image(gif, 0, 0);

      // フレーム数を表示します。
      let frames = gif.numFrames();
      textAlign(CENTER);
      textSize(16);
      text(frames + ' frames', 50, 90);

      describe('アニメーションGIF。下部に "10 frames" というテキストが表示されています。');
    }
    </code>
    </div>
class: p5.Image
type: '{Integer}'
---

# numFrames