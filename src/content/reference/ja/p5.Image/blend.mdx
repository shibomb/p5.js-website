---
title: blend
module: Image
submodule: Image
file: src/image/p5.Image.js
description: >
  <p>別の画像からピクセルの領域をこの画像にコピーします。</p>

  <p>最初のパラメータ <code>srcImage</code> は、ブレンドする

  <a href="/reference/p5/p5.Image">p5.Image</a> オブジェクトです。</p>

  <p>次の4つのパラメータ <code>sx</code>、<code>sy</code>、<code>sw</code>、<code>sh</code> は、

  ソース画像からブレンドする領域を決定します。<code>(sx, sy)</code> は領域の

  左上隅です。<code>sw</code> と <code>sh</code> は領域の幅と高さです。</p>

  <p>次の4つのパラメータ <code>dx</code>、<code>dy</code>、<code>dw</code>、<code>dh</code> は、

  ブレンド先のキャンバスの領域を決定します。<code>(dx, dy)</code> は領域の

  左上隅です。<code>dw</code> と <code>dh</code> は領域の幅と高さです。</p>

  <p>10番目のパラメータ <code>blendMode</code> は、画像の色をブレンドするために使用するエフェクトを

  設定します。オプションには <code>BLEND</code>、<code>DARKEST</code>、<code>LIGHTEST</code>、<code>DIFFERENCE</code>、

  <code>MULTIPLY</code>、<code>EXCLUSION</code>、<code>SCREEN</code>、<code>REPLACE</code>、<code>OVERLAY</code>、<code>HARD_LIGHT</code>、

  <code>SOFT_LIGHT</code>、<code>DODGE</code>、<code>BURN</code>、<code>ADD</code>、または <code>NORMAL</code> があります。</p>
line: 1430
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let mountains;
    let bricks;

    // 画像を読み込みます。
    function preload() {
      mountains = loadImage('/assets/rockies.jpg');
      bricks = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // レンガの画像を山の画像にブレンドします。
      mountains.blend(bricks, 0, 0, 33, 100, 67, 0, 33, 100, ADD);

      // 山の画像を表示します。
      image(mountains, 0, 0);

      // レンガの画像を表示します。
      image(bricks, 0, 0);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側に薄く表示されています。');
    }
    </code>
    </div>

    <div>
    <code>
    let mountains;
    let bricks;

    // 画像を読み込みます。
    function preload() {
      mountains = loadImage('/assets/rockies.jpg');
      bricks = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // レンガの画像を山の画像にブレンドします。
      mountains.blend(bricks, 0, 0, 33, 100, 67, 0, 33, 100, DARKEST);

      // 山の画像を表示します。
      image(mountains, 0, 0);

      // レンガの画像を表示します。
      image(bricks, 0, 0);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側に透明に表示されています。');
    }
    </code>
    </div>

    <div>
    <code>
    let mountains;
    let bricks;

    // 画像を読み込みます。
    function preload() {
      mountains = loadImage('/assets/rockies.jpg');
      bricks = loadImage('/assets/bricks_third.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      // レンガの画像を山の画像にブレンドします。
      mountains.blend(bricks, 0, 0, 33, 100, 67, 0, 33, 100, LIGHTEST);

      // 山の画像を表示します。
      image(mountains, 0, 0);

      // レンガの画像を表示します。
      image(bricks, 0, 0);

      describe('山の風景の前にレンガの壁があります。同じレンガの壁が画像の右側に色あせて表示されています。');
    }
    </code>
    </div>
class: p5.Image
overloads:
  - line: 1430
    params:
      - name: srcImage
        description: |
          <p>ソース画像</p>
        type: p5.Image
      - name: sx
        description: |
          <p>ソースの左上隅のx座標。</p>
        type: Integer
      - name: sy
        description: |
          <p>ソースの左上隅のy座標。</p>
        type: Integer
      - name: sw
        description: |
          <p>ソース画像の幅。</p>
        type: Integer
      - name: sh
        description: |
          <p>ソース画像の高さ。</p>
        type: Integer
      - name: dx
        description: |
          <p>目的地の左上隅のx座標。</p>
        type: Integer
      - name: dy
        description: |
          <p>目的地の左上隅のy座標。</p>
        type: Integer
      - name: dw
        description: |
          <p>目的地の画像の幅。</p>
        type: Integer
      - name: dh
        description: |
          <p>目的地の画像の高さ。</p>
        type: Integer
      - name: blendMode
        description: >
          <p>ブレンドモード。BLEND、DARKEST、LIGHTEST、DIFFERENCE、
              MULTIPLY、EXCLUSION、SCREEN、REPLACE、OVERLAY、HARD_LIGHT、
              SOFT_LIGHT、DODGE、BURN、ADD、または NORMAL のいずれか。</p>
          <p>利用可能なブレンドモード: normal | multiply | screen | overlay |
                     darken | lighten | color-dodge | color-burn | hard-light |
                     soft-light | difference | exclusion | hue | saturation |
                     color | luminosity</p>
          <p><a
          href="http://blogs.adobe.com/webplatform/2013/01/28/blending-features-in-canvas/">http://blogs.adobe.com/webplatform/2013/01/28/blending-features-in-canvas/</a></p>
        type: Constant
  - line: 1556
    params:
      - name: sx
        description: ''
        type: Integer
      - name: sy
        description: ''
        type: Integer
      - name: sw
        description: ''
        type: Integer
      - name: sh
        description: ''
        type: Integer
      - name: dx
        description: ''
        type: Integer
      - name: dy
        description: ''
        type: Integer
      - name: dw
        description: ''
        type: Integer
      - name: dh
        description: ''
        type: Integer
      - name: blendMode
        description: ''
        type: Constant
chainable: false
---


# blend

