---
title: magSq
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: |
  <p>ベクトルの大きさ（長さ）の二乗を計算します。</p>
line: 1402
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorオブジェクトを作成します。
      let p = createVector(30, 40);

      // 原点から線を描きます。
      line(0, 0, p.x, p.y);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // ベクトルの大きさの二乗を表示します。
      let m = p.magSq();
      text(m, p.x, p.y);

      describe('灰色の正方形の左上隅から対角線上に黒い線が伸びています。線の端に2500という数字が書かれています。');
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ベクトルの大きさの二乗。
  type: Number
overloads:
  - line: 1402
    params: []
    return:
      description: ベクトルの大きさの二乗。
      type: Number
  - line: 3698
    params:
      - name: vecT
        description: |
          <p>大きさの二乗を返すベクトル</p>
        type: p5.Vector
    static: 1
    return:
      description: vecTの大きさの二乗
      type: Number
chainable: false
---

# magSq
