---
title: fromAngle
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: |
  <p>角度から新しい2Dベクトルを作成します。</p>
line: 3099
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = p5.Vector.fromAngle(0);

      // コンソールに "p5.Vector Object : [1, 0, 0]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = p5.Vector.fromAngle(0, 30);

      // コンソールに "p5.Vector Object : [30, 0, 0]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形の中心から黒い矢印が伸びています。矢印は右を指しています。');
    }
    function draw() {
      background(200);

      // 中心にp5.Vectorを作成します。
      let v0 = createVector(50, 50);

      // 角度0と大きさ30のp5.Vectorを作成します。
      let v1 = p5.Vector.fromAngle(0, 30);

      // 黒い矢印を描画します。
      drawArrow(v0, v1, 'black');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
params:
  - name: angle
    description: >
      <p>希望する角度（ラジアン単位）。<a href="#/p5/angleMode">angleMode()</a>の影響を受けません。</p>
    type: Number
  - name: length
    description: |
      <p>新しいベクトルの長さ（デフォルトは1）。</p>
    type: Number
    optional: true
return:
  description: 新しい<a href="#/p5.Vector">p5.Vector</a>オブジェクト。
  type: p5.Vector
chainable: false
---

# fromAngle
