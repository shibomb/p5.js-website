---
title: angleBetween
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2つのベクトル間の角度を計算します。</p>

  <p>返される角度は符号付きで、つまり

  <code>v1.angleBetween(v2) === -v2.angleBetween(v1)</code>となります。</p>

  <p>ベクトルが<a href="/reference/p5/createVector/">createVector()</a>で作成された場合、
  <code>angleBetween()</code>は現在の

  <a href="/reference/p5/angleMode/">angleMode()</a>の単位で

  角度を返します。</p>
line: 2384
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);
      let v1 = createVector(0, 1);

      // コンソールに "1.570..." と表示されます。
      print(v0.angleBetween(v1));

      // コンソールに "-1.570..." と表示されます。
      print(v1.angleBetween(v0));
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 度数法を使用します。
      angleMode(DEGREES);
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);
      let v1 = createVector(0, 1);

      // コンソールに "90" と表示されます。
      print(v0.angleBetween(v1));

      // コンソールに "-90" と表示されます。
      print(v1.angleBetween(v0));
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);
      let v1 = createVector(0, 1);

      // コンソールに "1.570..." と表示されます。
      print(p5.Vector.angleBetween(v0, v1));

      // コンソールに "-1.570..." と表示されます。
      print(p5.Vector.angleBetween(v1, v0));
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 度数法を使用します。
      angleMode(DEGREES);

      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);
      let v1 = createVector(0, 1);

      // コンソールに "90" と表示されます。
      print(p5.Vector.angleBetween(v0, v1));

      // コンソールに "-90" と表示されます。
      print(p5.Vector.angleBetween(v1, v0));
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形の中心から2本の矢印が伸びています。赤い矢印は右を指し、青い矢印は下を指しています。矢印の上には "Radians: 1.57" と "Degrees: 90" というテキストが書かれています。');
    }
    function draw() {
      background(200);

      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(50, 50);
      let v1 = createVector(30, 0);
      let v2 = createVector(0, 30);

      // 赤い矢印を描画します。
      drawArrow(v0, v1, 'red');

      // 青い矢印を描画します。
      drawArrow(v0, v2, 'blue');

      // ラジアンを使用します。
      angleMode(RADIANS);

      // ラジアンでの角度を表示します。
      let angle = round(v1.angleBetween(v2), 2);
      text(`Radians: ${angle}`, 20, 20);

      // 度数法を使用します。
      angleMode(DEGREES);

      // 度数法での角度を表示します。
      angle = round(v1.angleBetween(v2), 2);
      text(`Degrees: ${angle}`, 20, 35);
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ベクトル間の角度。
  type: Number
overloads:
  - line: 2384
    params:
      - name: value
        description: >
          <p><a href="#/p5.Vector">p5.Vector</a>のx、y、zコンポーネント。</p>
        type: p5.Vector
    return:
      description: ベクトル間の角度。
      type: Number
  - line: 3811
    params:
      - name: v1
        description: |
          <p>1つ目のベクトル。</p>
        type: p5.Vector
      - name: v2
        description: |
          <p>2つ目のベクトル。</p>
        type: p5.Vector
    static: 1
    return:
      description: 2つのベクトル間の角度。
      type: Number
chainable: false
---


# angleBetween

