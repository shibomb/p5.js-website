---
title: sub
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルの<code>x</code>、<code>y</code>、<code>z</code>成分から減算します。</p>

  <p><code>sub()</code>は、<code>v.sub(1, 2, 3)</code>のように個別の数値、
  <code>v.sub(v2)</code>のように別の<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクト、
  または<code>v.sub([1, 2, 3])</code>のように数値の配列を使用できます。</p>

  <p>成分に値が提供されない場合、その成分は変更されません。例えば、
  <code>v.sub(4, 5)</code>は<code>v.x</code>から4を、<code>v.y</code>から5を減算し、
  <code>v.z</code>からは0を減算します。引数なしで<code>sub()</code>を呼び出す
  （<code>v.sub()</code>のように）と、何の効果もありません。</p>

  <p><code>sub()</code>の静的バージョン（<code>p5.Vector.sub(v2, v1)</code>のように）は、
  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、
  元のオブジェクトは変更しません。</p>
line: 635
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 点のスタイルを設定します。
      strokeWeight(5);

      // 右下。
      let pos = createVector(75, 75);
      point(pos);

      // 右上。
      // 数値を減算します。
      pos.sub(0, 50);
      point(pos);

      // 左上。
      // p5.Vectorを減算します。
      let p2 = createVector(50, 0);
      pos.sub(p2);
      point(pos);

      // 左下。
      // 配列を減算します。
      let arr = [0, -50];
      pos.sub(arr);
      point(pos);

      describe('灰色の背景に正方形に配置された4つの黒い点。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorオブジェクトを作成します。
      let p1 = createVector(75, 75);
      let p2 = createVector(50, 50);

      // 元のベクトルを変更せずに減算します。
      let p3 = p5.Vector.sub(p1, p2);

      // 点を描画します。
      strokeWeight(5);
      point(p1);
      point(p2);
      point(p3);

      describe('左上から右下への対角線上に3つの黒い点。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形に描かれた3つの矢印。赤と青の矢印が左上から伸びています。紫の矢印が赤い矢印の先端から青い矢印の先端まで伸びています。');
    }

    function draw() {
      background(200);

      let origin = createVector(0, 0);

      // 赤い矢印を描画します。
      let v1 = createVector(50, 50);
      drawArrow(origin, v1, 'red');

      // 青い矢印を描画します。
      let v2 = createVector(20, 70);
      drawArrow(origin, v2, 'blue');

      // 紫の矢印。
      let v3 = p5.Vector.sub(v2, v1);
      drawArrow(v1, v3, 'purple');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 635
    params:
      - name: x
        description: |
          <p>減算するベクトルのx成分。</p>
        type: Number
      - name: 'y'
        description: |
          <p>減算するベクトルのy成分。</p>
        type: Number
        optional: true
      - name: z
        description: |
          <p>減算するベクトルのz成分。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 761
    params:
      - name: value
        description: |
          <p>減算するベクトル</p>
        type: 'p5.Vector|Number[]'
    chainable: 1
  - line: 3405
    params:
      - name: v1
        description: |
          <p>減算元の<a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
      - name: v2
        description: |
          <p>減算する<a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
      - name: target
        description: |
          <p>結果を受け取るベクトル。</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 結果の<a href="#/p5.Vector">p5.Vector</a>
      type: p5.Vector
chainable: true
---


# sub

