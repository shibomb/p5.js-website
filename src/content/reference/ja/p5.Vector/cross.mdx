---
title: cross
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2つのベクトルの外積を計算します。</p>

  <p>外積は、2つのベクトルによって作られる平面から真っ直ぐに飛び出すベクトルです。外積の大きさは、

  元の2つのベクトルによって形成される平行四辺形の面積です。</p>

  <p><code>cross()</code>の静的バージョン、つまり<code>p5.Vector.cross(v1, v2)</code>は、

  <code>v1.cross(v2)</code>を呼び出すのと同じです。</p>
line: 1554
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(1, 0);
      let v2 = createVector(3, 4);

      // 外積を計算します。
      let cp = v1.cross(v2);

      // コンソールに "p5.Vector Object : [0, 0, 4]" と出力します。
      print(cp.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(1, 0);
      let v2 = createVector(3, 4);

      // 外積を計算します。
      let cp = p5.Vector.cross(v1, v2);

      // コンソールに "p5.Vector Object : [0, 0, 4]" と出力します。
      print(cp.toString());
    }
    </code>
    </div>
class: p5.Vector
return:
  description: 外積を<a href="#/p5.Vector">p5.Vector</a>として返します。
  type: p5.Vector
overloads:
  - line: 1554
    params:
      - name: v
        description: |
          <p>外積を取る<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
    return:
      description: 外積を<a href="#/p5.Vector">p5.Vector</a>として返します。
      type: p5.Vector
  - line: 3590
    params:
      - name: v1
        description: |
          <p>1つ目の<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
      - name: v2
        description: |
          <p>2つ目の<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
    static: 1
    return:
      description: 外積。
      type: Number
chainable: false
---


# cross

