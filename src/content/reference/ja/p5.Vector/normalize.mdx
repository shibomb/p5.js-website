---
title: normalize
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p><a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトのコンポーネントを

  スケーリングして、その大きさを1にします。</p>

  <p><code>normalize()</code>の静的バージョン、つまり<code>p5.Vector.normalize(v)</code>は、

  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、

  元のオブジェクトは変更しません。</p>
line: 1726
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorを作成します。
      let v = createVector(10, 20, 2);

      // 正規化します。
      v.normalize();

      // コンソールに "p5.Vector Object : [0.445..., 0.890..., 0.089...]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorを作成します。
      let v0 = createVector(10, 20, 2);

      // 正規化されたコピーを作成します。
      let v1 = p5.Vector.normalize(v0);

      // コンソールに "p5.Vector Object : [10, 20, 2]" と出力します。
      print(v0.toString());
      // コンソールに "p5.Vector Object : [0.445..., 0.890..., 0.089...]" と出力します。
      print(v1.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe("赤と青の矢印が円の中心から伸びています。両方の矢印はマウスに追従しますが、青い矢印の長さは円の半径に固定されています。");
    }

    function draw() {
      background(240);

      // 中心へのベクトル
      let v0 = createVector(50, 50);

      // 中心からマウスへのベクトル
      let v1 = createVector(mouseX - 50, mouseY - 50);

      // 円の半径
      let r = 25;

      // 赤い矢印を描画
      drawArrow(v0, v1, 'red');

      // 青い矢印を描画
      v1.normalize();
      drawArrow(v0, v1.mult(r), 'blue');

      // 円を描画
      noFill();
      circle(50, 50, r * 2);
    }

    // 2つのベクトル間に矢印を描画
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: 正規化された<a href="#/p5.Vector">p5.Vector</a>。
  type: p5.Vector
overloads:
  - line: 1726
    params: []
    return:
      description: 正規化された<a href="#/p5.Vector">p5.Vector</a>。
      type: p5.Vector
  - line: 3711
    params:
      - name: v
        description: |
          <p>正規化するベクトル</p>
        type: p5.Vector
      - name: target
        description: |
          <p>結果を受け取るベクトル</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: '長さ1に正規化されたベクトルv'
      type: p5.Vector
chainable: false
---


# normalize

