---
title: dist
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルで表現された2点間の距離を計算します。</p>

  <p>点の座標は、原点からその点まで伸びるベクトルの成分で表現できます。</p>

  <p><code>dist()</code>の静的バージョンである<code>p5.Vector.dist(v1, v2)</code>は、

  <code>v1.dist(v2)</code>を呼び出すのと同じです。</p>

  <p><code>dist(x1, y1, x2, y2)</code>のように座標を使用して点間の距離を計算するには、

  <a href="/reference/p5/dist/">dist()</a>を使用してください。</p>
line: 1612
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(1, 0);
      let v2 = createVector(0, 1);

      // それらの間の距離を計算します。
      let d = v1.dist(v2);

      // コンソールに "1.414..." と表示されます。
      print(d);
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(1, 0);
      let v2 = createVector(0, 1);

      // それらの間の距離を計算します。
      let d = p5.Vector.dist(v1, v2);

      // コンソールに "1.414..." と表示されます。
      print(d);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形に3本の矢印が描かれています。赤と青の矢印が左上から伸びています。紫の矢印が赤い矢印の先端から青い矢印の先端まで伸びています。紫の矢印の近くに黒で数字36が書かれています。');
    }

    function draw() {
      background(200);

      let origin = createVector(0, 0);

      // 赤い矢印を描画します。
      let v1 = createVector(50, 50);
      drawArrow(origin, v1, 'red');

      // 青い矢印を描画します。
      let v2 = createVector(20, 70);
      drawArrow(origin, v2, 'blue');

      // 紫の矢印。
      let v3 = p5.Vector.sub(v2, v1);
      drawArrow(v1, v3, 'purple');

      // テキストのスタイルを設定します。
      textAlign(CENTER);

      // 大きさを表示します。
      let m = floor(v3.mag());
      text(m, 50, 75);
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: 距離。
  type: Number
overloads:
  - line: 1612
    params:
      - name: v
        description: >
          <p><a href="#/p5.Vector">p5.Vector</a>のx、y、z座標。</p>
        type: p5.Vector
    return:
      description: 距離。
      type: Number
  - line: 3605
    params:
      - name: v1
        description: |
          <p>1つ目の<a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
      - name: v2
        description: |
          <p>2つ目の<a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
    static: 1
    return:
      description: 距離
      type: Number
chainable: false
---


# dist

