---
title: equals
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルのすべての成分が別のベクトルの成分と等しいかどうかをチェックします。</p>

  <p><code>equals()</code>は、ベクトルの成分がすべて別のベクトルの成分と同じ場合に
  <code>true</code>を返し、そうでない場合は<code>false</code>を返します。</p>

  <p>1つのパラメータを持つ<code>equals()</code>のバージョンは、それを別の
  <a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトとして解釈します。</p>

  <p>複数のパラメータを持つ<code>equals()</code>のバージョンは、それらを
  別のベクトルの成分として解釈します。不足しているパラメータには
  0の値が割り当てられます。</p>

  <p><code>equals()</code>の静的バージョン（<code>p5.Vector.equals(v0, v1)</code>のような）は、
  両方のパラメータを<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトとして解釈します。</p>

line: 3001
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(10, 20, 30);
      let v1 = createVector(10, 20, 30);
      let v2 = createVector(0, 0, 0);

      // コンソールに "true" を出力します。
      print(v0.equals(v1));

      // コンソールに "false" を出力します。
      print(v0.equals(v2));
    }
    </code>
    </div>

    <div class = "norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(5, 10, 20);
      let v1 = createVector(5, 10, 20);
      let v2 = createVector(13, 10, 19);

      // コンソールに "true" を出力します。
      print(v0.equals(v1.x, v1.y, v1.z));

      // コンソールに "false" を出力します。
      print(v0.equals(v2.x, v2.y, v2.z));
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(10, 20, 30);
      let v1 = createVector(10, 20, 30);
      let v2 = createVector(0, 0, 0);

      // コンソールに "true" を出力します。
      print(p5.Vector.equals(v0, v1));

      // コンソールに "false" を出力します。
      print(p5.Vector.equals(v0, v2));
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ベクトルが等しいかどうか。
  type: Boolean
overloads:
  - line: 3001
    params:
      - name: x
        description: |
          <p>ベクトルのx成分。</p>
        type: Number
        optional: true
      - name: 'y'
        description: |
          <p>ベクトルのy成分。</p>
        type: Number
        optional: true
      - name: z
        description: |
          <p>ベクトルのz成分。</p>
        type: Number
        optional: true
    return:
      description: ベクトルが等しいかどうか。
      type: Boolean
  - line: 3074
    params:
      - name: value
        description: |
          <p>比較するベクトル。</p>
        type: p5.Vector|Array
    return:
      description: ''
      type: Boolean
  - line: 3869
    params:
      - name: v1
        description: |
          <p>比較する最初のベクトル</p>
        type: p5.Vector|Array
      - name: v2
        description: |
          <p>比較する2番目のベクトル</p>
        type: p5.Vector|Array
    static: 1
    return:
      description: ''
      type: Boolean
chainable: false
---


# equals

