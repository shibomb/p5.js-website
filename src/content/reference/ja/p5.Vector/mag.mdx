---
title: mag
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルの大きさ（長さ）を計算します。</p>

  <p>2Dベクトルの大きさを計算するには、<a href="/reference/p5/mag/">mag()</a>を使用し、
  
  <code>mag(x, y)</code>のようにコンポーネントを指定します。</p>
line: 1362
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // p5.Vectorオブジェクトを作成します。
      let p = createVector(30, 40);

      // 原点から線を描きます。
      line(0, 0, p.x, p.y);

      // テキストのスタイルを設定します。
      textAlign(CENTER);
      textSize(16);

      // ベクトルの大きさを表示します。
      let m = p.mag();
      text(m, p.x, p.y);

      describe('灰色の正方形の左上隅から対角線上に黒い線が伸びています。線の端に数字50が書かれています。');
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ベクトルの大きさ。
  type: Number
overloads:
  - line: 1362
    params: []
    return:
      description: ベクトルの大きさ。
      type: Number
  - line: 3682
    params:
      - name: vecT
        description: |
          <p>大きさを返すベクトル</p>
        type: p5.Vector
    static: 1
    return:
      description: vecTの大きさ
      type: Number
chainable: false
---


# mag

