---
title: random2D
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: |
  <p>ランダムな方向を持つ新しい2D単位ベクトルを作成します。</p>
line: 3243
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = p5.Vector.random2D();

      // コンソールに "p5.Vector Object : [x, y, 0]" と出力します
      // ここで x と y は小さなランダムな数値です。
      print(v.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      // フレームレートを遅くします。
      frameRate(1);

      describe('灰色の正方形の中心から伸びる黒い矢印。1秒ごとに方向が変わります。');
    }

    function draw() {
      background(200);

      // 中心にp5.Vectorを作成します。
      let v0 = createVector(50, 50);

      // ランダムなp5.Vectorを作成します。
      let v1 = p5.Vector.random2D();

      // 描画のためにv1をスケーリングします。
      v1.mult(30);

      // 黒い矢印を描画します。
      drawArrow(v0, v1, 'black');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: 新しい <a href="#/p5.Vector">p5.Vector</a> オブジェクト。
  type: p5.Vector
chainable: false
---

# random2D
