---
title: setHeading
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2Dベクトルを、その大きさを変えずに特定の角度に回転させます。</p>

  <p>慣例により、正のx軸の角度は0です。角度は時計回りに増加します。</p>

  <p>ベクトルが<a href="/reference/p5/createVector/">createVector()</a>で作成された場合、
  <code>setHeading()</code>は現在の<a href="/reference/p5/angleMode/">angleMode()</a>の
  単位を使用します。</p>
line: 2134
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = createVector(0, 1);

      // コンソールに "1.570..." と表示されます。
      print(v.heading());

      // 左を指すように設定します。
      v.setHeading(PI);

      // コンソールに "3.141..." と表示されます。
      print(v.heading());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 度数法を使用します。
      angleMode(DEGREES);

      // p5.Vectorオブジェクトを作成します。
      let v = createVector(0, 1);

      // コンソールに "90" と表示されます。
      print(v.heading());

      // 左を指すように設定します。
      v.setHeading(180);

      // コンソールに "180" と表示されます。
      print(v.heading());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形の中心から2本の矢印が伸びています。赤い矢印は右を指し、青い矢印は下を指しています。');
    }

    function draw() {
      background(200);

      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(50, 50);
      let v1 = createVector(30, 0);

      // 赤い矢印を描画します。
      drawArrow(v0, v1, 'red');

      // 下を指すように設定します。
      v1.setHeading(HALF_PI);

      // 青い矢印を描画します。
      drawArrow(v0, v1, 'blue');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
params:
  - name: angle
    description: |
      <p>回転の角度。</p>
    type: Number
chainable: true
---


# setHeading

