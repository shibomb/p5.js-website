---
title: dot
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2つのベクトルのドット積（内積）を計算します。</p>

  <p>ドット積は、2つのベクトル間の重なりを表す数値です。

  視覚的には、ドット積は一方のベクトルがもう一方のベクトルに投影する「影」と

  考えることができます。ドット積の大きさは、2つのベクトルが同じ方向または

  反対方向を向いているときに最大になります。2つのベクトルが直角を

  形成するとき、その大きさは0になります。</p>

  <p>1つのパラメータを持つ<code>dot()</code>のバージョンは、それを別の

  <a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトとして解釈します。</p>

  <p>複数のパラメータを持つ<code>dot()</code>のバージョンは、それらを別のベクトルの

  <code>x</code>、<code>y</code>、<code>z</code>成分として解釈します。</p>

  <p><code>dot()</code>の静的バージョン（<code>p5.Vector.dot(v1, v2)</code>のような）は、

  <code>v1.dot(v2)</code>を呼び出すのと同じです。</p>
line: 1441
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(3, 4);
      let v2 = createVector(3, 0);

      // ドット積を計算します。
      let dp = v1.dot(v2);

      // コンソールに "9" を出力します。
      print(dp);
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(1, 0);
      let v2 = createVector(0, 1);

      // ドット積を計算します。
      let dp = p5.Vector.dot(v1, v2);

      // コンソールに "0" を出力します。
      print(dp);
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形に描かれた2つの矢印。黒い矢印は右を指し、赤い矢印はマウスに従います。テキスト "v1 • v2 = 何か" はマウスが動くにつれて変化します。');
    }

    function draw() {
      background(200);

      // 中心。
      let v0 = createVector(50, 50);

      // 黒い矢印を描画します。
      let v1 = createVector(30, 0);
      drawArrow(v0, v1, 'black');

      // 赤い矢印を描画します。
      let v2 = createVector(mouseX - 50, mouseY - 50);
      drawArrow(v0, v2, 'red');

      // ドット積を表示します。
      let dp = v2.dot(v1);
      text(`v2 • v1 = ${dp}`, 10, 20);
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ドット積。
  type: Number
overloads:
  - line: 1441
    params:
      - name: x
        description: |
          <p>ベクトルのx成分。</p>
        type: Number
      - name: 'y'
        description: |
          <p>ベクトルのy成分。</p>
        type: Number
        optional: true
      - name: z
        description: |
          <p>ベクトルのz成分。</p>
        type: Number
        optional: true
    return:
      description: ドット積。
      type: Number
  - line: 1542
    params:
      - name: v
        description: |
          <p>ドット積を計算する<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
    return:
      description: ''
      type: Number
  - line: 3576
    params:
      - name: v1
        description: |
          <p>1つ目の<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
      - name: v2
        description: |
          <p>2つ目の<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
    static: 1
    return:
      description: ドット積。
      type: Number
chainable: false
---


# dot

