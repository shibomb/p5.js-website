---
title: rotate
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2Dベクトルを角度だけ回転させます。ただし、その大きさは変更しません。</p>

  <p>慣例により、正のx軸の角度は0とされています。角度は時計回りに増加します。</p>

  <p>ベクトルが<a href="/reference/p5/createVector/">createVector()</a>で作成された場合、
  <code>rotate()</code>は現在の<a href="/reference/p5/angleMode/">angleMode()</a>の
  単位を使用します。</p>

  <p><code>rotate()</code>の静的バージョン（例：<code>p5.Vector.rotate(v, PI)</code>）は、
  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、
  元のオブジェクトは変更しません。</p>
line: 2238
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = createVector(1, 0);

      // コンソールに "p5.Vector Object : [1, 0, 0]" と出力します。
      print(v.toString());

      // 4分の1回転させます。
      v.rotate(HALF_PI);

      // コンソールに "p5.Vector Object : [0, 1, 0]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 度数法を使用します。
      angleMode(DEGREES);

      // p5.Vectorオブジェクトを作成します。
      let v = createVector(1, 0);

      // コンソールに "p5.Vector Object : [1, 0, 0]" と出力します。
      print(v.toString());

      // 4分の1回転させます。
      v.rotate(90);

      // コンソールに "p5.Vector Object : [0, 1, 0]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);

      // 回転したコピーを作成します。
      let v1 = p5.Vector.rotate(v0, HALF_PI);

      // コンソールに "p5.Vector Object : [1, 0, 0]" と出力します。
      print(v0.toString());
      // コンソールに "p5.Vector Object : [0, 1, 0]" と出力します。
      print(v1.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 度数法を使用します。
      angleMode(DEGREES);

      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(1, 0);

      // 回転したコピーを作成します。
      let v1 = p5.Vector.rotate(v0, 90);

      // コンソールに "p5.Vector Object : [1, 0, 0]" と出力します。
      print(v0.toString());

      // コンソールに "p5.Vector Object : [0, 1, 0]" と出力します。
      print(v1.toString());
    }
    </code>
    </div>

    <div>
    <code>
    let v0;
    let v1;

    function setup() {
      createCanvas(100, 100);

      // p5.Vectorオブジェクトを作成します。
      v0 = createVector(50, 50);
      v1 = createVector(30, 0);

      describe('灰色の正方形の中心から黒い矢印が伸びています。矢印は時計回りに回転します。');
    }

    function draw() {
      background(240);

      // v1を回転させます。
      v1.rotate(0.01);

      // 黒い矢印を描画します。
      drawArrow(v0, v1, 'black');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 2238
    params:
      - name: angle
        description: |
          <p>回転角度。</p>
        type: Number
    chainable: 1
  - line: 3491
    params:
      - name: v
        description: ''
        type: p5.Vector
      - name: angle
        description: ''
        type: Number
      - name: target
        description: |
          <p>結果を受け取るベクトル</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 結果として得られる新しい<a href="#/p5.Vector">p5.Vector</a>
      type: p5.Vector
chainable: true
---


# rotate

