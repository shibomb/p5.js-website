---
title: reflect
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2Dの線または3Dの平面についてベクトルを反射させます。</p>

  <p>線または平面の向きは、形状から離れる方向を指す法線ベクトルによって記述されます。</p>

  <p><code>reflect()</code>の静的バージョン（<code>p5.Vector.reflect(v, n)</code>のような形式）は、

  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、

  元のオブジェクトは変更しません。</p>
line: 2869
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // 法線ベクトルを作成します。
      let n = createVector(0, 1);
      // 反射させるベクトルを作成します。
      let v = createVector(4, 6);

      // vをnについて反射させます。
      v.reflect(n);

      // コンソールに "p5.Vector Object : [4, -6, 0]" と出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // 法線ベクトルを作成します。
      let n = createVector(0, 1);

      // 反射させるベクトルを作成します。
      let v0 = createVector(4, 6);

      // 反射したベクトルを作成します。
      let v1 = p5.Vector.reflect(v0, n);

      // コンソールに "p5.Vector Object : [4, -6, 0]" と出力します。
      print(v1.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('中央に垂直な線が引かれたグレーの正方形から3本の矢印が伸びています。黒い矢印は右を指し、青い矢印は左下を指し、赤い矢印は右下を指しています。');
    }
    function draw() {
      background(200);

      // 垂直な線を描画します。
      line(50, 0, 50, 100);

      // 法線ベクトルを作成します。
      let n = createVector(1, 0);

      // 中心。
      let v0 = createVector(50, 50);

      // 反射させるベクトルを作成します。
      let v1 = createVector(30, 40);

      // 反射したベクトルを作成します。
      let v2 = p5.Vector.reflect(v1, n);

      // 描画のために法線ベクトルのスケールを調整します。
      n.setMag(30);

      // 黒い矢印を描画します。
      drawArrow(v0, n, 'black');

      // 赤い矢印を描画します。
      drawArrow(v0, v1, 'red');

      // 青い矢印を描画します。
      drawArrow(v0, v2, 'blue');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 2869
    params:
      - name: surfaceNormal
        description: |
          <p>反射の基準となる<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
    chainable: 1
  - line: 3826
    params:
      - name: incidentVector
        description: |
          <p>反射させるベクトル。</p>
        type: p5.Vector
      - name: surfaceNormal
        description: ''
        type: p5.Vector
      - name: target
        description: |
          <p>結果を受け取るベクトル。</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 反射したベクトル
      type: p5.Vector
chainable: true
---


# reflect

