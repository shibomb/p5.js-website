---
title: limit
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルの大きさを最大値に制限します。</p>

  <p><code>limit()</code>の静的バージョン（例：<code>p5.Vector.limit(v, 5)</code>）は、

  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、

  元のオブジェクトは変更しません。</p>
line: 1835
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = createVector(10, 20, 2);

      // その大きさを制限します。
      v.limit(5);

      // コンソールに "p5.Vector Object : [2.227..., 4.454..., 0.445...]" と出力されます。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(10, 20, 2);

      // コピーを作成し、その大きさを制限します。
      let v1 = p5.Vector.limit(v0, 5);

      // コンソールに "p5.Vector Object : [2.227..., 4.454..., 0.445...]" と出力されます。
      print(v1.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe("円の中心から赤と青の矢印が伸びています。両方の矢印はマウスに従いますが、青い矢印は円の縁を超えることはありません。");
    }
    function draw() {
      background(240);

      // 中心へのベクトル
      let v0 = createVector(50, 50);

      // 中心からマウスへのベクトル
      let v1 = createVector(mouseX - 50, mouseY - 50);

      // 円の半径
      let r = 25;

      // 赤い矢印を描画
      drawArrow(v0, v1, 'red');

      // 青い矢印を描画
      drawArrow(v0, v1.limit(r), 'blue');

      // 円を描画
      noFill();
      circle(50, 50, r * 2);
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 1835
    params:
      - name: max
        description: |
          <p>ベクトルの最大の大きさ。</p>
        type: Number
    chainable: 1
  - line: 3738
    params:
      - name: v
        description: |
          <p>制限するベクトル</p>
        type: p5.Vector
      - name: max
        description: ''
        type: Number
      - name: target
        description: |
          <p>結果を受け取るベクトル（オプション）</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 大きさがmaxに制限されたv
      type: p5.Vector
chainable: true
---

# limit
