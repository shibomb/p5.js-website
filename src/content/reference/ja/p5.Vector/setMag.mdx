---
title: setMag
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルの大きさを指定された値に設定します。</p>

  <p><code>setMag()</code>の静的バージョン、例えば<code>p5.Vector.setMag(v, 10)</code>は、

  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、

  元のオブジェクトは変更しません。</p>
line: 1933
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = createVector(3, 4, 0);

      // コンソールに "5" を出力します。
      print(v.mag());

      // 大きさを10に設定します。
      v.setMag(10);

      // コンソールに "p5.Vector Object : [6, 8, 0]" を出力します。
      print(v.toString());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(3, 4, 0);

      // 大きさ10のコピーを作成します。
      let v1 = p5.Vector.setMag(v0, 10);

      // コンソールに "5" を出力します。
      print(v0.mag());

      // コンソールに "p5.Vector Object : [6, 8, 0]" を出力します。
      print(v1.toString());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('2本の矢印が正方形の左上隅から中心に向かって伸びています。赤い矢印は中心まで到達し、青い矢印は途中までしか伸びていません。');
    }

    function draw() {
      background(240);

      let origin = createVector(0, 0);
      let v = createVector(50, 50);

      // 赤い矢印を描画します。
      drawArrow(origin, v, 'red');

      // vの大きさを30に設定します。
      v.setMag(30);

      // 青い矢印を描画します。
      drawArrow(origin, v, 'blue');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 1933
    params:
      - name: len
        description: |
          <p>このベクトルの新しい長さ。</p>
        type: Number
    chainable: 1
  - line: 3766
    params:
      - name: v
        description: |
          <p>大きさを設定するベクトル</p>
        type: p5.Vector
      - name: len
        description: ''
        type: Number
      - name: target
        description: |
          <p>結果を受け取るベクトル（オプション）</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 大きさがlenに設定されたv
      type: p5.Vector
chainable: true
---


# setMag

