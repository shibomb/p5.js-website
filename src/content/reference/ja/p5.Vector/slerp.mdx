---
title: slerp
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>2つのベクトル間の新しい方向と大きさを計算します。</p>

  <p><code>amt</code>パラメータは、古いベクトルと新しいベクトルの間で補間する量です。
  0.0は方向と大きさを古いベクトルと同じに保ち、0.5は中間に設定し、
  1.0は方向と大きさを新しいベクトルと同じに設定します。</p>

  <p><code>slerp()</code>は<a href="/reference/p5.Vector/lerp/">lerp()</a>と異なり、
  大きさを補間します。<code>v0.slerp(v1, 0.5)</code>を呼び出すと、<code>v0</code>の
  大きさが元の大きさと<code>v1</code>の大きさの中間の値に設定されます。
  <code>v0.lerp(v1, 0.5)</code>を呼び出しても、そのような保証はありません。</p>

  <p><code>slerp()</code>の静的バージョン（<code>p5.Vector.slerp(v0, v1, 0.5)</code>など）は、
  新しい<a href="/reference/p5/p5.Vector">p5.Vector</a>オブジェクトを返し、
  元のオブジェクトは変更しません。</p>
line: 2667
isConstructor: false
itemtype: method
example:
  - |-

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(3, 0);

      // コンソールに "3" を出力します。
      print(v0.mag());

      // コンソールに "0" を出力します。
      print(v0.heading());

      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(0, 1);

      // コンソールに "1" を出力します。
      print(v1.mag());

      // コンソールに "1.570..." を出力します。
      print(v1.heading());

      // v0とv1の中間点まで補間します。
      v0.slerp(v1, 0.5);

      // コンソールに "2" を出力します。
      print(v0.mag());

      // コンソールに "0.785..." を出力します。
      print(v0.heading());
    }
    </code>
    </div>

    <div class="norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(3, 0);

      // コンソールに "3" を出力します。
      print(v0.mag());

      // コンソールに "0" を出力します。
      print(v0.heading());

      // p5.Vectorオブジェクトを作成します。
      let v1 = createVector(0, 1);

      // コンソールに "1" を出力します。
      print(v1.mag());

      // コンソールに "1.570..." を出力します。
      print(v1.heading());

      // v0とv1の中間にあるp5.Vectorを作成します。
      let v3 = p5.Vector.slerp(v0, v1, 0.5);

      // コンソールに "2" を出力します。
      print(v3.mag());

      // コンソールに "0.785..." を出力します。
      print(v3.heading());
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形の中心から3本の矢印が伸びています。赤い矢印は右を指し、青い矢印は左を指し、紫の矢印は下を指しています。');
    }

    function draw() {
      background(200);

      // p5.Vectorオブジェクトを作成します。
      let v0 = createVector(50, 50);
      let v1 = createVector(20, 0);
      let v2 = createVector(-40, 0);

      // v1とv2の中間にあるp5.Vectorを作成します。
      let v3 = p5.Vector.slerp(v1, v2, 0.5);

      // 赤い矢印を描画します。
      drawArrow(v0, v1, 'red');

      // 青い矢印を描画します。
      drawArrow(v0, v2, 'blue');

      // 紫の矢印を描画します。
      drawArrow(v0, v3, 'purple');
    }

    // 2つのベクトル間に矢印を描画します。
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ''
  type: p5.Vector
overloads:
  - line: 2667
    params:
      - name: v
        description: |
          <p>補間先の<a href="#/p5.Vector">p5.Vector</a>。</p>
        type: p5.Vector
      - name: amt
        description: |
          <p>0.0（古いベクトル）と1.0（新しいベクトル）の間の補間量。
                               0.5は中間点です。</p>
        type: Number
    return:
      description: ''
      type: p5.Vector
  - line: 3652
    params:
      - name: v1
        description: |
          <p>古いベクトル。</p>
        type: p5.Vector
      - name: v2
        description: |
          <p>新しいベクトル。</p>
        type: p5.Vector
      - name: amt
        description: ''
        type: Number
      - name: target
        description: |
          <p>結果を受け取るベクトル。</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: v1とv2の間でslerp補間されたベクトル
      type: p5.Vector
chainable: false
---


# slerp

