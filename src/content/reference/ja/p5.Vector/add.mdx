---
title: add
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >
  <p>ベクトルの <code>x</code>、<code>y</code>、<code>z</code> 成分に加算します。</p>

  <p><code>add()</code> は、<code>v.add(1, 2, 3)</code> のように個別の数値、
  <code>v.add(v2)</code> のように別の <a href="/reference/p5/p5.Vector">p5.Vector</a> オブジェクト、
  または <code>v.add([1, 2, 3])</code> のように数値の配列を使用できます。</p>

  <p>成分に値が提供されない場合、その成分は変更されません。例えば、
  <code>v.add(4, 5)</code> は <code>v.x</code> に 4 を、<code>v.y</code> に 5 を加算し、
  <code>v.z</code> には 0 を加算します。
  引数なしで <code>add()</code> を呼び出す（<code>v.add()</code> のように）と、
  何も効果はありません。</p>

  <p><code>add()</code> の静的バージョン（<code>p5.Vector.add(v2, v1)</code> のように）は、
  新しい <a href="/reference/p5/p5.Vector">p5.Vector</a> オブジェクトを返し、
  元のオブジェクトは変更しません。</p>
line: 282
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 点のスタイルを設定
      strokeWeight(5);

      // 左上
      let pos = createVector(25, 25);
      point(pos);

      // 右上
      // 数値を加算
      pos.add(50, 0);
      point(pos);

      // 右下
      // p5.Vectorを加算
      let p2 = createVector(0, 50);
      pos.add(p2);
      point(pos);

      // 左下
      // 配列を加算
      let arr = [-50, 0];
      pos.add(arr);
      point(pos);

      describe('灰色の背景に正方形に配置された4つの黒い点。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      background(200);

      // 左上
      let p1 = createVector(25, 25);

      // 中央
      let p2 = createVector(50, 50);

      // 右下
      // p1とp2を加算
      let p3 = p5.Vector.add(p1, p2);

      // 点を描画
      strokeWeight(5);
      point(p1);
      point(p2);
      point(p3);

      describe('左上から右下への対角線上に3つの黒い点。');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);

      describe('灰色の正方形に描かれた3つの矢印。赤い矢印が左上隅から中央に伸びています。青い矢印が赤い矢印の先端から伸びています。紫の矢印が原点から青い矢印の先端まで伸びています。');
    }

    function draw() {
      background(200);

      let origin = createVector(0, 0);

      // 赤い矢印を描画
      let v1 = createVector(50, 50);
      drawArrow(origin, v1, 'red');

      // 青い矢印を描画
      let v2 = createVector(-30, 20);
      drawArrow(v1, v2, 'blue');

      // 紫の矢印
      let v3 = p5.Vector.add(v1, v2);
      drawArrow(origin, v3, 'purple');
    }

    // 2つのベクトル間に矢印を描画
    function drawArrow(base, vec, myColor) {
      push();
      stroke(myColor);
      strokeWeight(3);
      fill(myColor);
      translate(base.x, base.y);
      line(0, 0, vec.x, vec.y);
      rotate(vec.heading());
      let arrowSize = 7;
      translate(vec.mag() - arrowSize, 0);
      triangle(0, arrowSize / 2, 0, -arrowSize / 2, arrowSize, 0);
      pop();
    }
    </code>
    </div>
class: p5.Vector
overloads:
  - line: 282
    params:
      - name: x
        description: |
          <p>加算するベクトルのx成分。</p>
        type: Number
      - name: 'y'
        description: |
          <p>加算するベクトルのy成分。</p>
        type: Number
        optional: true
      - name: z
        description: |
          <p>加算するベクトルのz成分。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 411
    params:
      - name: value
        description: |
          <p>加算するベクトル</p>
        type: 'p5.Vector|Number[]'
    chainable: 1
  - line: 3353
    params:
      - name: v1
        description: |
          <p>加算する <a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
      - name: v2
        description: |
          <p>加算する <a href="#/p5.Vector">p5.Vector</a></p>
        type: p5.Vector
      - name: target
        description: |
          <p>結果を受け取るベクトル。</p>
        type: p5.Vector
        optional: true
    static: 1
    return:
      description: 結果の <a href="#/p5.Vector">p5.Vector</a>。
      type: p5.Vector
chainable: true
---


# add

