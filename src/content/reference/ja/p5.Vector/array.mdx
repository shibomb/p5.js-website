---
title: array
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: >-
  <p>このベクトルを浮動小数点数配列として表します。</p>

  <p>これは一時的な使用のためのものです。他の用途に使用する場合は、自分自身のベクトルにコピーするために <b>p5.Vector.<a
  href="#/p5.Vector/copy">copy()</a></b> メソッドを使用して、内容をコピーする必要があります。</p>

  <p>/// todo ///</p>

  <p>Returns the vector's components as an array of numbers.</p>


  <hr/>

  <p>Returns the vector's components as an array of numbers.</p>


  <pre>

  todo: update overloads.

  {
      "v": "p5.Vector: 配列に変換するベクトル。"
  }

  </pre>
line: 2979
isConstructor: false
itemtype: method
example:
  - |-

    <div class = "norender">
    <code>
    function setup() {
      // Create a p5.Vector object.
      let v = createVector(20, 30);

      // Prints "[20, 30, 0]" to the console.
      print(v.array());
    }
    </code>
    </div>
class: p5.Vector
return:
  description: |-
    array with the vector's components. /// todo /// Number[]: 3つの値をもつ配列
    array with the vector's components.
  type: 'Number[]'
overloads:
  - line: 2979
    params: []
    return:
      description: array with the vector's components.
      type: 'Number[]'
  - line: 3856
    params:
      - name: v
        description: |
          <p>the vector to convert to an array</p>
        type: p5.Vector
    static: 1
    return:
      description: an Array with the 3 values
      type: 'Number[]'
chainable: false
---


# array
