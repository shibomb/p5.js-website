---
title: array
module: Math
submodule: Vector
file: src/math/p5.Vector.js
description: |
  <p>ベクトルの成分を数値の配列として返します。</p>
line: 2979
isConstructor: false
itemtype: method
example:
  - |-

    <div class = "norender">
    <code>
    function setup() {
      // p5.Vectorオブジェクトを作成します。
      let v = createVector(20, 30);

      // コンソールに "[20, 30, 0]" と出力します。
      print(v.array());
    }
    </code>
    </div>
class: p5.Vector
return:
  description: ベクトルの成分を含む配列。
  type: 'Number[]'
overloads:
  - line: 2979
    params: []
    return:
      description: ベクトルの成分を含む配列。
      type: 'Number[]'
  - line: 3856
    params:
      - name: v
        description: |
          <p>配列に変換するベクトル</p>
        type: p5.Vector
    static: 1
    return:
      description: 3つの値を含む配列
      type: 'Number[]'
chainable: false
---

# array