---
title: subtype
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>ファイルのサブタイプを文字列として表します。</p>

  <p>例えば、<code>'image'</code>の

  <a
  href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types/"
  target="_blank">MIMEタイプ</a>

  を持つファイルは、<code>png</code>や<code>jpeg</code>などのサブタイプを持つ場合があります。</p>
line: 5642
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用して
    // ファイルを読み込み、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルが読み込まれたらdisplaySubtype()を呼び出します。
      let input = createFileInput(displaySubtype);
      input.position(0, 100);

      describe('グレーの正方形と、その下にファイル入力があります。ユーザーがファイルを読み込むと、そのサブタイプが黒字で書かれます。');
    }

    // ファイルが読み込まれたらp5.Fileのタイプを表示します。
    function displaySubtype(file) {
      background(200);

      // p5.Fileのサブタイプを表示します。
      text(`このファイルのサブタイプは: ${file.subtype}`, 10, 10, 80, 80);
    }
    </code>
    </div>
class: p5.File
---


# subtype

