---
title: data
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>ファイルのデータを含む文字列です。</p>
  <p>データは画像データ、テキストの内容、または JSON や <a href="/reference/p5/p5.XML">p5.XML</a> オブジェクトの場合はパースされたオブジェクトである可能性があります。</p>
line: 5751
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用してファイルをロードし、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルがロードされたら displayData() を呼び出します。
      let input = createFileInput(displayData);
      input.position(0, 100);

      describe('グレーの正方形と、その下にファイル入力があります。ユーザーがファイルをロードすると、そのデータが黒字で書かれます。');
    }

    // p5.File のデータがロードされたら表示します。
    function displayData(file) {
      background(200);

      // p5.File のデータを表示します。ランダムな文字列のように見えます。
      text(file.data, 10, 10, 80, 80);
    }
    </code>
    </div>
class: p5.File
---


# data

