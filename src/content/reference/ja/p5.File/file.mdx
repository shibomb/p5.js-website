---
title: file
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>基礎となる

  <a href="https://developer.mozilla.org/en-US/docs/Web/API/File/"
  target="_blank">File</a>

  オブジェクト。すべての<code>File</code>プロパティとメソッドにアクセスできます。</p>
line: 5555
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用して
    // ファイルを読み込み、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルが読み込まれたらdisplayInfo()を呼び出します。
      let input = createFileInput(displayInfo);
      input.position(0, 100);

      describe('グレーの正方形と、その下にファイル入力があります。ユーザーがファイルを読み込むと、その情報が黒字で書かれます。');
    }

    // p5.Fileが読み込まれたら使用します。
    function displayInfo(file) {
      background(200);

      // p5.Fileの名前を表示します。
      text(file.name, 10, 10, 80, 40);

      // p5.Fileのタイプとサブタイプを表示します。
      text(`${file.type}/${file.subtype}`, 10, 70);

      // p5.Fileのサイズをバイト単位で表示します。
      text(file.size, 10, 90);
    }
    </code>
    </div>
class: p5.File
---


# file

