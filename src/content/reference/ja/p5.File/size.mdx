---
title: size
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>ファイルのバイト数。</p>
line: 5716
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用してファイルを読み込み、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルが読み込まれたらdisplaySize()を呼び出します。
      let input = createFileInput(displaySize);
      input.position(0, 100);

      describe('グレーの正方形と、その下にファイル入力があります。ユーザーがファイルを読み込むと、そのサイズがバイト単位で黒字で書かれます。');
    }

    // ファイルが読み込まれたら、p5.Fileのサイズをバイト単位で表示します。
    function displaySize(file) {
      background(200);

      // p5.Fileのサイズを表示します。
      text(`このファイルは ${file.size} バイトです。`, 10, 10, 80, 80);
    }
    </code>
    </div>
class: p5.File
---


# size

