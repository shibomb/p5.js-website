---
title: name
module: DOM
submodule: DOM
file: src/dom/dom.js
description: |
  <p>ファイル名を文字列として表します。</p>
line: 5681
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用して
    // ファイルを読み込み、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルが読み込まれたらdisplayName()を呼び出します。
      let input = createFileInput(displayName);
      input.position(0, 100);

      describe('グレーの正方形とその下にファイル入力があります。ユーザーがファイルを読み込むと、その名前が黒字で書かれます。');
    }

    // p5.Fileの名前を読み込み後に表示します。
    function displayName(file) {
      background(200);

      // p5.Fileの名前を表示します。
      text(`このファイルの名前は: ${file.name}`, 10, 10, 80, 80);
    }
    </code>
    </div>
class: p5.File
---


# name

