---
title: type
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>ファイルの

  <a
  href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types/"
  target="_blank">MIMEタイプ</a>

  を文字列として表します。</p>

  <p>例えば、<code>'image'</code>と<code>'text'</code>はどちらもMIMEタイプです。</p>
line: 5604
isConstructor: false
itemtype: property
example:
  - |-

    <div>
    <code>
    // ファイル入力を使用してファイルを読み込み、その情報を表示します。

    function setup() {
      createCanvas(100, 100);

      background(200);

      // ファイル入力を作成し、キャンバスの下に配置します。
      // ファイルが読み込まれたらdisplayType()を呼び出します。
      let input = createFileInput(displayType);
      input.position(0, 100);

      describe('グレーの正方形とその下にファイル入力があります。ユーザーがファイルを読み込むと、そのタイプが黒字で書かれます。');
    }

    // ファイルが読み込まれたらp5.Fileのタイプを表示します。
    function displayType(file) {
      background(200);

      // p5.Fileのタイプを表示します。
      text(`このファイルのタイプは: ${file.type}`, 10, 10, 80, 80);
    }
    </code>
    </div>
class: p5.File
---


# type

